{
  "name": "@laika/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The single Node process that is Laika: API, MCP, SSE, cron, and the built SPA.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx watch src/index.ts",
    "db:generate": "drizzle-kit generate",
    "build": "pnpm run clean && tsc -p tsconfig.build.json && pnpm run build:assets",
    "build:assets": "cp -R src/static dist/static && cp -R src/db/migrations dist/db/migrations",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.20.1",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.12",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "better-auth": "^1.7.1",
    "better-sqlite3": "^13.0.3",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.3",
    "ulid": "^3.0.2",
    "zod": "^4.4.3"
  }
}
