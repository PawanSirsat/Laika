# dependencies
node_modules/

# build output
dist/
server/public/

# sqlite
*.db
*.db-wal
*.db-shm
data/

# env
.env*

# test output
coverage/
*.lcov

# tool caches
.eslintcache
.prettiercache
*.tsbuildinfo

# tool scratch output
#
# Written into the working tree by tooling, never source. Ignored by name rather
# than with a broad `.*` rule, because `.prettierrc`, `.npmrc`, `.nvmrc` and
# `.prettierignore` are all tracked deliberately and a blanket pattern would hide
# them. `pnpm format` reads this file via `--ignore-path`, so an entry here is
# also what keeps the repo-wide format gate green — a gate that goes red for
# reasons unrelated to the code stops being read. (LAI-031)
.playwright-mcp/
playwright-report/
test-results/

# os / editor noise
.DS_Store

# Local, per-machine Claude Code settings. Ignored here rather than relying on
# a developer global gitignore — otherwise `pnpm format` checks a file git ignores
# and the gate goes red on one machine and green on another.
.claude/settings.local.json
