# CHIEF — 2026-09-19

## 2026-09-19T09:30:00Z — [ops] the task opens as a centred modal

**What changed**
- docs/DECISIONS.md — D-063: a centred modal, not a right-hand drawer. Amends
  D-059.2. Also retires pixel figures from §11.4.2's table.
- docs/SPEC.md §11.4.2 — the table drops `840px`, gains a `decision` column, and
  each row now names the decision governing it.
- .tasks/backlog/LAI-476-task-opens-as-a-centred-modal.md — SHELL, p1,
  `depends-on: [LAI-285]`.

**Decisions**
- **Scoped it to the frame and said so twice.** LAI-285 already made the drawer
  two columns at 1120px, which is the shape a Jira modal has; what the owner is
  asking for is the *box*. Named the out-of-scope half explicitly — breadcrumb,
  epic link, attachments, subtasks, minimise, the `Details` disclosure — because
  a screenshot invites reproducing everything in it, and **those are Jira's data
  model, not ours.**
- **Centred in the scrimmed region, not the viewport.** The scrim starts at
  `--rail-width` so the sidebar stays usable with a task open — LAI-252 did that
  deliberately. Dimming the whole window to gain symmetry trades a working
  behaviour for a few pixels. Recorded the alternative in D-063 as a revisit
  trigger rather than leaving it unsaid.
- **Folded the dialog semantics into the same task.** `TaskDrawer.tsx` has no
  `role`, no `aria-modal`, no label and no focus trap today — it focuses the
  panel and listens for Escape, which is most of the behaviour and none of the
  semantics. **A panel pinned to an edge is arguably a complementary region; a
  centred box over a dimmed page is a modal dialog and nothing else.** So this is
  not scope creep onto the owner's request — it is what the request makes true.
  Said that in D-063 in those words so it does not read as me padding the task.
- **Required a maximum height, asserted at two viewport heights.** A drawer is
  viewport-height by construction and a centred box is not; "as tall as its
  content" is how a modal ends up with its close button off-screen. Same
  two-height reasoning as LAI-475, for the same reason: one height is what a
  wrong constant already satisfies.
- **Renaming `drawer/` is allowed but not required, and in its own commit.** The
  name is wrong after this, but a rename tangled into the reshaping diff makes
  the reshaping unreviewable.

**Discovered**
- **SPEC §11.4.2's table was wrong on two of its three rows.** It said `840px`
  for the task detail — **LAI-285 shipped `1120` and nothing noticed** — and it
  said three spaces until D-061 yesterday. **Nothing parses §11.4 prose.**
- **That is the second §11.4 drift in two days, so I fixed the class rather than
  the instance.** Yesterday I wrote that a guard over prose is a real design
  question and declined to file one on a hunch. With a second measured instance
  the cheaper answer became clear and it is not a guard: **§11.4.2.1 says what a
  screen must contain, so a pixel width in it duplicates a CSS value nothing can
  check it against.** The figures are gone. **A number that cannot be falsified
  is worse than no number** — it reads as authoritative to whoever finds it
  first, which is exactly how `840` survived LAI-285.
  **Narrow on purpose:** §4's tables have `schema-spec-drift.test.ts` behind them
  and stay exact. The rule is *a figure belongs in the SPEC when something can
  falsify it*, not *the SPEC should be vaguer*.
- **A table headed with one decision id misattributes every row later amended.**
  The table was headed `D-059`; after D-061 its sidebar row was D-061's value
  under D-059's name. Each row now carries its own decision. **I caused that
  misattribution yesterday** by editing the row and not the header.
- **The design file draws a right drawer** — `position:absolute; top:0; right:0;
  bottom:0; width:840px` in `Laika 02-04`. So this is a departure from the
  artefact, not from a reading of it. Flagged in the task so nobody "restores
  fidelity", and flagged as *a record of an earlier decision* rather than as
  wrong.
- **SHELL's review queue is 27 deep**, up from 22 yesterday, and **LAI-476
  depends on LAI-285 which is in it.** The backlog is now gated on my review
  throughput rather than on SHELL's.

**State**
LAI-476 in `.tasks/backlog/`, unclaimed, p1, blocked on LAI-285 (in review).
Gate green on `master` — `TEST 0 / LINT 0 / FMT 0`, 106 files, 1938 tests. Not
pushed. Four tasks now queued for SHELL (LAI-473, 474, 475, 476) behind a 27-deep
review queue. **Clearing that queue is the next thing I should do.**

## 2026-09-19T10:05:00Z — [ops] LAI-476's dependency was mine, not the task's

**What changed**
- .tasks/backlog/LAI-476-task-opens-as-a-centred-modal.md — `depends-on:
  [LAI-285]` → `[]`, and the note rewritten to say why.

**Decisions**
- **Removed the dependency. The owner was right and I was wrong.** Checked
  before changing it: `shell:server/web/src/components/drawer/drawer.css` is
  already at `1120px` with LAI-285's two columns committed. **SHELL's tree has
  the code; only the *review* is outstanding.**
- **The error was treating `depends-on` as "related to", not "cannot proceed
  without".** §2 reads it as *present in `.tasks/done/` on `master`*, so writing
  LAI-285 there converted **my review backlog into a builder's blocker**. That is
  CHIEF's throughput problem leaking downstream, and it is the opposite of what
  the field is for.
- **The frame genuinely does not depend on the contents.** Centring a box,
  capping its height and giving it dialog semantics is true of a one-column panel
  and a two-column one alike. If LAI-285 were sent back tomorrow, LAI-476 would
  still build.
- **Kept the real consideration and demoted it to sequencing.** Both tasks edit
  `.drawer` in `drawer.css`; one-task-per-session means they happen in series on
  one branch, so there is nothing to collide. Told SHELL to log it rather than
  absorb it silently if LAI-285 comes back mid-flight.
- **Audited the other four while I was there.** LAI-473 → LAI-472 is the only
  remaining `depends-on` of mine and it is real: LAI-472 is unclaimed in
  `backlog/`, its endpoint does not exist, and §5.1 forbids building a screen
  before the endpoints it calls. LAI-474, 475, 476 are all `[]`.

**Discovered**
- **A dependency on a task that is already *built* is not a dependency.** The
  distinction that matters is where the *code* is, not where the *task file* is
  — and for a builder working on their own branch, a task in `review/` has
  already delivered its code to them. **§2's `done/`-on-`master` test is the
  right check for work that has not been written; it is the wrong instinct for
  work that has.**
  Same family as §2's own note that `(absent)` is not `free` — **asking the
  wrong artefact where something is.** I checked the task file's directory when
  the question was about the branch's file tree.
- **This is the second time in two days that my own instrument pointed the wrong
  way** (the id sweep on the 18th, this today). Both were *"the check I ran
  answers a different question from the one I had"*, and both were caught by
  someone else — the gate then, the owner now.

**State**
LAI-476 claimable immediately, p1, no dependencies. Gate green — `TEST 0 /
LINT 0 / FMT 0`, 106 files, 1938 tests. Not pushed. SHELL now has **three**
immediately-claimable tasks (LAI-474, 475, 476) and one blocked (LAI-473).
