# CORE — 2026-09-01

## 2026-09-01T09:35:00Z — LAI-409

**What changed**
- `server/test/mcp/parity.test.ts` (new, 15 tests)

**Decisions**
- **`finish_task`'s twin is a composite, not an exemption.** There is no
  `POST /tasks/:id/finish`. A human finishing work does two things — moves the
  task to review, and says what they did — so the twin is those two REST calls
  in sequence. That is a real pair rather than a second exemption, and it is
  precisely what proves LAI-408's one-transaction service did not become a
  third write path. Had I exempted it, the one tool most likely to drift would
  have been the one nothing checked.
- **Two clean databases per pair**, so the comparison is structural rather than
  an artefact of one run following the other.
- **`normalise` strips ids, clocks, `actor_kind` and `actor_token_id`** — and a
  separate test asserts the last two *do* differ. Without it the parity tests
  would pass on a tool that forgot it was an agent, which is the one thing an
  agent's row is supposed to say.
- **Compared through `GET /projects/:slug/activity`**, which serialises with the
  same `eventView` the SSE stream uses — so this compares what a browser
  receives, not what the table stores (AC5).
- **Both directions of the derived list.** A tool with no pair is named; so is a
  pair for a tool that no longer exists. A stale pair reads as coverage and is
  not.

**The mutation that taught me what this test actually guards**

My first attempt at AC6 mutated the **shared service** — `createTask` writing
`task.updated` instead of `task.created`. The `create_task` pair **stayed
green**, and something else went red.

That is the property working, and it took a minute to see: both sides call the
same service, so changing it moves them together. **Parity cannot be broken by
changing the thing both halves share** — which is exactly what the layering
rules guarantee, and exactly why CHIEF was right that this test *confirms* a
structural property rather than propping it up.

The mutation that matters is the **tool diverging from the route**. All three
name the tool:

```
× create_task — identical activity, differing only in attribution
× start_working — identical activity, differing only in attribution
× list_projects — identical activity, differing only in attribution
```

- `create_task` writing an extra comment the route does not → red.
- `start_working` reaching for `changeStatus` instead of `claimTask` → red.
  That one is the realistic failure: a plausible-looking substitution that
  produces a different verb.
- A read tool writing anything → red.
- A REST twin for `log_unlisted_work` appearing → the self-expiry fires and
  says to remove the entry.

**Discovered**
- **A mutation that goes red is not automatically a mutation that proved
  anything.** My first one went red on *"the agent's rows say agent"* — a
  different test entirely — and I would have recorded AC6 as satisfied had I
  only read the exit code. The harness prints the failing test name for exactly
  this reason, and this is the second time today that reading it mattered.
- **My second mutation did not write anything.** I "made a read tool write" by
  adding a `listTasks` call — which is a read. It stayed green because the code
  was still correct, not because the test was weak. Both of my first two
  mutations were wrong in different ways, and the test was fine.
- **`pnpm typecheck` caught two errors `pnpm test` did not** — an unused
  parameter and a `Map` literal whose element type TypeScript inferred from its
  first entry, so the one carrying a `note` no longer matched. Sixth occurrence
  today; LAI-136 is filed and now p2.
- nothing filed.

**State**
In review. M3's server chain is complete: LAI-402 → 403 → 406 → 407/408 → 409.

## 2026-09-01T10:25:00Z — LAI-417

**What changed**
- `server/src/services/heartbeats.ts` (new), `http/routes/heartbeats.ts` (new)
- `server/src/policy/actions.ts`, `can.ts` — `heartbeat.send_own`
- `server/src/app.ts` — mounts `/api/v1/heartbeats`
- `server/test/http/routes/heartbeats.test.ts` (new, 15),
  `server/test/services/heartbeats.test.ts` (new, 9)
- `server/test/policy/*`, `tooling/policy-spec-drift.test.ts` — the new action

**Decisions**
- **Asked before building, for the third time.** §3.1 had no cell and AC3 says
  stop and file rather than reuse something plausible. CHIEF ratified the row
  unchanged. It costs one message and nothing has had to be unpicked.
- **Token-only is enforced in the route, not by `can()`.** §9.1 restricts the
  *credential*; §3.1 grades by *role*. `can()` cannot express it, and a cookie
  resolves a perfectly good actor — which is exactly why the refusal has to be
  explicit rather than assumed to fall out of the permission check.
- **No `activity` row.** §4.8's verb is `heartbeat.session` — a session, not a
  ping. CHIEF checked §4.8 independently before agreeing. Said in a comment so
  the absence reads as a decision.
- **`matched_task_id` stays null and `202` rather than `201`.** §9.2 is M5, and
  nothing is created that a client can go and read.
- **Bounds in the service as well as the route**, because the plugin and a
  future MCP tool reach it without passing through zod (LAI-404's lesson).

**Two things the tests found that I had wrong**

1. **Deleting `assertCan` left every test green.** `heartbeat.send_own` is ✓ for
   every role, so the role never refuses — what refuses is the credential, and I
   had no `read_only` token in the suite. The gate was unguarded and the
   mutation is the only reason I know. *A permission test needs an actor for
   whom the permission actually differs* — LAI-081, LAI-405, and now here.
2. **A Viewer holding a token stored as `full` is still refused.** I expected
   that to pass. `can()` applies `effectiveTokenScope` at **check** time rather
   than trusting the stored row, so a token minted while somebody was a Member
   stops writing the moment they are demoted, with nothing revoking it. Stronger
   than what I was testing for, and now asserted as its own case.

**Discovered**
- **The `heartbeats.token_id` foreign key caught a fake fixture.** My service
  tests used a synthetic token id; the route tests mint real ones through the
  API and never met it. The constraint refused it immediately, which is the
  schema doing its job — a fabricated id would otherwise have sat in a test
  fixture claiming to be a token.
- **My branch is green this time, unlike LAI-408**, because the exemption entry
  went in from the start rather than after the fact. Verified it is
  load-bearing: removing it turns the drift test red.
- nothing filed.

**State**
In review. §3.1's row is CHIEF's half and lands in the merge commit.

## 2026-09-01T10:50:00Z — LAI-136

**What changed**
- `server/package.json` — `test` is now `pnpm run typecheck && vitest run`

**Decisions**
- **A composite, not a `pretest` hook.** Both work; the composite is visible in
  `package.json` where somebody reads what `test` does. A `pretest` hook is
  invisible at the call site, and the whole defect here was a check that was not
  where people looked.
- **`test:watch` left alone**, so the fast inner loop is unchanged.
- **The web half is filed as LAI-141, not fixed here.** `server/web/package.json`
  is SHELL's (D-016, D-031) and this task's own Notes said to split rather than
  reach across. `node --test` strips types exactly as vitest does, so the hole is
  identical and the reason for the split is ownership alone.

**Proof, and the cost**

The probe is the class that actually bit — `noUncheckedIndexedAccess`:

```
src/services/heartbeats.ts(120,3): error TS2322:
  Type 'string | undefined' is not assignable to type 'string'.
```

`pnpm --filter @laika/server test` now exits **2** and names the file and line.
Before this change that file ran green.

**Measured**: typecheck 3.3s, vitest 26.8s, composite **31.6s** — about **12%**
slower. Cheap for a run that can no longer be green over a file that does not
compile.

**Discovered**
- Six occurrences on this branch alone in a day and a half: LAI-402's test
  helpers, LAI-403's `TokenContext` literals, LAI-406's SDK transport cast,
  LAI-407's result types, LAI-409's `Map` inference, and LAI-405's unused
  import. Every one was caught by `pnpm typecheck` after `pnpm test` was green.
- nothing filed beyond LAI-141.

**State**
In review.

## 2026-09-01T11:20:00Z — LAI-137

**What changed**
- `server/test/tooling/build.test.ts` — the build's outcome is captured rather
  than thrown; one test asserts on it; the rest skip with a reason; the module
  comment records why this is not a slower `pnpm typecheck`

**The comparison, which is the deliverable**

Probe: a `src` file importing from `test/`. It **passes `pnpm typecheck`** and
**fails `pnpm run build`**, because `tsconfig.build.json` sets `rootDir: "src"`
— the same class as LAI-045's cast, and the reason this file is a separate gate.

```
BEFORE   Tests  12 skipped (12)

AFTER    × compiles at all
         AssertionError: pnpm run build failed:
         src/version.ts(28,25): error TS6059: File '…/test/helpers/db.ts' is not
           under 'rootDir' '…/server/src'.
         Tests  1 failed | 12 skipped (13)
```

**Decisions**
- **One failure and twelve skips, not thirteen failures.** My first version threw
  from a guard, which produced `13 failed` — loud, but it turns one cause into
  thirteen and invites the reader to think thirteen things broke. The skips are
  honest: those tests genuinely did not run. What was wrong before was not the
  word `skipped` but that it sat beside **zero failures**.
- **The compiler's own output is in the message.** `execFileSync` with
  `stdio: 'pipe'` puts it on the error; without it the failure says "Command
  failed" and sends someone to re-run the build by hand to find out why.
- **The build still runs once**, in `beforeAll`. It is the slowest thing in the
  suite and must not become per-test.

**Discovered**
- **My commit message overclaimed.** It said the typecheck distinction was
  "written into the module comment" and it was not — AC4's requirement, missed.
  Caught by checking the claim instead of trusting it, and fixed in a follow-up
  that says so rather than an amend that would have hidden it. Same defect as a
  commit describing a change it does not carry, which CHIEF hit this morning;
  the check that catches it is reading the diff against the message.
- **A regex rewrite silently dropped `async`** from three tests while converting
  them to take a context. `pnpm typecheck` caught it — the first time today the
  new LAI-136 composite would have caught something on its own.
- nothing filed.

**State**
In review.

## 2026-09-01T12:15:00Z — LAI-057

**What changed**
- `server/src/shutdown.ts` — `createRuntimeShutdown`, the composition extracted
- `server/src/index.ts` — four lines become one call
- `server/test/shutdown.test.ts` — 4 wiring tests
- `server/test/tooling/build.test.ts` — an end-to-end shutdown-timing test

**Decisions**
- **The wiring went into `shutdown.ts`, not a new `bootstrap.ts`.** It is the
  shutdown handler configured for Laika's runtime, so it belongs beside the
  handler; and `shutdown.test.ts` already exists as its mirror, so no new module
  and no new exemption.
- **Order is asserted, not just occurrence.** `['feed', 'server', 'sqlite']` —
  closing the listener first would wait out the grace and then cut the stream
  mid-frame, which is the actual failure, and a test that only checked "closeAll
  was called" would pass on it.
- **One test drives a real `ActivityFeed`.** The doubles prove the wiring; this
  proves the thing it is wired to still notifies. Without it, `closeAll` could be
  renamed to something inert and every other test would stay green — which is the
  exact shape of the bug being fixed, one level down.
- **The end-to-end test asserts a number.** It exits either way; "it exited" is
  true whether or not the feed was closed, and that is why this went unnoticed.

**The end-to-end test measured something nobody had**

| | shutdown |
| --- | --- |
| no stream open (LAI-406) | 143ms |
| one SSE stream open, wiring intact | **4016ms** |
| one SSE stream open, `onStopping` severed | 10013ms |

The 10s case is the regression this guards. **The 4s is new**, and it is the
server's own timing — `shutdown.start` → `shutdown.complete` in its log is
4005ms, so it is not the test client. Filed as **LAI-142** rather than fixed:
this task guards the wiring and explicitly is not a rewrite.

The threshold sits at **7s**, between the two measurements. Deliberately not
tighter: pinning it at 4s would freeze a stall as the standard.

**Discovered**
- **LAI-406's 143ms was measured without a stream open**, which I did not think
  about at the time. It was true and it was not the interesting case — the whole
  point of `onStopping` is what happens when a stream *is* open, and that is the
  measurement I did not take. A number that is right about the easy case reads
  as reassurance about the hard one.
- The three severing mutations — feed not closed, database not closed, order
  reversed — all go red, including the exact one that passed 560 tests during
  the LAI-048 review.
- LAI-142 filed (discovered-from: LAI-057).

**State**
In review.

## 2026-09-01T12:55:00Z — LAI-093

**What changed**
- `server/src/db/activity.ts` — `creatingClientNames`, a batched
  `activity` → `tokens` join
- `server/src/services/tasks.ts` — `TaskView.created_by_client`, loaded through
  the existing `ViewContext`
- `server/test/services/tasks.test.ts` — 6 tests
- `server/test/db/activity.test.ts` — the exact-exports list

**Decisions**
- **Derived, no column.** The criterion said to check first, and it is reachable:
  `activity.actor_token_id` is on every row and `tokens.name` is the name. The
  test that makes this concrete is *"follows a rename rather than keeping the old
  name"* — a stored copy would still say `mira-cli` after the token was renamed,
  and nothing would ever reconcile them.
- **`null`, never `"unknown"`.** Three different causes — browser session, task
  older than tokens, token since deleted — all mean the same thing to a reader:
  the channel is known and the client is not. Inventing a word for it would be
  worse than the honest absence, and a blank string would render as a gap.
- **Batched into `ViewContext`.** `listTasks` returns up to 200 and
  `commentCounts` and `tagsForTasks` already sit there for this reason.
- **Not done for comments.** `activity` has a `task_id` column but no
  `comment_id` — the id is in the payload JSON, so the join would mean matching
  serialised text. The criterion said not to widen if it was not free, and it is
  not.

**Discovered**
- **My mutation harness bypasses the gate I added yesterday.** The
  `innerJoin → leftJoin` mutation reported `STAYED GREEN`, and I nearly recorded
  it as a test gap. It is not: `pnpm typecheck` rejects it —
  `'string | null' is not assignable to 'string'` — and typecheck is part of
  `pnpm test` since LAI-136. The harness runs `npx vitest` directly, so it does
  not see it.
  **So "stayed green" from that harness means "vitest did not catch it", not
  "nothing catches it".** I have been reading it as the stronger claim. Worth
  fixing the harness or reading its output more narrowly; for now, a green
  mutation deserves a typecheck before it is called a gap.
- **A SPEC line may be worth adding and it is CHIEF's.** §4.5's table is columns
  and this is deliberately not one, so it does not belong there — but `tokens.name`
  is now a wire-visible value, which constrains what renaming or deleting a token
  means. Proposed to CHIEF rather than assumed; nothing filed yet.
- nothing filed.

**State**
In review.

## 2026-09-01T15:05:00Z — LAI-094, watch + mentions

**What changed**
- `server/src/db/schema.ts` — `task_watchers`, `comment_mentions`; migration
  `server/src/db/migrations/0011_cooing_gambit.sql`.
- `server/src/services/watchers.ts`, `server/src/services/mentions.ts` — new.
- `server/src/services/comments.ts` — `syncMentions` on create and on edit.
- `server/test/services/watchers.test.ts` (17), `.../mentions.test.ts` (15).
- `server/test/db/schema.test.ts`, `server/test/tooling/schema-spec-drift.test.ts`
  — the table list, and two in-flight exemptions.
- `.tasks/backlog/LAI-143-watch-and-mention-endpoints.md` — new.

**Decisions**

*Watching is derived; the table records only the exceptions.* Assignee,
commenter and mentioned-person already exist in three tables, so the set is
computed at read time. Materialising it means a write on every assign and
comment, drift the first time a path forgets one, and — the part that decided
it — every commenter from before this feature would be a non-watcher. Same
reasoning `commentCounts` already uses.

*`watching = 0` rather than deleting the row.* Three states have to be
distinguishable and row-presence expresses two. The one it loses is the one that
matters: somebody who unwatches is resubscribed by their own next comment.

*Ambiguity is judged across every user row, deactivated included.* Filtering to
active users would make `@ada` look unambiguous exactly when a second Ada exists.

*No endpoints.* The criteria ask for the substrate; §6 rows are CHIEF's. Filed
LAI-143 rather than widening in flight — the same rule I asked CHIEF to hold to
on LAI-059, which does not stop applying when it is inconvenient for me.

**Discovered**

*A bug found by reading, not by a test.* `tasksWatchedBy`'s mention lookup
joined `task_watchers` instead of `comment_mentions`, so it found tasks where the
person already had a row and missed every implicit mention. Typecheck was clean.
It is now covered — re-introducing it turns `collects all four reasons` red.

*A test helper that ignored its own argument.* `mentionRows(commentId)` selected
every row and `.filter(() => true)`. With one comment in the fixture every
assertion still passed, and the edit test would have passed had `syncMentions`
written to the wrong comment entirely. Same class as LAI-407's unasserted setup
call: the check ran, but not on what it named.

*`drizzle-kit generate` emits unformatted JSON, and `format:fix` cannot see it.*
`_journal.json` and `0011_snapshot.json` were committed in my first commit, so
they left `git diff HEAD` and the fixer's file list. Only repo-wide `pnpm format`
caught them. This is exactly **LAI-101**, already in backlog — logged here rather
than filed again.

*Nine mutations, all caught.* Four on `mentions.ts` (drop the `project.read`
check; ambiguous picks the first; edit merges instead of re-deriving;
soft-deleted comments still count) and five on `watchers.ts` (the original join
bug; explicit-0 stops suppressing, in each of the two paths; no read filter on
the set; own-only removed).

**State**
In review. One thing is red on `core` and CHIEF knows: `has a §4 section for
every table` needs §4.18/§4.19, which CHIEF holds and applies in the merge. Both
exemptions self-expire — verified by adding `tasks` to the map and watching the
staleness guard fire.

## 2026-09-01T15:40:00Z — LAI-094 landing, second half of §4.4

CHIEF merged §4.18/§4.19 into local `master`. Merged it, **confirmed
`drops a table exemption once §4 describes the table` was red before touching
anything**, dropped both in-flight entries, re-gated, pushed `core`.

**A red lint that was not a red branch.** First `pnpm lint` after the merge: 72
errors across LAI-227's three new `server/web/test/browser/` files, all
"Unsafe member access … on a type that cannot be resolved". `playwright` is
declared in `server/web/package.json` and present in `pnpm-lock.yaml`, but was
not in this worktree's `node_modules` — the merge brought a dependency and
nothing had installed it. `pnpm install --frozen-lockfile`, lockfile untouched,
lint clean.

Worth writing down because of what it looked like: eslint reports it as **72
errors in another session's files** and says nothing about a missing package. I
nearly reported a red branch to CHIEF and filed against SHELL. Any worktree that
has not installed since LAI-227 will see the same thing.

## 2026-09-01T16:05:00Z — LAI-116, heartbeat repo attribution

**What changed**
- `server/src/services/heartbeats.ts` — `resolveRepoProjects`,
  `branchProjectPrefix`, `RepoAttribution`; the view carries the resolution.
- `server/src/http/routes/heartbeats.ts` — `heartbeat.repo_unmatched` (warn),
  `heartbeat.repo_ambiguous` (info).
- `server/test/services/heartbeats.test.ts` (+17),
  `server/test/http/routes/heartbeats.test.ts` (+5).
- `.tasks/backlog/LAI-144-heartbeat-repo-is-a-remote-not-a-name.md` — new.

**Decisions**

*Option 2, disambiguate by branch, falling back to every match.* Attributing to
nobody would make presence empty for exactly the monorepo LAI-108 permitted.

*§9.2's regex, unanchored.* Anchoring drops `feature/lai-42-x`. A resolver
stricter than the convention it implements is a second convention; §9.2's task
resolution must reuse `branchProjectPrefix` rather than parse again.

*Nothing stored.* §9.3 already forbids a presence store, and one column cannot
hold a legitimately-many result.

*The 202 keeps its empty body.* AC1 says the POST resolves, and it does — but
serialising it is a §9.1 contract change riding along inside an ambiguity task.
That is the ride-along LAI-091 refused; refusing it there and taking it here
would be the rule bending for my own convenience. Logged instead.

*Case folding in JS, not SQLite's `lower()`.* ASCII-only vs Unicode-aware — a
comparison that disagrees with itself depending on which side ran it.

**Discovered**

*The plugin sends a URL where §4.3 wants `owner/name`.* `plugin/hooks/README.md`
documents "git remote"; none of the three real remote forms matches
`PawanSirsat/Laika` by any comparison. **On a correctly configured instance,
presence would be permanently empty.** Filed as LAI-144 — a different rule about
a different failure from the one these criteria name, and it matters more than
this task did.

*An exhaustive assertion earning its keep.* `writes §4.10's columns and nothing
else` went red the moment the view gained two fields. Extended, not loosened,
and it now also asserts the stored row's column list — so the row and the
resolution stay distinguishable, which is what D-005 actually cares about.

*Six mutations, all caught.* Case-sensitivity, no branch narrowing, ambiguity
picking the first silently, an anchored parse, an unmatched repo erroring, the
route going quiet.

**State**
In review. AC4 is CHIEF's; §9 text handed over on submission.

## 2026-09-01T16:45:00Z — LAI-118, append-only triggers survive a rebuild

**What changed**
- `server/src/db/migrate.ts` — `ensureActivityTriggers`, called by
  `runMigrations` after every migration pass.
- `server/src/db/migrations/README.md` — new; AC6, in my own area.
- `server/test/db/migrate.test.ts` — new, 9 tests.
- `server/test/tooling/structure.test.ts` — dropped `db/migrate.ts` from
  `NO_MIRROR_REQUIRED`.

**Decisions**

*The unconditional call is the mechanism, not `IF NOT EXISTS`.* A migration runs
once and cannot repair a database a later migration rebuilt.

*The post-condition checks the body, not the name — and this changed mid-task.*
My first version checked existence; mutating it away turned **nothing** red,
because `CREATE TRIGGER` either succeeds or throws, so the check was
unreachable. The reachable failure is a hand-pasted block that lost its `RAISE`:
present, correctly named, inert, and invisible to `IF NOT EXISTS` because
something holds the name.

*It throws rather than repairing.* A missing trigger is an accident of SQLite's
rebuild; a wrong one is a mistake in a migration, and rewriting it at boot hides
a file that will be applied again on the next clean install.

*AC6 needed no filing.* The criterion allows the migration folder, which is
`server/`.

**Discovered**

*My rebuild helper rebuilt nothing.* It renamed `` `activity` `` in the captured
DDL, but SQLite rewrites stored DDL in **double quotes** on
`ALTER TABLE … RENAME`, and every rebuild since 0000 has gone through one. The
replace matched nothing, and the test asserted on a table it had not touched. It
now matches either form and throws if the replacement did not land.

*The append-only assertions passed on an empty table.* A `BEFORE UPDATE` trigger
fires **per row**; with no rows the statement matches nothing and succeeds
whether or not the guarantee exists. Both of these are the harness lesson again:
the check ran, but not against what it named. Third and fourth instance today.

*Most of the file would pass if nothing ever called the function.* Only
`re-establishes the triggers on a boot with no pending migrations` fails when
the wiring is removed — and that is also the property that matters.

**State**
In review. Seven mutations, all caught, including the two that initially were
not. Next: LAI-144 (CHIEF resequenced it ahead of LAI-099 — it blocks M4).

## 2026-09-01T17:20:00Z — LAI-144, a git remote where §4.3 wants owner/name

**What changed**
- `server/src/services/heartbeats.ts` — `normaliseRepo`; `fold` now normalises
  then lowercases, and returns `null` rather than `''`.
- `server/test/services/heartbeats.test.ts` (+10).
- `.tasks/backlog/LAI-145-plugin-sends-owner-name.md` — new, `area: plugin`.

**Decisions**

*The server normalises, both sides.* CHIEF ruled on the side; both sides is
mine. §4.3 asks for `owner/name` and nothing enforces it, so a project row
holding a URL is as likely as a heartbeat carrying one, and normalising one side
only is a comparison that disagrees with itself depending on where the URL
landed — the same fault as folding case on one side.

*Nested paths kept whole.* GitLab subgroups are real; truncating to two segments
would merge two different repositories.

**Discovered**

*A scheme must be detected before the scp form.* `https://github.com/` has no
path, so a URL pattern requiring one falls through, and the scp pattern then
reads `https` as the host and returns **`github.com`** as the repository name.
Caught by the degrade test.

*A test that named the right property and could not fail for it.* My
"does not mistake a name containing git" test used `kvell/gitignore` and
`kvell/legit`. **Neither contains `.git`** — so the mutation that strips `.git`
anywhere passed it. The real case is `PawanSirsat/PawanSirsat.github.io`, which
a global replace turns into `PawanSirsathub.io`, and every GitHub account has
that repository. Reading the test, it looks correct; only the mutation showed
otherwise. This is the strongest argument yet for mutating rather than reviewing.

**State**
In review. AC2 is CHIEF's; text handed over. Six mutations, all caught once the
`.github.io` case existed. Next: LAI-099, once CHIEF holds SHELL at a boundary.

**Process note.** My submission script asserted its anchors but the `git mv` on
the next line was not chained to it, so the task file moved to review with no
ticks and no note when an anchor failed. Same class as the harness lesson:
the guard fired and the next step ran anyway. Re-applied by line number.

## 2026-09-01T18:00:00Z — LAI-427, a comment that outran its assertion

**What changed**
- `server/test/db/migrate.test.ts` — `re-establishes nothing and changes nothing`
  now asserts `PRAGMA schema_version` as well as the stored SQL.

**Decisions**

*Keep the `name, sql` comparison.* It is a different property — that a future
edit did not change the trigger bodies — and the real fix was that one comment
described two properties and only one assertion.

**Discovered**

*Verified the instrument before using it.* create 1→2, no-op stays 2,
drop-and-recreate 4. And `sqlite_master.rowid` after a drop-and-recreate is the
**same rowid** — SQLite reuses the freed slot. It is the obvious instrument and
it is wrong; CHIEF checked before filing, and I confirmed rather than taking it.

*Proved the blindness, not just the fix.* Removing only the new assertion under
the same mutation turns the test green again — so the byte-identical comparison
really was blind rather than merely redundant. Half of this task was provable
only in that direction and it would have been easy to assume.

*The defect was a comment, not a test.* It claimed byte-identical SQL
distinguished a recreate; it cannot, because both come from the same string
constant. I wrote it on LAI-118 while being careful about exactly this class of
thing. A guard that overstates itself is worse than none, because the next
person stops looking — D-037's sibling, aimed at me.

**State**
In review. Next: LAI-099, once CHIEF holds SHELL at a boundary. Nothing else in
flight.

## 2026-09-01T18:40:00Z — LAI-126, the two actual timestamps

**What changed**
- `server/src/services/tasks.ts` — `TaskView.started_at` / `completed_at`,
  mapped in `toView`; `changeStatus` stamps `started_at` on first entry to
  `in_progress`.
- `server/test/services/tasks.test.ts` (+5), including an exhaustive `TaskView`
  key assertion.
- `.tasks/backlog/LAI-121-*.md` — folded in, as these criteria direct.
- `.tasks/backlog/LAI-146-completed-at-survives-a-reopen.md` — new.

**Decisions**

*`started_at` on the first entry only.* A task sent back and picked up again did
not start twice; overwriting would silently shorten every cycle time (LAI-124).

*Added an exhaustive `TaskView` shape guard.* Beyond the criteria and flagged as
such. It will go red on LAI-099's rename, which is the point.

*Folded the client half into LAI-121 rather than filing a third task* against
one file — the criteria name LAI-121 explicitly as the place.

**Discovered**

*The missing field was the smaller half.* `started_at` was set only by
`claimTask`; `changeStatus` handled `done` and nothing else, so a lead moving
somebody else's task left it null while the task was genuinely under way. AC3
could not have passed — the criterion described behaviour that did not exist.

*Nothing pinned the `TaskView` shape.* Adding two fields to the most-consumed
contract in the product turned **nothing** red. `HeartbeatView` has such a guard
and caught the identical change on LAI-116 in a second. Same product, same week,
opposite outcomes — the difference was one assertion.

*The task file's claim about `task-lifecycle.ts` is wrong.* It holds
`canTransition` and `isReady` and no timestamp logic at all.

**State**
In review. Five mutations, all caught. Next: LAI-099 — CHIEF ruled the scope
(D-044: #1 response field, #2 request body, #4 MCP; out: #3 URL path, #5 activity
payload, #6 DB column) and SHELL has LAI-429 for the web half.

## 2026-09-01T19:15:00Z — LAI-099, and a red branch I did not catch

**The important thing first: I submitted LAI-126 red and did not know it.**

`pnpm --filter @laika/web test` was failing on `core` — LAI-213's drift check,
on the two fields LAI-126 added. I gated on `pnpm --filter @laika/server test`
because CORE owns `server/`. The drift check lives in `server/web/test/` but
**reads `server/src/services/tasks.ts`**, so a purely server-side field addition
turns a web test red and my gate could not see it.

Every task today was gated the same way; LAI-126 was the first to add a `*View`
field, which is why it was the first to show. I verified the thing I owned rather
than the thing my change affected. **A gate that cannot see the check my change
breaks is not a gate.** Now D-045: the gate is the repo-root `pnpm test`.

LAI-126's own criteria said to fold the client half into LAI-121 — which
schedules a fix and lands nothing. **It was a two-owner change and I treated it
as one-owner.**

**What changed (LAI-099)**
- `server/src/services/tasks.ts` — `TaskView.dependencies` → `blocked_by`; #5's
  two payload sites marked deliberate.
- `server/src/http/routes/tasks.ts` — body takes `blocked_by_task_id`; #3 marked.
- `server/src/mcp/write-tools.ts`, `read-tools.ts` — #4 both directions.
- `server/src/db/schema.ts` — #6 marked deliberate.
- Four test files.

**Decisions**

*Renamed #1, #2, #4; left #3, #5, #6 with one line each where the name is.*
D-044's scope, unchanged. #5's comment also says why LAI-045's read-time
translation is not a way round it, because that is the obvious next question.

*Did not exempt the drift check.* `clientOmits` is in `server/web/`, so the only
exemption available to me is a crossing — which is what D-042 withdrew, and what
D-045 step 1 now covers by letting the first half submit red with the failures
named.

**Discovered**

*"Each half is green alone" was false, and I measured it rather than asserting
it.* One rename produces failures in **both** directions: `TaskView.blocked_by`
is served and `Task` does not declare it, and `Task.dependencies` is declared and
`TaskView` does not send it. I told CHIEF I expected this and explicitly said I
had not verified it; SHELL measured the mirror image independently. Stating a
prediction as a prediction is what made it usable — a confident right guess would
have been the same mistake as the confident wrong one it replaced.

**State**
In review, red on two named `@laika/web` assertions and green everywhere else.
Turned green by LAI-429 (SHELL, in review) and LAI-121 (SHELL, next).

## 2026-09-01T20:10:00Z — LAI-113, seven verbs

**What changed**
- `server/src/db/enums.ts` — seven verbs, plus the no-backfill reasoning where
  the vocabulary is declared.
- `server/src/db/migrations/0012_wide_fenris.sql` — generated; **no trigger
  rescue block**.
- `server/src/services/sprints.ts` — `sprint.created|updated|deleted`, and
  `sprint.tasks_changed` replacing `task.updated` on sprint moves.
- `server/src/services/projects.ts` — `project.context_updated`.
- `server/src/services/unlisted.ts` — `unlisted.promoted`, `unlisted.dismissed`.
- `server/src/db/activity.ts` — `latestFieldEdit` reads **both** vocabularies.
- `server/test/db/activity.test.ts` (+4), and six service/tooling test files.
- `.tasks/backlog/LAI-147-web-half-of-the-activity-verbs.md` — new.

**Decisions**

*`sprint.tasks_changed` replaces `task.updated` on a sprint move.* CHIEF's test —
"could a reader answer *when did this happen* without inspecting a payload?" —
settles it: `task.updated` fails that test for a sprint move. `taskId` stays set,
so the row remains on the task's own timeline too. I had queried the seventh verb
before starting; the test CHIEF gave resolved it and I built it.

*The `entity`/`action` payload stays at every site.* Old rows have nothing else.

**Discovered**

*LAI-118 works, and this was its first real test.* The migration rebuilds
`activity` — `DROP TABLE` + `RENAME` — carries **zero** `CREATE TRIGGER`
statements, and both triggers are present after `runMigrations`. First migration
in this repo's history that did not need the block pasted in.

*What not backfilling costs, and who pays it.* `latestFieldEdit` filtered on
`project.updated`. Leaving it would have silently lost every context edit made
before the rename, and the symptom — "last edited by nobody" — looks nothing like
its cause. It now accepts both verbs permanently. **"Do not rewrite history" is
easy to agree with; "therefore every reader of that history must accept two
vocabularies for ever" is the part with the bug in it**, and it only surfaced
because a test read the old rows.

*LAI-113 is a two-owner change and nobody predicted it.* The client mirrors
`ACTIVITY_TYPES` in `stream-types.ts`, and the dashboard asserts it handles every
verb. **This is a drift axis LAI-213 does not cover** — LAI-213 binds `*View`
types; this is a closed vocabulary shared on both sides. So "will it land green?"
is not answered by "does a `*View` move?" but by "does anything closed on the
server have a copy on the client?". Filed as LAI-147.

*A blunt replace hit two sites.* I used a `>= 1` occurrence assert instead of
`== 1` and rewrote an unrelated promote assertion to `unlisted.dismissed`. The
suite caught it; the guard I normally rely on did not, because I weakened it.

**State**
In review, red on four named assertions: one CHIEF's §4.8, two LAI-147's, two
already-reported from LAI-099/LAI-126.

## 2026-09-01T21:15:00Z — LAI-222, the org endpoint and org role management

**What changed**
- `server/src/services/orgs.ts`, `server/src/http/routes/orgs.ts` — new;
  mounted in `server/src/app.ts`.
- `server/src/services/users.ts` — `setOrgRole`, `setUserActive`, the
  last-active-Owner invariant, `ORG_ROLES` re-export.
- `server/src/http/routes/users.ts` — `PATCH /:id`.
- `server/src/policy/actions.ts`, `can.ts` — `org.read`, a read action.
- `server/src/db/enums.ts` + migration `0013_perfect_the_fury.sql` —
  `user.deactivated`, `user.reactivated`.
- `server/test/services/orgs.test.ts` (9), `http/routes/orgs.test.ts` (5),
  and additions to the users service and route tests.
- `.tasks/backlog/LAI-148-avatar-color-is-dead-and-wrong.md` — new.

**Decisions**

*The last active Owner is one invariant, not four rules.* Four criteria describe
one trap; both write paths ask one question. Self-versus-other never enters into
it, so AC5 falls out rather than being a second path that can disagree.

*It counts active Owners, not Owner rows.* A deactivated Owner is not cover.

*An Admin may demote themselves.* Recoverable by any Owner; only unrecoverable
states are guarded.

**Discovered**

*My `member_list.read` recommendation was the wrong shape, not just the wrong
answer.* CHIEF ruled against it on §11.4.2, which I could not see from the code.
But my argument's form was *"nothing this returns is sensitive"* rather than
*"nothing this row grants is sensitive"*, and those differ the moment the
response grows — D-037's contingent-fact failure, in a permission matrix, where
the next field inherits the grant. That part I could have caught.

*Three existing guards caught real mistakes in one task.* The route lint rule
(I imported `db/enums.ts` into a route); the policy matrix completeness test
(`org.read` had no assertion); and `404s /users/:id`, which was correct only
while nothing was registered on that path. The third I updated rather than
deleted — the distinction it pins, 404 for an unregistered path versus 405 for an
unbuilt method, is the part worth keeping.

*`avatar_color` disagrees three ways.* §4.1 says derived from id; the server
derives from **email** and stores it; the client derives from **id** at render
and ignores the served value. The column is dead *and* contradicts the spec. The
client is right and not by preference — `avatarColor(id, theme)` is theme-aware
and one stored value cannot be legible in both, which §5.1 requires. Filed as
LAI-148 rather than decided, as the task asked.

**State**
In review. Six mutations, all caught. Red only on LAI-113's inherited seven —
read the failure to confirm none of the three verbs I added were in it.

## 2026-09-01T22:10:00Z — LAI-143, watch/mention transport

**What changed**
- `server/src/policy/actions.ts`, `can.ts` — `task.watch`, every project role,
  **not** in `READ_ACTIONS`.
- `server/src/services/watchers.ts` — regraded from `project.read`.
- `server/src/services/mentions.ts` — `canBeMentioned` extracted;
  `mentionableUsers` added, both calling it.
- `server/src/http/routes/tasks.ts`, `me.ts`, `projects.ts`; mounted in `app.ts`.
- Tests across `policy/`, `services/mentions`, `http/routes/tasks`.

**Decisions**

*`PUT`/`DELETE`, not `POST`.* Watching is idempotent state, not an event.

*`204`, not the task.* Nothing about the task changed.

*A second router on `/me`.* `GET /me` is mounted before the database is known to
exist — "who am I" is answerable without one — so giving `meRoutes` a required
`db` would move it behind that guard and change what a half-configured instance
answers. `/users` already carries two routers for the same reason.

**Discovered**

*`can.test.ts`'s Viewer rule was stated too strongly, and my change exposed it.*
`Viewer write attempts all fail` already had four exceptions (own tokens ×2,
joining a public project, own unlisted work, own heartbeat). The rule it means is
*"a Viewer writes nothing that is not about themselves"*. `task.watch` is the
fifth and the one that makes the role/scope distinction visible. Restated with
the reasoning at the site rather than appending a fifth name, because otherwise
the exception reads as arbitrary.

*Proved the exemption expires rather than asserting it.* Exempting `task.write`,
which §3.2 certainly grants, turns **two** guards red — the staleness test and
`gives every exemption a reason naming a task`.

*Proved the `READ_ACTIONS` absence is load-bearing.* Putting `task.watch` into it
turns the `read_only` pair red. Without that mutation the absence is a claim.

**Process note.** I asked CHIEF a sequencing question and started before the
answer arrived; our messages crossed. It cost nothing, but the rule I broke is
the one I had cited to them two hours earlier — a decision made in flight cannot
be un-made cheaply, and the round trip is cheapest before the first commit.

**State**
In review. Three mutations caught. Red only on LAI-113's inherited seven.
Next: LAI-207, with `presence_enabled = 0` producing a **disabled** state rather
than an empty one (CHIEF's carry-forward; LAI-432 depends on it).

## 2026-09-01T23:00:00Z — LAI-207, the org presence setting

**What changed**
- `server/src/db/schema.ts` — `orgs.presence_enabled`, migration `0014`.
- `server/src/services/orgs.ts` — on `OrgView` for **every** role; `updateOrg`.
- `server/src/http/routes/orgs.ts` — `PATCH /`.
- `server/src/services/setup.ts`, `http/routes/setup.ts` — accepts it.
- `server/test/tooling/schema-spec-drift.test.ts` — `COLUMNS_NOT_IN_SCHEMA`
  emptied by its own staleness guard.
- `.tasks/backlog/LAI-149-restore-the-presence-toggle.md` — new.

**Decisions**

*Read is every role, write is Admin+.* §11.4.2 shows a **disabled** state on
Capacity when it is 0, distinct from empty, and Capacity is not an admin screen.
The stronger argument if that ever changes: it is a claim about the people being
tracked, and they have the best reason to know it (D-005).

*Admin+, not Owner-only.* The task says Owner-only; §3.1's row is ✓ ✓ — —, and
D-011 makes the spec authoritative. Said at the site rather than followed
silently.

*Enforcement deliberately absent.* §4.2 puts it on `POST /heartbeats` (M4) and
the views (M5); `schema.ts` names both so it is not rediscovered as a gap.

**Discovered**

*An exemption retired itself.* `COLUMNS_NOT_IN_SCHEMA`'s only entry was
`orgs.presence_enabled`, its reason named LAI-207 as the task that would close
it, and the guard fired the moment the column landed — I did not go looking. The
cleanest end-to-end example of the exemption discipline in the repo.

*I left a test asserting `expect(true).toBe(true)`.* Written as a placeholder for
prose I wanted to keep and never replaced. Removed. It would have passed for ever
and meant nothing — the same class as the helper that ignored its argument, in
its purest form.

*A commit swept up an unrelated staged change.* My merge-resolution commit for
the LAI-147 add/add conflict also carried the LAI-207 claim, because an earlier
`git mv` had staged it and `git commit` during a merge commits the whole index.
The state is correct and §2's claim check is path-based so the lock held, but the
message says nothing about it. Same hazard CHIEF hit earlier today, from the
other side.

**State**
LAI-207 in review. Four mutations caught.

## 2026-09-01T23:45:00Z — LAI-433, whoami paired and §7.1 pinned

**What changed**
- `server/test/mcp/parity.test.ts` — `laika_whoami` paired with `GET /me`; the
  inline `name !== 'laika_whoami'` removed; two new tests pinning §7.1's names
  and §7.2's count against the served tools.

**Decisions**

*Paired, not exempted.* AC1 allowed either. A pair is **tested**; an exemption is
only excused — and for a read tool the property worth testing is that it stays a
read, which is the LAI-405 failure in this same file.

*Twinned with `GET /me` specifically*, not the generic read triple, because both
answer the same question. Both sides seed identically so the comparison is
between the two reads rather than between two setups.

**Discovered**

*I asserted a sentence I had not read.* My §7.2 check matched *"nine of them have
REST twins"* — which is how **the task file** describes it, not what §7.2 says
(*"cover the nine tools that have twins"*). The assertion could only ever have
failed. Same class as a criterion naming a location without checking it, with the
source being a task file instead of a spec section: **quote the artefact, do not
paraphrase from memory of a description of it.**

*§7.2 carries two numbers in one sentence.* "the nine tools that have twins" and
"a missing tenth pair" — both move with this task, and my test pins only the
first. Flagged to CHIEF rather than left for them to find at merge.

*A command applied half of itself, again.* An earlier edit's first replacement
missed (wrong import order) while its second landed, leaving `SPEC_DIR` declared
twice — caught by the transform error, not by me. Second time today that a
partially-applied edit produced a state neither intended nor obvious.

**State**
In review, red on two named assertions, both CHIEF's §7 half.

## 2026-09-02T00:35:00Z — LAI-430, branch → task

**What changed**
- `server/src/services/heartbeats.ts` — `parseBranchRef` (prefix + number,
  `branchProjectPrefix` now delegates), `resolveBranchTask`, wiring into
  `recordHeartbeat`, `presenceEnabled` gate.
- `server/test/services/heartbeats.test.ts` (+14).
- `.tasks/backlog/LAI-150-heartbeat-discard-when-presence-is-off.md` — new.

**Decisions**

*The project is decided before the number.* `LAI-42` and `WEB-42` are different
tasks; number-first gives the right answer whenever the number is unique and the
wrong one exactly when it is not.

*Presence off means no resolution, but the row still lands.* §4.2 also says
"accepts and discards"; doing that here would collapse two different rules —
"the row lands anyway" is right for a degraded branch and wrong for a disabled
org. Filed as LAI-150 with that distinction as a criterion.

**Discovered**

*Two of my own guards are untestable, and I said so rather than counting them.*
`projectIds.length !== 1` and the safe-integer bound both survive mutation: the
prefix check and the task lookup already cover those inputs. They stay — each
states the rule where it applies — but the comments now say defence in depth, not
checked property. Reporting two equivalent mutations is worth more than claiming
six caught.

*My own test conflated "degrades" with "malformed".* I listed `''` among branches
that resolve to null; an empty branch is a `422` (LAI-417). §9.2's rule is about
a branch that does not follow the convention, not one that is absent — and
conflating them would turn a client bug into a silent no-op.

*I referenced LAI-436 in a code comment — CHIEF's range, not mine.* Caught before
committing; the task is LAI-150. D-017 assigns 400-499 to CHIEF, and a task id
invented in a comment is a reference to something that will never exist.

**State**
In review, **fully green** — 1528 server, 562 web, no red to quote. Next:
LAI-431 (the scheduler), then LAI-432.

## 2026-09-02T01:40:00Z — LAI-431, the in-process scheduler

**What changed**
- `server/src/jobs/jobs.ts`, `backup.ts`, `scheduler.ts` — new.
- `server/src/db/enums.ts` + migration `0015` — four cron verbs.
- `server/src/shutdown.ts`, `src/index.ts` — scheduler started at boot, stopped
  with the streams.
- `server/test/jobs/` — 32 tests across three files.
- `server/test/tooling/activity-payload-names.test.ts` — the sweep drives the
  four jobs.
- `.tasks/backlog/LAI-151-web-half-of-the-cron-verbs.md` — new.

**Decisions**

*Four verbs, not six.* §4.8's D-022 note enumerates the cron's writers; the
snapshot and vacuum are not among them, and a weekly "nothing happened" line is
how the one that says 40000 goes unnoticed.

*`heartbeat.pruned` is per run, not per heartbeat.*

*`stale_flagged_at` is set once, never refreshed.* "Since when" is the question,
and rewriting it nightly would make a month-old staleness look like today's.
Idempotence falls out of that rather than being added.

*SQLite's online backup, not a file copy.* WAL means the `.db` alone is not a
consistent snapshot.

**Discovered**

*§4.8 contradicted itself and had for a while.* Its nullability rule is
justified by cron rows its own type list made impossible to write. Eighth
instance of the missing-verb pattern and the first where the spec already
asserted the thing it forbade.

*My "failing job" did not fail.* `mkdirSync(recursive: true)` creates deeply
nested paths happily, so the isolation test asserted on a failure that never
happened. Real `ENOTDIR` now — a directory whose parent is a file.

*My "never deletes a foreign file" test could not fail.* `operator-notes.txt` and
`laika.sqlite` sort **after** `laika-…`, so removing the prefix filter deleted
only real backups and the test stayed green. Renamed to sort first. Same class as
LAI-144's `.github.io` — a test that names the right property and is built so the
property cannot be violated. **Second time this exact shape has caught me**, and
both times only the mutation found it.

**State**
In review. Server 1560/1560; web red on the client vocabulary mirror (LAI-151,
third occurrence). Nine mutations, all caught.

## 2026-09-02T02:30:00Z — LAI-219, per-account sign-in throttling

**What changed**
- `server/src/auth/sign-in-throttle.ts` — new.
- `server/src/app.ts` — wired at the `/auth/*` mount, sign-in only; injectable.
- `server/test/auth/sign-in-throttle.test.ts` (12),
  `server/test/http/sign-in-throttle.test.ts` (9).
- `server/test/tooling/schema-spec-drift.test.ts` — LAI-431's four exemptions
  dropped for CHIEF's merge.

**Decisions**

*Delay, not lockout, capped at 15 minutes.* A lockout hands an attacker a
permanent outage for five requests, and D-004 makes the account list small and
known. A capped delay bounds the owner's worst case and clears itself.

*Keyed on the submitted address*, so it is not an account-existence oracle.
Asserted on status **and** body.

*Only a rejected credential counts.*

**Discovered**

*I opened a cheaper denial of service and two other sessions' tests found it.*
My first version counted every non-2xx as a failed attempt — including the `403`
the origin check returns **before any password is looked at**. That would let an
attacker throttle any account from a foreign origin without ever submitting a
guess: strictly worse than the trade the design accepts. `origin.test.ts` went
red and the breakage was correct. I would have shipped it.

*Two policy bugs my own tests found before any mutation.* The delay was armed one
failure late; and `WINDOW_MS` equalled `MAX_DELAY_MS`, so an attacker at the cap
got a free reset every time they waited it out — the delay stopped growing and
the count never persisted.

*The exemption round trip is mine, not the rule's.* CHIEF asked whether an
in-flight exemption earns its keep when the branch is red anyway. It does — but
the round trip it cost was avoidable: I merged master several times today without
re-running the staleness guards. Checking them after every merge is the fix, and
it is a habit rather than a rule change.

**State**
LAI-219 in review. Nine mutations, all caught. Branch red on exactly the four
LAI-431 verbs, which is CHIEF's half and the reason the entries came out.

## 2026-09-02T03:20:00Z — LAI-432, presence and capacity

**What changed**
- `server/src/services/presence.ts`, `src/http/routes/presence.ts` — new;
  mounted in `app.ts`.
- `server/src/policy/actions.ts`, `can.ts` — `presence.read`, `capacity.read`,
  both in `READ_ACTIONS`.
- `server/test/services/presence.test.ts` (16),
  `test/http/routes/presence.test.ts` (6), matrix rows, in-flight exemptions.

**Decisions**

*`enabled` is a field, not an inference.* LAI-150's corollary: once a disabled
org stores no rows, an empty list is the only thing left.

*A heartbeat attributing to no project is visible to everyone.* It names no
project; it says only that somebody is working, which the member list gives away.
The one place the filter deliberately does not bite, so it is said out loud.

*`unlisted` gated on `audit_log.export`* — what `listUnlisted` already costs, so
capacity cannot become a way around §3.1's row. Absent, not empty.

*Two new actions rather than a borrow.* LAI-222's lesson.

**Discovered**

*My agent test only asserted the human case.* `is_agent: false` for a cookie
heartbeat passes against an implementation that never reports an agent at all —
which is exactly the requirement §11.4.2 has. Both halves now. **Third instance
of the same shape today** (LAI-144's `.github.io`, LAI-431's backup filenames).
Every one was a test that named the right property and could not fail for it, and
every one was found by mutating rather than by reading.

*Typecheck caught what vitest did not, again.* `name = orgRole` as a default made
TypeScript infer the parameter as `OrgRole`, so every call passing a real name
was an error the test run ignored. The LAI-136 composite earning itself again.

*Ran the staleness guards straight after merging master*, per the habit I gave
CHIEF — clean this time, and it cost one command.

**State**
In review, fully green — 1605 server, 585 web. Seven mutations, all caught.
M5's server side is complete: LAI-430, LAI-431, LAI-432.

## 2026-09-02T03:55:00Z — LAI-150, a disabled org takes no heartbeat

**What changed**
- `server/src/services/heartbeats.ts` — returns before the insert when
  `presence_enabled = 0`.
- `server/src/services/presence.ts` — comment corrected to present tense.
- `server/test/services/heartbeats.test.ts` (+5),
  `test/http/routes/heartbeats.test.ts` (+3).

**Decisions**

*Discarding means writing nothing.* A stored row still carries user, token, repo
and branch — the same metadata minus the task link — so keeping it left the
switch promising a privacy property it did not deliver.

*Still `202`, not `403`.* A plugin must not start reporting errors because an org
turned a feature off. Asserted in both states so the client contract does not
depend on the setting.

*Not a bypass.* Validation and the credential rule are unchanged when disabled.
**Mutating the check to sit above validation turns seven tests red** — and that
is the natural place to put it, which is why the mutation was worth running.

**Discovered**

*Degraded and disabled stayed separate, and I checked rather than assumed.*
LAI-430's tests pass unchanged because none of them asserted a row lands while
presence is off — only that one lands when the *branch* fails to resolve. The two
rules are now tested side by side in one file, which is where a later reader
would otherwise collapse them.

*A comment I wrote four hours ago had already gone stale.* `presence.ts` said
"once LAI-150 stops storing rows"; LAI-150 was me, two tasks later. Corrected,
and extended with the half I had missed: inferring from an empty table is wrong
in **both** directions.

**State**
In review, fully green — 1613 server, 585 web. Three mutations, all caught.

## 2026-09-02T04:35:00Z — LAI-438, presence names a repo only to someone in its project

**What changed**
- `server/src/services/presence.ts` — `repo`, `branch`, `project_ids` and
  `matched_task_id` withheld together unless the reader can see an attributing
  project; the entry itself always shown.
- `server/test/services/presence.test.ts` (+5, 3 rewritten).

**Decisions**

*The person is shown; the location is not.* Presence answers "who is working
right now", which is org-level. Dropping the entry would make the headcount
depend on who is asking.

*"No project" and "no project you can read" are one code path*, so they cannot
drift apart.

*Absent, not empty strings.*

**Discovered**

*A mutation survived because no fixture had a resolved heartbeat.*
`matched_task_id` leaked while `repo` did not, and every test passed — the field
was always `null` in every fixture, so withholding it was untested in the only
case where it matters. **Fourth instance today of a test that cannot fail for
the property it names, and the first where the gap was fixture *state* rather
than fixture data.** The other three were a filename, a boolean and a sort order.

*My LAI-432 reasoning was checkable and I did not check it.* I argued an
unattributed heartbeat was safe because "it names no project, so there is nothing
to leak" — and returned `repo` three lines below. D-046 is why it matters and I
could not have known that; the claim was false against the return statement
anyway. **The check on "is this safe to return" is the shape of the response,
not the shape of the argument.**

*Two decisions taken today changed what a third meant.* D-046 (user-level
settings) changed what §9.3's "presence shows the repo" implies, exactly as
LAI-126 expired D-040 this morning. Both times the newer decision was in a
different area from the thing it changed.

**State**
In review, fully green — 1618 server, 585 web. Five mutations, all caught after
the task-id gap was covered.

## 2026-09-02T05:15:00Z — LAI-435, dates recovered from the audit trail

**What changed**
- `server/src/db/backfill.ts` — new; called from `runMigrations`.
- `server/test/db/backfill.test.ts` (13), plus a wiring test in
  `test/db/migrate.test.ts`.

**Decisions**

*A boot step, not a `.sql` migration.* The criteria ask both for a migration and
for reading `activity` through its own module; SQL cannot call TypeScript. Beside
`ensureActivityTriggers`, idempotent by only-ever-filling-a-null.

*`task.updated` rows are not read, and the criterion's premise did not hold.* It
says `{ field: 'status' }` is "what the seeded demo has, so a real shape" —
**nothing in this repo writes it**; `updateTask` writes `{ changed: [...] }` and
has no status field. Such a row also does not say what the status *became*, so
reading it would be the invention AC3 forbids. Said in the module rather than
left as an omission.

*`updated_at` untouched.* This recovers what happened; it is not a change
somebody made, and moving it would put every backfilled task at the top of an
`updated_since` catch-up.

**Discovered**

*Two mutations survived the first pass, and both were guards with no teeth.*
Reading every activity type changed nothing observable — no other verb carries
`to: 'in_progress'` — so the type filter was decoration until a test made the
**verb** decide rather than the payload. And removing the call from
`runMigrations` was invisible because every test called the function directly:
the exact gap LAI-118's wiring test exists for, and **I did not write its
counterpart until the mutation asked for it**, on the file where I had learned
the lesson three tasks earlier.

**State**
In review, fully green — 1632 server, 585 web. Seven mutations, all caught; five
before those two were covered.

## 2026-09-02T06:00:00Z — LAI-142, shutdown 4022ms → 57ms

**What changed**
- `server/src/shutdown.ts` — `closeIdleConnections()` on a 50ms interval until
  `close()` reports done; `REAP_INTERVAL_MS` exported.
- `server/test/shutdown.test.ts` (+3), `test/tooling/build.test.ts` threshold
  7s → 1.5s with all four measurements in the comment.

**Measured**

| | before | after |
| --- | --- | --- |
| no stream | 9ms | 10ms |
| one SSE stream | **4022ms** | **57–68ms** |
| stream, `onStopping` severed | 10013ms | 10010ms, `shutdown.forced` |

**Discovered**

*The task's hypothesis was wrong and I proved it rather than adopting it.*
`keepAliveTimeout` 1000 → 2027ms, but 8000 → 4012ms, so it participates and is
not the cap. A `getConnections()` probe every 250ms showed **`open: 1` for the
entire four seconds** — the connection was never reaped, it was outlived.
`closeIdleConnections()` ran once while the response was still flushing, so the
connection was not yet idle, and nothing looked again.

*A real ordering bug my own unit test found.* The reaper was created **after**
`server.close()`, whose callback clears it — and a `close()` callback can fire
**synchronously**, which every test double does. `reaper` was cleared before it
was initialised. The end-to-end measurement passed throughout, because a real
server with an open connection closes asynchronously: **the fast path hid it and
the fake exposed it**, which is the reverse of the usual complaint about doubles.

*The floor is one reap interval*, and saying so is AC2's requirement — 57ms
against 9ms, and the difference is `REAP_INTERVAL_MS`.

**State**
In review, fully green — 1635 server, 585 web.

## 2026-09-02T06:40:00Z — LAI-140, one org lookup

**What changed**
- `server/src/db/orgs.ts` — `requireOrg` (id + name); `requireOrgId` delegates;
  the no-org case is the setup gate's `conflict`.
- `server/src/services/invites.ts`, `tokens.ts`, `activity.ts` — private copies
  removed.
- `server/test/db/orgs.test.ts` (+3, one changed deliberately).
- `.tasks/backlog/LAI-152-presence-enabled-read-twice.md` — new.

**Decisions**

*The gate's answer, not a 404 and not a 500.* Reaching the lookup without an org
means the setup gate was bypassed; a 404 says the thing is missing and a 500 says
the server broke, and neither is true.

*No `setup_path` in the details.* `db/` does not know about routes.

*Converged the fourth copy (`activity.ts`) although the criteria named two.*
Leaving one of four would be the same drift under a different file name, and the
task's Goal is "one declaration of one fact". Three lines, strictly reducing.

**Discovered**

*There were four copies, not three.* `activity.ts:orgIdFor` — found by `grep`
while doing the work, not by reading the task.

*`presenceEnabled` is byte-identical in two services and I wrote both today*, an
hour apart, in LAI-430 and LAI-432. Filed as LAI-152 rather than folded in — it
is a different fact about the same row. **I did not notice while writing either
copy; it took converging something else to see it.** That is the argument for
doing convergence tasks at all: the duplication is invisible from inside either
end of it.

**State**
In review, fully green — 1638 server, 585 web. Four mutations, all caught.

## 2026-09-02T07:05:00Z — LAI-428, a matched form decides

**What changed**
- `server/src/services/heartbeats.ts` — `find` the matching form, then read its
  capture, instead of `reduce` with `?.[1] ?? null`.
- `server/test/services/heartbeats.test.ts` (+1).

**Discovered**

*My docblock asserted the ordering and the loop discarded it.* `reduce` with
`?? null` reads "matched but captured nothing" as "did not match", so
`https://github.com` fell through to the scp form and came back as
`github.com` — while the comment three lines up explained why that could not
happen.

*The one tested input was the one that could not expose it.*
`https://github.com/` **with** the slash captures an empty string rather than
`undefined`, so `?? null` never fired. Fourth instance in three days of a test
built from the single example where the property cannot break.

*I could not have found this by reading, because I had read it.* The docblock
asserting the ordering is mine and it was true — and irrelevant, because the code
below it did not use the ordering it described.

**State**
In review, fully green — 1639 server, 585 web. Reversing `REMOTE_FORMS` turns
nine tests red; restoring the old `reduce` turns the new one red.

## 2026-09-02T07:55:00Z — LAI-148 and LAI-152

**LAI-152** — `presenceEnabled` converged into `db/orgs.ts`. Non-throwing, unlike
`requireOrgId`, because "no org" *has* a correct answer here: nothing set up
means nothing switched off, and §4.2's default is on. That reasoning lived in a
`?? 1` in two files and in prose in neither. The no-org case is now tested — the
one behaviour that would have diverged silently.

*Two mutations did not apply* (`?? 1` is not unique in that file) and printed
`ANCHOR FAILED` while the suite came back green. Re-anchored and re-run rather
than read as coverage. Fifth no-op mutation this week.

**LAI-148** — `avatar_color` removed from `UserView`, `users`,
`additionalFields`, and `auth/avatar.ts` deleted.

*The argument was not "three sources disagree, pick one".* The server **could
not** have derived from the id even if §4.1 were followed — the signup hook's own
comment says better-auth has not assigned an id when it runs, which is why it
used the email. With the client's version being the only theme-aware one (§5.1),
no version of "keep the column" worked.

*`ALTER TABLE DROP COLUMN`, not a rebuild*, so the `activity` triggers were never
at risk — verified on a fresh migrate anyway.

*A stale `NO_MIRROR_REQUIRED` entry was caught by its own staleness guard*, not
by me. Second time today a list I maintain found something first.

**State**
Both in review. LAI-148 is red on two named assertions — §4.1 (CHIEF's) and
LAI-213's drift (LAI-153, filed) — and the web one is the criterion rather than a
nuisance.

## 2026-09-02T08:30:00Z — LAI-442, deactivation stops at authentication

**What changed**
- `server/src/auth/resolve-actor.ts` — the cookie path refuses an inactive
  account, as the token path already did.
- `server/src/http/middleware/auth.ts` — rethrows `ApiError` instead of
  swallowing it into an anonymous request.
- `server/src/app.ts` — sign-in refused **after** better-auth verifies the
  password.
- `server/test/auth/deactivated.test.ts` — new, 9 tests.
- `server/test/tooling/schema-spec-drift.test.ts` — LAI-148's exemption dropped
  after merging CHIEF's §4.1 half.

**Decisions**

*Refuse sign-in after the credential is verified, not before.* Before would
answer 403 to anyone who typed the address — an account-existence oracle, and
LAI-219 spent a task keeping that property.

*403 for a cookie, 401 for a token.* What converges is the **place**, not the
status: a refused token is a bad credential; a valid cookie for a deactivated
person is a good credential whose holder may do nothing.

*Reactivation restores an unexpired session immediately.* `loadActor` reads the
row every request, so deactivation is immediate and reactivation is immediate by
the same mechanism. Asymmetry would need session invalidation, which nothing
does.

**Discovered**

*The fix would have reintroduced the bug one layer up.* `authMiddleware`
swallows non-`TokenAuthError` exceptions into `actor: null`, so throwing from the
resolver would have answered "you are not signed in" instead of "your account is
switched off" — the same wrong-shape defect this task removes. Found by reading
the middleware before writing the throw.

*Ran the staleness guards straight after merging master* — LAI-148's column
exemption had expired against CHIEF's §4.1 half. First time the habit fired
against their half rather than mine.

**State**
In review. 1651 server green; web red only on LAI-153 (SHELL's). Five mutations,
four caught, the fifth unreachable and reported as such.

## 2026-09-02T08:55:00Z — LAI-146, completed_at across a reopen

**What changed**
- `server/src/services/tasks.ts` — the decision written at the site; behaviour
  unchanged.
- `server/test/services/tasks.test.ts` (+4).

**Decisions**

*The latest completion, not cleared on a reopen.* Option 2 from my own filing.
`started_at` keeps its first value and `completed_at` its latest — asymmetric,
and asserted on one journey because testing either alone leaves the other looking
arbitrary.

**Discovered**

*The behaviour was already right and entirely undecided.* `changeStatus` has
always written `completedAt: now` on each arrival at `done`. Nothing said so,
nothing tested it, and LAI-435's backfill had to step around it. **An undecided
decision that happens to be implemented correctly is still undecided** — the next
person has nothing telling them which of the three answers that line is, and two
of them are one character away.

**State**
In review. 1655 server green; web red only on LAI-153 (SHELL's). Three mutations,
all caught. §6.4's text handed to CHIEF.

## 2026-09-02T09:20:00Z — LAI-101, format:fix reaches committed work

**What changed**
- `package.json` — `format:fix`'s window is the branch's merge-base with
  `master`, with `|| echo HEAD` as the fallback. Root grant per the task.
- `server/test/tooling/format-fix.test.ts` (+5).

**Measured**

Old expression on this branch: **0** files. New: **7**. That is the gap in one
number, and it bit me twice today — `drizzle-kit` emits unformatted JSON, and by
the time repo-wide `pnpm format` saw it the files were committed and `format:fix`
had nothing to do.

**Discovered**

*The fallback was load-bearing and invisible.* `|| echo HEAD` reads as
belt-and-braces. **Every existing test in that file runs in a repo with no
`master`** — `git init` plus one commit — so it is the only reason LAI-026's
suite kept passing after this change. Removing it turns two red, and there is now
a test asserting it directly rather than the suite depending on it silently.

*AC4 was the one worth confirming rather than reasoning.* On `master` the
merge-base **is** `HEAD`, so the diff is empty. That is the case that would
rewrite the whole repository if it were wrong.

**State**
In review. Three mutations, all caught: back to `HEAD`, no fallback, whole repo.

## 2026-09-02T10:00:00Z — LAI-214, refuse API requests during the drain

**What changed**
- `server/src/http/middleware/stopping.ts` — new; mounted early in `app.ts`.
- `server/src/errors.ts`, `http/error-handler.ts` — `unavailable: 503`.
- `server/src/shutdown.ts`, `src/index.ts` — `markStopping`, called first.
- `server/test/http/middleware/stopping.test.ts` (6), `test/shutdown.test.ts` (+2).

**Measured before building**

LAI-142 had already closed the reported path: a pooled keep-alive connection is
refused at every delay from 0ms up, and zero `ready` frames are served after
`shutdown.start`. I built the flag anyway because reaping cannot close the case
where a connection is **busy** when shutdown starts — never idle, never reaped,
briefly reusable when its request ends. Small, timing dependent, and exactly the
shape that shows up once on somebody's machine and never in a test.

**Decisions**

*Every API path, not `/events`.* The reused-connection route serves any of them.

*`/health` exempt.* A supervisor needs an answer; "I am draining" is the useful
one. Pinned by a test so the exemption is not incidental.

*`markStopping` before `closeAll()`.* Marking afterwards leaves a window exactly
as wide as the closing takes. The test asserts the **order**, not the effect.

**State**
In review, red on §6.3's code table (CHIEF's half) — `unavailable` is an
eleventh code. Five mutations, all caught. Shutdown still 61ms.

## 2026-09-02T10:30:00Z — LAI-124, throughput and cycle time

**What changed**
- `server/src/services/metrics.ts` — new.
- `server/src/http/routes/activity.ts` — `GET /:slug/metrics?since=`.
- `server/test/services/metrics.test.ts` (12), route tests (+5).

**Decisions**

*Filter on `status = 'done'`, not `completed_at IS NOT NULL`.* The natural query
is the wrong one: LAI-146 makes `completed_at` survive a reopen, so a task done,
reopened and in progress carries a timestamp for a cycle it is in the middle of.

*Cancelled excluded by construction*, so no second clause can disagree with
LAI-083 and LAI-085.

*A completed task with no start is counted, not measured.* Real throughput, no
cycle. Reported as `unmeasured` rather than dropped — a board where work skips
`in_progress` is a fact about the board.

*`cycle_time` is `null`, not zeroed, when nothing completed.* `null` says no
data; zeros say measured-and-nothing.

*Nearest-rank percentiles.* A p90 no task actually took invites somebody to go
looking for it.

**Discovered**

*CHIEF's note removed the decision, not just the work.* Reconstructing a first
transition from `task.status_changed` means deciding what a task sent back and
picked up again did — `started_at` already answers it, in the place that stamps
it. Three tasks (LAI-126, LAI-146, LAI-435) turned this from an event-replay
problem into two column reads.

**State**
In review. Six mutations, all caught. Red only on §6.3's code table, which is
LAI-214's `unavailable`.

---

## 2026-09-01T12:05:00Z — LAI-135 — `activity`'s SET NULL cascades

**Changed**
- `server/src/db/schema.ts` — `activity.project_id`, `task_id`, `actor_id`:
  `set null` → `restrict`, with the reasoning in a docblock above them.
- `server/src/db/migrations/0017_chilly_mephistopheles.sql` (+ snapshot, journal)
- `server/test/db/activity.test.ts` — seven tests, new describe block.
- Filed `.tasks/backlog/LAI-154`, `.tasks/backlog/LAI-155`.

**Decided**
The cascades were the wrong half, not the trigger and not the deletes. A SET NULL
is an UPDATE; §4.8 forbids UPDATE; so the schema was asking the table to permit
the exact thing it exists to prevent. A row in an append-only log losing its
subject *is* an edit. `restrict` puts the refusal where the constraint lives and
names the row being deleted instead of the audit log.

It also matches what this schema already does — `tasks.created_by` and two others
were `restrict` on a user reference before I touched anything. I only noticed
because a `grep -c` during an unrelated check returned 3 when I expected 0, which
was worth more than the check I was running.

`org_id` stays `cascade` and became LAI-154. It is a cascade *DELETE*, a
different question, and deleting an org's own log with the org is defensible. I
had written a comment claiming it is blocked by the DELETE trigger, then went and
measured it rather than leave a contingent claim sitting under an assertion that
does not prove it — `activity is append-only: DELETE is not permitted`. It is.

`heartbeats`, `unlisted_work`, `meeting_reviews` keep `set null`: the Notes asked,
and the answer is same shape, different consequence — none is append-only.

**Discovered**

*Two things the gate found that the branch would have hidden.*

`core` was behind `master` and red on §6.3, because CHIEF landed the `unavailable`
row while I was working. I had it queued to report as a blocking red before I ran
`git merge master`. That is the §4.4 habit — re-run the staleness guards after
every merge — catching me rather than me applying it, which is the second time
this week it has been the merge and not my judgement that got it right.

*And the one worth stopping for.* `@laika/server` **exits 1 with 1692/1692
passing** — two `ActivityFeed` poll timers firing after their database closed.
I reverted my schema and migration and reproduced it, so it is not mine; filed as
LAI-155 at p2. A suite that prints all-green and exits non-zero is worse than a
red one, because the fix for a red suite is obvious and the fix for this is to
stop reading the exit code.

It does not reproduce on any single file. I bisected every file in `test/http/`
one at a time and got zero every time, then ran the four files that mention
`ActivityFeed` together and got zero. The poll interval is 250 ms and a lone file
tears down inside that, so it only fires under full-suite load. Anyone who tries
to reproduce it the obvious way will conclude it is not there — which is in the
task file, because it is the part that costs an hour.

*Hygiene.* LAI-154 rode into the code commit because it was already staged. Same
staged-index trap as the LAI-207 claim earlier this week. Nothing wrong landed —
both files are mine and the commit message already names LAI-154 — but "I staged
it a while ago" is not something I am noticing at commit time yet.

**State**
In review. Six of seven new tests go red under the reverted migration, and they
fail with the exact error LAI-135 reports. The seventh pins compatibility and
correctly stays green under both; one assertion inside an eighth is
defence-in-depth and unreachable, and is counted as such rather than as coverage.
Repo-root gate: server 1692/1692, web 585/585, cli 19/19, lint and format clean —
with LAI-155's exit code the one thing still wrong, and not mine.

---

## 2026-09-01T12:55:00Z — LAI-231 — the poll timer that outlived its database

**Changed**
- `server/test/helpers/auth.ts` — the harness owns an `ActivityFeed`, passes it
  to `createApp`, and closes it before the database.
- `server/test/helpers/auth-harness.test.ts` — new, two tests.
- `server/test/shutdown.test.ts` — the ordering, asserted as an ordering.

**Decided**
The harness, not `poll()`. `createApp` falls back to `new ActivityFeed({ db })`
and hands the caller no way to stop it; `index.ts` never takes that path because
it owns the feed and gives it to shutdown as well. So the harness had diverged
from the production lifecycle, and the fix is to stop diverging rather than to
build a test-only mechanism.

A `catch` inside `poll()` would have turned this green and was the worse answer:
a real fault in `readActivityAfter` would then stop every subscriber receiving
activity with every assertion still passing — the same defect one layer down.
The risk it would insure against is shutdown ordering breaking, and the honest
answer to that is AC4's assertion, which is now there.

**Discovered**

*The undefended half was not the one the bug was in.* `closeAll()` disarms the
timer and shutdown calls it before `sqlite.close()` were **both** tested, in
separate files, and nothing tested the join — the closest test injects a timer
that never fires, so it cannot see it. The new test uses a real interval and asks
`isPolling()` from inside `sqlite.close()`, because after `shutdown()` returns,
disarmed and disarmed-too-late look the same. Mutated against both files.

*Three wrong instruments in one task, all mine, all the same shape.*
The bisect glob that skipped `test/http/middleware/` while I reported "does not
reproduce on any single file" — I had the right directory and then searched it
with a pattern that could not reach the answer. `echo $?` after a pipe, reporting
grep's status as vitest's. And `grep -c` counting **lines** for the "2 errors vs
1" discrepancy, when vitest prints each unhandled error twice — there was one
source all along.

Each time the command answered a question next to the one I was asking, and each
time the output looked like an answer. The bisect one had already been copied
into a p1 task as the finding that made it actionable, which is the cost: a wrong
measurement in a task file is load-bearing for whoever reads it next. I retracted
it within the hour, but not before it was propagated.

The fix is the same in all three: print what the instrument covered, not only
what it found. `27` — the number of files my glob matched — was available at the
time and would have ended it.

**State**
In review. Root gate **EXIT=0**, zero unhandled errors: server 1695/1695, web
585/585, cli 19/19; lint and format EXIT=0. Measured by exit code with the output
redirected, not by pass counts through a pipe.

---

## 2026-09-01T13:40:00Z — LAI-437 — a broken database is not a bad credential

**Changed**
- `server/src/http/middleware/auth.ts` — rethrow instead of swallow; test the
  specific error type first; docblock corrected.
- `server/test/http/middleware/auth.test.ts` — six tests.
- Filed `.tasks/backlog/LAI-156`.

**Decided**
Rethrow rather than wrap in `ApiError('internal')`. A wrapper discards the stack
and the `request_id` (§13.2), which are the two things the operator needs; the
error handler's unhandled path already produces both. Classification is entirely
`instanceof` — the AC asked for no string matching and there is none.

The Notes' "can the read path skip the write" question turned out to be already
implemented — the throttle skips inside 60s — so a read-only database serves
reads for a minute after each token's last use and then stops. That is worse than
either alternative because a total outage looks intermittent. Fixing it means
deciding whether a failed *bookkeeping* write should fail the request, which is
the same question LAI-231 answered the other way for `poll()` this morning, so it
went to LAI-156 with both arguments rather than riding along.

**Discovered**

*The comment was defending a path that does not exist.* The swallow was justified
by "a malformed or expired cookie is an anonymous request, not a 500". better-auth
**returns null** for a garbage cookie, a nonsense header and an invalid token — it
never throws, so it never reaches that catch. The comment named a real-sounding
case nobody checked could occur, and that is why the swallow survived review.
Third instance today of a comment claiming more than the code does.

*A mutation that passed found a bug I wrote.* Mutating "a refused token answers
internal too" changed nothing — because the branch was **unreachable**.
`TokenAuthError extends ApiError`, and **my LAI-442 change put an `ApiError`
branch above the token branch**, so since then every rejected token has logged
`auth.session_refused` with no `reason`: unknown, revoked, expired and
inactive_user collapsed into one line, which is the only thing that log exists to
distinguish. Proved with a probe before claiming it, then fixed and pinned on the
reason rather than on the presence of a line.

Nothing caught it because the **status was right either way**, and a 401 with the
right message is what a reviewer checks. Twice today the interesting bug has been
behind a correct-looking status code — and both times the thing that found it was
a mutation refusing to go red, not a test going red.

*One of four mutations printed ANCHOR FAILED and did not run.* Its `EXIT=0` would
have read as coverage. The habit of printing whether the edit landed is the only
reason that was visible, and it is the same failure as this morning's glob.

**State**
In review. Root gate EXIT=0, zero unhandled errors: server 1701/1701, web
585/585, cli 19/19; lint EXIT=0 — it caught an untyped `JSON.parse` in my own new
test — format EXIT=0.

---

## 2026-09-01T14:20:00Z — LAI-208 — the third card marker

**Changed**
- `server/src/services/tasks.ts` — `stale_flagged_at` on `TaskView`.
- `server/src/jobs/jobs.ts` — the job clears the flag as well as setting it.
- `server/test/http/routes/tasks.test.ts`, `test/services/tasks.test.ts`,
  `test/jobs/jobs.test.ts`.
- Filed `.tasks/backlog/LAI-157` (area: web).

**Decided**
The stored timestamp, not a boolean. `ready` is computed server-side because
§4.5's rule must have one definition; staleness is *written down* by a job, so
sending `stale: true` would discard the only thing the row holds, and §11.4.1's
marker wants to say how stale.

And — CHIEF's question, which the field is not correct without — **the job owns
the field in both directions**. Nothing un-wrote it before; harmless while the
column reached no client, and a permanent mark on a briefly-slow task the moment
it does. Clearing in the routes on a status change was the alternative and is
worse: the rule for stale is three conditions, and a handler clearing on
"somebody touched it" is a second, simpler rule that can disagree. The cost is a
day's lag for a rescued task, which is smaller than two definitions of staleness,
and LAI-157 is explicitly told not to paper over it client-side.

Submitted **red** under §4.4/D-045 — one assertion, quoted in the task file.
`clientOmits` is in `server/web/`, so there is no exemption to take that is not a
crossing.

**Discovered**

*Two guards caught me, both mine to have avoided.* The field-list test in
`test/services/tasks.test.ts` pins `TaskView`'s keys exactly so a new field is a
deliberate act — it did that. And my own `expect(result.changed).toBe(1)` guard
caught a broken setup: the first round-trip test used a `PATCH` to set
`in_progress`, which that route ignores — status goes through
`POST /tasks/:id/status`, one §5 step at a time — so the task stayed in `backlog`,
the job flagged nothing, and the assertions would have compared `null` to `null`
and passed.

**That is the LAI-407 defect exactly, in my own new test, on the day I have been
writing about it.** The setup call had no assertion, so it could not fail. The
guard was a second line of defence that should not have been needed; it is now
asserted at the call, where it belongs.

*The mutation worth keeping.* "Clear the flag unconditionally" — a clear-then-
reflag every night — still reports a non-null value to every "is it set"
assertion while destroying the "since when" the field exists for. Only
`leaves it alone while the task is still stale` sees it. Four mutations, four
caught, and that is the one I would not have written a month ago.

**State**
In review, red by design on `server/web`'s drift check and nothing else.
`@laika/server` 1710/1710, `cli` 19/19, lint and format EXIT=0.

---

## 2026-09-01T14:45:00Z — LAI-443 — the branch below the one I fixed

**Changed**
- `server/test/http/middleware/auth.test.ts` — three tests. No source change.

**Decided**
Put them next to the `token_rejected` case rather than in `deactivated.test.ts`,
where the subject would otherwise have put them. The ordering *is* the behaviour
— `TokenAuthError extends ApiError`, so which branch runs is decided entirely by
which `instanceof` comes first — and it is invisible from either test alone. A
reader who opens one file should be able to see the pair.

Both negative directions asserted: a refused token logs `token_rejected` and
**not** `session_refused`, and a deactivated account the reverse. Without the
negatives, "log both lines every time" satisfies the pair, and two lines that
always appear tell an operator nothing.

**Discovered**

*CHIEF's mutation — `if (false)` for the `ApiError` branch — went from 120/120
passing to red on two tests.* And the LAI-442 reordering mutation now turns
**LAI-437's** test red as well as one of these. That is the pair working: the
ordering is one property, and it fails from whichever side you break it.

*A log-only guard is worth having.* §6.1 keeps the reason out of the response on
purpose, so these lines are the only place the distinction survives. There is no
response assertion that could have caught either regression, which is exactly why
both were invisible — the status was right throughout.

**State**
In review. `@laika/server` 1713/1713, `cli` 19/19, lint and format EXIT=0.
`server/web` still red on one assertion, and it is **LAI-208's** deliberate
`stale_flagged_at` drift, not this task's — it clears when LAI-157 lands.

---

## 2026-09-01T15:10:00Z — LAI-206 — real values for the first-boot panel

**Changed**
- `server/src/services/setup.ts` — `systemStatus()`, `migrationsApplied()`,
  `smtpConfigured()`.
- `server/src/http/routes/setup.ts` — `system` on the status response.
- `server/test/http/routes/setup.test.ts` — seven tests.
- Filed `.tasks/backlog/LAI-158` (area: web).

**Decided**
**Did not build AC1 and AC3's migration *total*.** §6.4's payload has no field
for one, and the CHIEF decision appended to the task drops it deliberately —
"not a number in a file" — which is exactly what AC3's "total from the migrations
folder" would have been. The ACs predate the decision.

It is also right on the merits, and I checked rather than deferred: `index.ts`
runs `runMigrations` at line 30 and `serve()` at 60, and the migrator throws
rather than continuing, so **a server that can answer this request has applied
all of them**. `18/18` can never read anything else. A field that cannot vary is
precisely what LAI-106 AC5 says a status panel must not show. Offered CHIEF the
two lines if they want it in §6.4 anyway.

Everything read from the running instance: `database` from live PRAGMAs (a test
flips `journal_mode` and requires the field to follow, so a literal fails),
count from the migrator's own journal compared against the `.sql` files on disk.

`smtp_configured` boolean only, and asserted as such — the test stores a
recognisable blob and requires it not to appear in the body. This endpoint
answers before anybody has authenticated.

**Discovered**

*A new way for a task file to go wrong.* My frontmatter edit **failed to apply
and the commit landed anyway** — the script aborted on a bad anchor and the `&&`
chain after it did not stop. The file reached `.tasks/review/` still saying
`status: backlog`, `assignee: unclaimed`, nothing ticked, no outcome.

Not the `git mv` trap — the move worked. This was an **edit that never ran at
all**, and reading the field back out of the commit is the only thing that
caught it. Third time today §2's "verify from `git show`" has paid, and the first
time for this shape.

*A gap in the drift net, worth stating.* `SetupStatusBody` is not a `*View`, so
LAI-213's client-mirror check does not cover it. The client's status type can
drift from this one and nothing would notice. It is LAI-158's third criterion
rather than an implicit hope.

**State**
In review. `@laika/server` 1719/1719, `cli` 19/19, lint and format EXIT=0.
`server/web` red on LAI-208's declared assertion only.

---

## 2026-09-01T15:50:00Z — LAI-415 — the directory and the frontmatter must agree

**Changed**
- `server/test/tooling/task-file-state.test.ts` — new, eleven assertions over all
  236 task files. Reads `.tasks/`, never writes it, no auto-fix.

**Decided**
Exempted the 25 field-gaps **by name rather than leaving `done/` unchecked**. The
narrow version was three lines and would have silently dropped AC3's `done`; the
list looks worse and is better, because a new omission fails immediately and each
entry dies when the field is filled in.

State directories read from disk (AC2). `README.md` exempted alongside
`TEMPLATE.md` — neither is in a state directory, so the exemption is structural
rather than by name.

**Prove it can fail (AC6).** Flipped `status: review` → `done` on one of my own
files in `review/`:

```
× carries status: <the directory it is in>
+   ".tasks/review/LAI-443-session-refused-log-is-unasserted.md is in review/ but says status: done",
```

Reverted, `git status` clean. Re-ran it after a later retyping rather than
assuming it still held.

**Discovered**

*Four findings, reported not fixed.* `LAI-130` sits in `backlog/` saying
`status: done` — the two records disagree about whether the work is finished.
`LAI-118` is the LAI-045 shape exactly. `LAI-057` has **no frontmatter block at
all**, which is why it is also the only file with no id. And **`LAI-153` is in
`done/` and `review/` at once** — the same task, both copies, the two-copy state
§2 describes. That one is this week, not history.

*The task file's own exemption list was wrong, and the mechanism it specified
caught it.* AC5 names `LAI-101` as a third historical collision. It no longer
collides. Had I written it in on trust, the staleness guard would have failed
with "LAI-101 no longer collides — remove it from KNOWN_COLLISIONS". A task file
is a claim by someone who was also guessing — and here the claim and the
instrument that disproves it were in the same paragraph.

*One mutation tested a different branch from the one I claimed.* The "exempt a
field a file already has" mutation used a filename that does not exist, so it hit
the *file does not exist* staleness branch instead. Both are real, but I had
written down the wrong one. Re-ran it against a real file. That is the third time
today an instrument answered a question next to the one I was asking, and the
first time I caught it by reading my own output rather than by being told.

**State**
In review. `@laika/server` 1730/1730, `cli` 19/19, lint and format EXIT=0,
typecheck clean. `server/web` red on LAI-208's declared assertion only.

---

## 2026-09-01T16:30:00Z — LAI-119 — one idiom for a closed vocabulary

**Changed**
- `server/src/services/{tasks,sprints,projects}.ts` — re-exports.
- `server/src/http/routes/{tasks,sprints,projects}.ts` — reach for them.
- `server/test/http/vocabularies.test.ts` — new, eleven assertions.
- `server/test/tooling/task-file-state.test.ts` — LAI-415's stale exemptions.

**Decided**
The re-export, by weight of practice rather than by choosing: five files and one
MCP tool already used it, and `services/tasks.ts` already carried the line *with
a comment naming this task*. Three route files were the holdouts.

**Discovered**

*A fourth file the task did not list.* `routes/projects.ts` retyped
`PROJECT_ROLES` and `PROJECT_VISIBILITIES`, twice each — found by the structural
check on its first run, not by reading the task.

*Half the task's premise had expired and half was worse than stated.* The
`db/enums.ts` → SQL `CHECK` direction is **already covered** — AC4's mutation
turns `schema-migration-drift`'s "matches every named CHECK" red, a check that
did not exist when LAI-119 was filed. So AC4 is satisfied by a test I did not
write, and saying so beats claiming it.

The other direction was uncovered and I measured it before fixing: re-typing
`CreateBody`'s status list with `cancelled` dropped left **all 1730 tests
passing**.

*The check asks the route rather than reading its imports.* A test importing the
same constant passes by construction, so each case sends an invalid value and
reads the accepted set out of the refusal — the one place a route states what it
will take. Both entry points, because Zod on the body and `parseEnum` on the
query validate separately.

*One mutation printed ANCHOR FAILED twice before it ran.*
`priority: z.enum(TASK_PRIORITIES).optional(),` appears in both `CreateBody` and
`UpdateBody`; my second attempt widened the anchor with a neighbouring line that
is **also** duplicated between them. Third try, by line number. Each failed
attempt printed `EXIT=0` underneath, which without the guard reads as "the test
does not catch this" — the flattering direction, which is why the guard matters
more than it looks.

**LAI-415, alongside.** CHIEF fixed all four findings, so every exemption went
stale and named itself. `STATUS_DISAGREES` and `NO_FRONTMATTER_BLOCK` are
**deleted rather than emptied** — an emptied list is scaffolding that invites
re-exemption over a fix. `files.length - 1` became the property: it encoded
"exactly one file has no id", a contingent fact in a guard, which is this file's
own defect one level up.

And the check learned a third state CHIEF identified: **filed, then closed
without being built**. LAI-209/210/212 were never claimed, so they have no
`assignee`/`started`/`finished` because nobody started and nobody finished.
Exempting them by name would have recorded something false in a list titled
"predates the field"; requiring the fields would have meant inventing two
timestamps for work that never happened. Twenty entries now, from twenty-five,
and the new branch is load-bearing on four files.

**State**
In review. `@laika/server` 1740/1740, `cli` 19/19, lint and format EXIT=0.
`server/web` red on LAI-208's declared assertion only.

---

## 2026-09-01T17:00:00Z — LAI-228 — the refusal that lost the length

**Changed**
- `server/src/http/routes/projects.ts` — `ContextBody` drops `.max`, keeps
  `z.string()`.
- `server/test/http/routes/projects.test.ts` — tightened one test, added three.
- Filed `.tasks/backlog/LAI-159`.

**Decided**
`z.string()` stays. AC4 is not a formality: dropping the whole line hands a number
to `input.context_md.length` — `undefined` — and `undefined > CONTEXT_MD_LIMIT`
is false, so an oversize non-string would have been **stored**. The type is the
schema's job, the size is the service's.

**Discovered**

*The existing test passed against the bug, and its comment said why.* It matched
`/100000/` on the body — the **limit**, which both errors carry — above a comment
reading "whether zod or the service refuses it", which is precisely the
difference §7.3 singles out. A test naming the right property, built so the
property cannot be violated. Fourth of that family this week and the first where
the comment volunteered the hole.

*The Notes' general question has three more answers.* `heartbeats` loses
`repo_length`/`branch_length` and which of the two was too long; `projects`'s
`repo` loses the `expected: 'owner/name'` shape hint; `tasks`'s `tags` loses
`count`. Filed as LAI-159.

*And one case already right, which is what makes the rule statable.* `tasks.ts`
bounds a tag **name** at 64 where the service enforces 24 — the route bound is
**looser**, so the service's fuller message survives. So the rule is not "delete
every `.max`": a route bound may be a sanity guard looser than the service's, or
absent, but never equal, because equal means the service's error is unreachable.
Without that case I would have written the wrong rule into LAI-159.

*One mutation printed ANCHOR FAILED first.* `length: input.context_md.length,`
appears twice in `services/projects.ts` — the refusal and the activity payload
recording how the document grew. Second task running where the anchor collided
with a second, legitimate occurrence of the same line.

**State**
In review. `@laika/server` 1742/1742, `cli` 19/19, lint and format EXIT=0.
`server/web` red on LAI-208's declared assertion only.

---

## 2026-09-01T17:20:00Z — LAI-400 — D-035's names in server/ prose

**Changed**
- `server/README.md`, `src/http/static.ts`, `src/paths.ts`, and six test files.
  Eighteen references.

**Decided**
`server/README.md` needed more than a rename. "Nobody except Builder-A edits
anything under this directory" becomes **false** as "Nobody except CORE", because
D-016 gave `server/web/` to SHELL and the README's own layout block lists `web/`
two lines below. AC3 asks a boundary comment to keep explaining the boundary; a
rename that makes it wrong fails that harder than deleting it would. Flagged to
CHIEF rather than buried, with an offer to revert to the literal rename.

**Discovered**

*The known-occurrences list was half the job, which is why the task said to
re-run the grep.* It named Builder-A/B only; AC2's mapping also carries
PM → CHIEF, and there were **nine** PM references the list did not mention. The
difference between finishing the task and finishing the list.

*Two string literals, both `describe()` titles, and no assertion string contains
a session name.* Classified every match before editing rather than assuming, and
verified afterwards with a diff filter that strips comment lines — it returns
exactly those two.

*A note for LAI-401 that is not a rename.* `WEB_NO_MIRROR_PATTERNS`'s docblock
explains itself in terms of **D-028**, which D-031 retired. So renaming
Builder-A → CORE there would leave a comment that reads correctly and describes
an arrangement that no longer exists — this task's own failure mode, one level
up. Their file, their call; left as a note.

**State**
In review. `@laika/server` 1742/1742 — unchanged, as a prose task should leave it
— `cli` 19/19, lint and format EXIT=0. `server/web` red on LAI-208's declared
assertion only. **My area's backlog is now clear.**

---

## 2026-09-01T17:50:00Z — LAI-444 — the census nobody had taken

**Changed**
- `server/test/tooling/response-type-coverage.test.ts` — new, six assertions.
- Filed `.tasks/backlog/LAI-160` (area: web).

**Decided**
Built the completeness guard rather than renaming anything, because **the task's
premise is wrong**. LAI-213's check does not find types by name — no `readdir`,
no regex on `View`, only a hand-written `PAIRS` table of seven, one of whose
entries (`ProjectSummary`) already does not end in `View`. So AC2's preferred
remedy — rename to `*View`, "no new machinery" — brings a type under nothing.

Told CHIEF before building, because the shape of the fix changes.

**Discovered**

*The census: 28 served, 7 paired, 21 unguarded — and **twelve of the twenty-one
already end in `View`**.* The convention is being followed and the guard does not
read it. Coverage is decided by a list nobody checks for completeness, which is
the task's own diagnosis one layer lower than it supposed: not the naming, the
table.

*`TokenView` is the sharpest single fact.* Client and server use the **identical
name** and are still not compared. Any theory that naming decides coverage dies
on that one line.

*Seven have no client mirror at all* — the unbuilt screens. Kept in a separate
group from the fourteen that do, because collapsing them would hide which needs
work and which needs a screen.

*The proof is the diagnosis.* Two of the three mutations add a new response type
— one ending in `View`, one not — and both go red. The net catches by what is
**served**, not by what it is called, and that is demonstrated rather than
argued.

**On §2.** I claimed this in two commits and that is what produced the red CHIEF
is holding LAI-400 for. Answered their question: **claim in one commit.** The
two-commit form has no property the one-commit form lacks, and it breaks §2's own
`0 insertions(+)` tell — on a claim commit today that output is normal and
correct, so the tell is dead exactly where it would have caught my LAI-206
mistake this afternoon. Claiming in one commit from LAI-159 onward regardless of
when §2 is rewritten.

**State**
In review. `@laika/server` 1748/1748, `cli` 19/19, lint and format EXIT=0.
`server/web` red on LAI-208's declared assertion only.

---

## 2026-09-01T18:20:00Z — LAI-159 — three routes shadowing their service

**Changed**
- `server/src/http/routes/{heartbeats,projects,tasks}.ts` — the shadowing `.max`
  removed; `min(1)` and the type rules kept.
- `server/src/services/heartbeats.ts` — re-exports `REPO_MAX_LENGTH` rather than
  declaring a second copy.
- `server/test/http/routes/{heartbeats,projects,tasks}.test.ts` — seven tests.
- `server/test/tooling/shadowed-bounds.test.ts` — new guard.

**Decided**
The rule is about **equality**, not about `.max`, and the tag-name case is what
makes it statable: `tasks.ts` bounds a tag name at a literal 64 where the service
enforces 24, so the looser route bound lets the service's fuller message through.
The check therefore looks for a shared **identifier** — a literal cannot be
shadowing anything, because there is no shared name to disagree about.

It compares against the **comparison** in a service, not the declaration: a
service may export a constant for a route without enforcing it.

`REPO_MAX_LENGTH` folded into `projects.ts`, which owns the `repo` column;
`BRANCH_MAX_LENGTH` stays in heartbeats, where nothing else has an opinion.

**Discovered**

*The guard's first run reported the fix as the defect.* Three failures, all of
them **my own comments** — the prose quoting ``.max(REPO_MAX_LENGTH)`` while
explaining why it was removed. A check that reads comments as code reports the
explanation of a fix as the thing it fixed. It strips comments now, and the
docblock says why, because that looks like tidiness and is not.

*The guard protects LAI-228 retroactively*, which is worth more than the three
fixes it was written for: restoring `.max(CONTEXT_MD_LIMIT)` turns it red. That
one was found by a person reading a route; this is what finds the fourth.

*Each fix is caught twice, independently.* The structural guard cannot tell
whether the service's error is any good; the behavioural tests cannot see a
**new** route repeating the mistake. Neither alone is enough and I would not have
written both a week ago.

**State**
In review. `@laika/server` 1758/1758, `cli` 19/19, lint and format EXIT=0.
`server/web` red on LAI-208's declared assertion only. First task claimed in one
commit under the new §2 — `3 insertions(+), 2 deletions(-)`, so the tell works.

---

## 2026-09-01T18:50:00Z — LAI-156 — a read-only database serves reads

**Changed**
- `server/src/auth/tokens.ts` — `touchTokenUsage` survives `SQLITE_READONLY`.
- `server/src/auth/resolve-actor.ts`, `src/http/middleware/auth.ts` — a logger
  threaded so the degradation has somewhere to be said.
- `server/test/http/middleware/auth.test.ts` — five new; LAI-437's three re-based.

**Decided**
It changes. The task frames it as LAI-231's question answered the other way, and
it is — but the answers differ **because of which signal is lost**, not by taste.
Swallowing in `poll()` hid the *only* evidence the feed had stopped delivering.
Here the read is unaffected and every write request still fails loudly at its own
write: one signal of many, muffled into a log line rather than into nothing.

And the state being replaced was worse than either alternative — the throttle
meant a read-only instance worked for sixty seconds after each token's last use
and then stopped. Nobody chose that; it fell out of a throttle written for a
different reason.

Constraints that make it defensible rather than convenient: only
`SQLITE_READONLY` (verified: `code=SQLITE_READONLY`, not guessed), only that one
statement, logged once per **spell** with the flag reset on the next successful
write.

LAI-437's tests re-based rather than deleted — they used this write as their
resolver failure, so leaving them would have asserted the opposite of what
LAI-437 established. Same property, against a failure that is still a failure.

**Discovered**

*A mutation survived and the bad test was mine.* "Catch every error instead of
only `SQLITE_READONLY`" **passed**. My "does not swallow a non-read-only write
failure" test dropped a table and got its 500 from `loadActor`'s **read** — so it
never exercised the catch and passed whether the catch was narrow or total.

The right property, built so the property could not be violated. That is the
family I have been finding in other people's tests all week, and this is the
second time today it has been in my own. Rewritten with a trigger that aborts the
`UPDATE`, so reads keep working and the failing statement is exactly the one the
catch wraps.

**State**
In review. `@laika/server` 1763/1763, `cli` 19/19, lint and format EXIT=0.
`server/web` red on LAI-208's declared assertion only. **My area is clear** —
nothing unclaimed in `.tasks/backlog/` with `area: server`.

---

## 2026-09-01T19:40:00Z — LAI-161 — §12's secrets at rest

**Changed**
- `server/src/secrets.ts` — new, AES-256-GCM under HKDF-SHA256.
- `server/test/secrets.test.ts` — twelve tests.
- Filed `.tasks/backlog/LAI-162`; released LAI-446 and gated LAI-447 behind this.

**Decided**
Released LAI-446 rather than absorbing the crypto into it. §12 is unimplemented —
no encrypt, no decrypt, no key derivation — and three columns are declared for
ciphertext nothing has ever written. Both of CHIEF's M6 tasks need it and neither
names it, because from outside it looks like a column read.

Filed rather than folded in because **a defect here is invisible to every test
somebody would write**: AES-GCM with a repeated nonce, or with the tag dropped,
still returns plausible ciphertext and still decrypts on the happy path.

Four decisions, all in the file: HKDF because `LAIKA_SECRET` is a passphrase and
§12 says "derived from"; a fixed salt because there is one secret per instance
and no population to protect; per-column keys via HKDF's `info`, typed as a
closed union so a typo is a compile error rather than a different valid key; and
a `v1.` text-versioned format so a second scheme can coexist.

Rotation answered at CHIEF's request: **unsupported**, every stored secret stops
decrypting, and the instance still reports itself configured — LAI-437's family
again. Pinned by a test; the procedure is LAI-162.

**Discovered**

*The fixed-nonce mutation is caught by exactly one assertion.* Every other test
in the file — all three tamper cases, every round-trip — passes with a constant
nonce. That is the whole argument for this being its own task, demonstrated
rather than asserted.

*And one mutation I could not write.* "Never set the auth tag" is unreachable:
Node's GCM `final()` throws when no tag was set. So that row proves `setAuthTag`
is load-bearing, not that a missing check would be caught. Said so rather than
counting it.

*My filtered run was green while the root gate exited 2.* A
`noUncheckedIndexedAccess` error in my own test — the workspace script is
`typecheck && vitest`, and a bare `vitest` skips the first half. D-045 from the
other direction: not a check in someone else's directory, but **half of my own
workspace's gate**.

**State**
In review. Root gate **EXIT=0**: server 1775/1775, web 604/604, cli 49/49, lint
and format EXIT=0. `server/web` is green too — SHELL's LAI-157 landed while I was
building, so LAI-208's declared red is gone.

---

## 2026-09-01T20:45:00Z — LAI-448 — a principal for triggers with no human

**Changed**
- `server/src/policy/actions.ts` — `SYSTEM_ACTIONS`, and `ALL_ACTIONS` gains them.
- `server/src/policy/can.ts` — `SystemPrincipal`, `systemPrincipal()`,
  `SYSTEM_GRANT`, and `can()` takes a `Principal`.
- `server/src/jobs/jobs.ts` — four `assertCan` calls; `vacuum` documented as the
  one exception.
- `server/test/policy/matrix.test.ts` — §3.4 rows, both directions.
- `server/test/jobs/jobs.test.ts` — a structural guard that every job asks.
- `server/test/tooling/policy-spec-drift.test.ts` — §3.4 parser + four in-flight
  exemptions.

**Decided**
A distinct kind, per D-050. Two of the six grants are **existing** actions —
`task.write`, `comment.create` — because the webhook performs the same operation
a person does on the same resource; only the principal differs. System-only twins
would be two rules for one operation and eventually two answers.

`vacuum` keeps no `assertCan`. Rule 1 governs reading and writing *data*; VACUUM
rewrites storage and touches no row, so there is no resource to name, and
inventing an action to satisfy the shape of the rule adds a permission that
grants nothing. Exempted **by name** so a fifth job cannot inherit it.

**Discovered**

*The finding was not the missing calls — it was that removing one is invisible.*
`assertCan(SYSTEM, …)` always passes, so deleting it changes nothing behaviour
can see. Measured: **zero failures** across `test/jobs` and `test/policy`. That
is how the rule came to be broken in eight places, and a task that only added the
calls would have left the next removal just as silent. Structural guard added,
comments stripped first — my own LAI-159 lesson, applied before it bit.

*The exemption would not have expired.* `ACTIONS_WITHOUT_A_ROW`'s staleness test
reads §3.1 and §3.2 only, and **§3.4 is a new section** — so the four entries
would have become permanent without anybody deciding that. Extended the drift
parser to scan §3.4 whenever it appears, and proved it by pasting a §3.4 into the
SPEC and watching the staleness test go red.

That is the third time this week the interesting defect has been in the *retirement*
of a guard rather than the guard.

*For LAI-446, found here rather than there.* Services take `ResolvedActor`, not
`Actor`, so a `SystemPrincipal` cannot be handed to `changeStatus` or
`addComment`. Widening those signatures is a real change and outside this task's
criteria; flagged so LAI-446 does not meet it cold.

**State**
In review. Root gate **EXIT=0**: server 1799/1799, web 604/604, cli 49/49, lint
and format EXIT=0. Four in-flight exemptions awaiting CHIEF's §3.4, each proved
to expire.

---

## 2026-09-01T21:30:00Z — LAI-447 — the provider's write half

**Changed**
- `server/src/db/schema.ts` + migration `0018` — `orgs.ai_key_last4`.
- `server/src/services/orgs.ts` — `nextAiSettings`, validation, encryption.
- `server/src/http/routes/orgs.ts` — the three fields, and the empty-secret guard.
- `server/src/app.ts`, `src/index.ts`, `test/helpers/auth.ts` — `serverSecret`
  threaded.
- `server/test/services/orgs.test.ts`, `test/http/routes/orgs.test.ts`,
  `test/tooling/schema-spec-drift.test.ts`.

**Decided**
`key_last4` **stored**, per AC4, and the reason is LAI-222's own docblock:
deriving it means decrypting the key to build a response, which is the one place
§12 must never reach. §4.2 has no row, so `COLUMNS_NOT_IN_SPEC` carries it.

Settings computed **as a whole** and validated against the resulting state.
Otherwise three legal requests reach a configuration no single request could ask
for — set `openai_compatible` with a URL, then clear the URL. There is a test for
that sequence and a mutation proving the natural implementation fails it.

**Discovered**

*I wrote a guard into a comment and did not implement it.* `serverSecret` is
optional on `createApp`, and I documented that "a route that needs it and does
not get it fails at the boundary rather than encrypting under an empty key" —
then passed `?? ''` and moved on. Caught by re-reading my own comment against the
code, which is the D-037 check pointed at myself. Implemented, with a test that
requires the column to stay null.

*AC6 is answered by measurement, and the answer is uncomfortable.* Nothing here
decrypts the key, so after a `LAIKA_SECRET` rotation `GET /org` answers exactly
as before — configured, provider, last four — and the failure waits for §10.2.
That is LAI-161's rotation finding one layer up, and I pinned it in an assertion
rather than a comment: the test asserts both the misleading answer **and** that
the key genuinely cannot be recovered, so it is unusable rather than mislabelled.

*The leak mutation worth keeping.* Adding `key_enc` to the view leaks through a
field nobody put in `OrgAiView` — caught only because the assertion reads the
**body** rather than the type. Three tests fire.

**State**
In review. Root gate **EXIT=0**: server 1816/1816, web 604/604, cli 49/49, lint
and format EXIT=0. One in-flight exemption, awaiting §4.2's `ai_key_last4` row.

---

## 2026-09-01T21:55:00Z — LAI-445 — coverage the census could not see

**Changed**
- `server/test/tooling/response-type-coverage.test.ts` — `extendsGraph`,
  `coveredByExtends`, three-way arithmetic; `ProjectView` out of `UNPAIRED`.

**Decided**
Resolved `extends` rather than adding a third column to the map. The task file
made the argument without meaning to: a classification somebody has to notice
covers the case in front of them, and this covers the next one too. Transitive,
because `A extends B extends C` compares C's fields just as surely as B's.

`names a client type that exists` untouched — the row **left** the map rather
than gaining prose, so there was never a reason to weaken the assertion that
keeps the list honest.

**Discovered**

*CHIEF asked me to drop an exemption that is not stale, and I did not.* The
instruction was to drop `orgs.ai_key_last4` from `COLUMNS_NOT_IN_SPEC` because
§4.2 now covers it. It does not: `git log --all -S "ai_key_last4" -- docs/SPEC.md`
is **empty**, master's §4.2 `orgs` table is still five rows, and `09326b1` —
whose commit message names the row — changed no file under `docs/`.

My drift check is 29/29 **green with the exemption**, which is the correct state
when the code has a column the spec does not. Dropping it would have turned the
gate red on a merge whose message said the opposite.

Second time today a message described a file that had not been written — the
first was LAI-447's "reuse the existing crypto" for a mechanism that did not
exist. The check is the same one CLAUDE.md already names: **read the artefact,
not the description of it**, and that applies to a commit message as much as to
a task file.

**State**
In review. Root gate EXIT=0: server 1818/1818, web 604/604, cli 49/49, lint and
format EXIT=0. Holding LAI-447's exemption until §4.2's row exists.

---

## 2026-09-01T22:50:00Z — LAI-446 — the GitHub webhook receiver

**Changed**
- `server/src/services/webhooks.ts` — `handlePush`, `handlePullRequest`.
- `server/src/http/routes/webhooks.ts` — new, mounted at `/webhooks` outside
  `/api/v1`.
- `server/src/auth/resolve-actor.ts` — `withProject`/`activityActor` widened to
  `ServiceCaller`, by overloads.
- `server/src/services/tasks.ts` — D-051's system branch on `changeStatus`.
- `server/test/http/routes/webhooks.test.ts`, `test/tooling/activity-payload-names.test.ts`.

**Decided**
`webhook.received` for an unverified delivery is a **log line, not an activity
row**. §10.1 says "logged as", and this endpoint answers before anything is
authenticated — an activity row there is a permanent, append-only record an
anonymous caller can create at will, and §4.8 has no retention.

Overloads rather than a generic with a cast for `withProject`: the cast would
assert exactly what the function exists to establish.

Dedupe in memory, with the restart cost written where it lives rather than
discovered. Eviction stops at the first live entry — sweeping made 100k
deliveries take thirteen seconds, found because the bound test was **slow**
rather than because it failed.

**Discovered**

*`issue_comment` cannot be built, and the model half-anticipated it.*
`comments.author_id` is NOT NULL with a FK to `users`, and a mirrored GitHub
comment has no Laika user — while `created_via` **already includes `'webhook'`**
and `comments.created_via` uses that enum. So the data model anticipated
webhook-authored comments and then made them unstorable. Three ways out, all
schema or policy decisions; I named them and did not guess.

*Two guards caught me.* Routes may not import `db/` — I reached for
`requireOrgId` directly. And `activity-payload-names` requires the sweep to
**produce** every type a service can emit, so adding `webhook.commit` to the
vocabulary was not enough; the sweep drives `handlePush` for real now.

*And the `&&`-chain failure again.* My first attempt to write the outcome aborted
on a bad anchor — correctly, because CHIEF had already ticked AC5 when ruling
D-050 — and the commit landed anyway with `status: in-progress` and one tick.
Second time today. The anchor guard was right; the shell was not. Caught by
reading the field back out of the commit.

**State**
In review, three of four handlers. Root gate EXIT=0: server 1831/1831, web
604/604, cli 49/49, lint and format EXIT=0.

---

## 2026-09-02T00:20:00Z — LAI-449 — a comment with no Laika author

**Changed**
- `server/src/db/schema.ts` + migration `0019` — `comments.author_id` nullable.
- `server/src/services/comments.ts` — view type, the write, two explicit refusals.
- `server/src/services/watchers.ts`, `src/services/webhooks.ts`,
  `src/http/routes/webhooks.ts` — `handleIssueComment`.
- `server/test/http/routes/webhooks.test.ts` — five tests.
- Filed `.tasks/backlog/LAI-163`.

**Decided**
Edit and delete refuse **explicitly**, not by the `ownerId` comparison. §3.2's
cells are *own* and *own + any*: null already denies the *own* half, but a lead
holds *any* and falls through. `409` rather than `403` — it is not that this
actor may not; the request does not apply to this comment, whoever asks.

Only `created` is mirrored. §4.7 stores no GitHub id, so an edit has no row to
find, and mirroring it as a second comment would be worse than not mirroring it.

**Discovered**

*The compiler enumerated two of the seven readers for me.* I grepped four; the
view type and the watcher set both failed to typecheck the moment the column
became nullable. Cheaper than my list and it does not forget.

*One guard is defence in depth and I am not counting it.* Skipping a null author
in `impliedWatcherIds` fails no test — `watchersOfTask` filters through
`canRead`, and `loadActor(db, null)` finds nobody, so the null is dropped before
anything sees it. **The mutation survived, the rewrite did not save it, and that
is how I learned the property was unobservable rather than untested.** Labelled
where it lives instead.

My first test there was the weak-fixture shape again — asserting the stored
`author_id` was null, which is true whether or not the null reaches the set.

*AC2 expected a drift failure and none came.* `schema-spec-drift` compares column
**names**, not nullability, so §4 can promise `NOT NULL` while the schema is
optional, indefinitely, green. Its sibling `schema-migration-drift` **does** check
`notNull` — so the chain §4 → schema → migrations is checked on two legs and not
the third. Filed as LAI-163.

**State**
In review. Root gate EXIT=0: server 1836/1836, web 604/604, cli 49/49, lint and
format EXIT=0. LAI-446's fourth handler is now built.
