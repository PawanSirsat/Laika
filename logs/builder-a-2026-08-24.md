# Builder-A — 2026-08-24

## 2026-08-24T01:52:00+05:30 — LAI-001

**What changed**
- `package.json` — workspace root: `type: module`, `packageManager: pnpm@10.32.1`,
  `engines.node >=22`, scripts `dev/build/test/typecheck/lint/lint:fix/format/format:fix`
- `pnpm-workspace.yaml` — packages `server` + `cli` placeholder; `onlyBuiltDependencies: [esbuild]`
- `tsconfig.base.json` — `strict`, `noUncheckedIndexedAccess`, `noImplicitOverride`,
  `exactOptionalPropertyTypes`, `moduleResolution: bundler`, `target/lib ES2023`,
  plus `noImplicitReturns`, `noFallthroughCasesInSwitch`, `noUnused*`, `verbatimModuleSyntax`
- `eslint.config.js` — flat config, `recommendedTypeChecked` + `stylisticTypeChecked`
  via `projectService`, `eslint-config-prettier` last; `ban-ts-comment` set to
  `allow-with-description` (min 10 chars) to enforce CLAUDE.md §5
- `.prettierrc` — printWidth 100, single quotes, trailing commas, LF
- `.npmrc` — `engine-strict`, `link-workspace-packages`, `prefer-workspace-packages`, `node-linker=isolated`
- `.nvmrc` — `22`
- `.gitignore` — node_modules, dist, `server/public/`, `*.db{,-wal,-shm}`, `data/`,
  `.env*`, coverage, tool caches, `.DS_Store`
- `pnpm-lock.yaml` — generated and committed
- `server/package.json` — `@laika/server`, scripts `typecheck/test/test:watch`
- `server/tsconfig.json` — extends the base; `include: src, test, vitest.config.ts`
- `server/vitest.config.ts` — node environment, `restoreMocks`/`clearMocks`
- `server/test/toolchain.test.ts` — 3 passing tests proving the runner runs TypeScript
- `server/src/.gitkeep` — so LAI-002 has a place to land
- `.sessions/builder-a.md` — identity confirmed for today

**Decisions**
- **TypeScript pinned to `~6.0.3`, not `7.0.2`.** `typescript-eslint@8.67.0`
  declares `typescript: ">=4.8.4 <6.1.0"`. TS 7 is the native (Go) port and sits
  outside that range, so choosing it would have silently cost us the type-aware
  linting this task explicitly requires. 6.0.3 is the newest the linter supports.
  Revisit when typescript-eslint ships TS 7 support — it will want a task.
- **`format` is a *check* script, `format:fix` writes.** AC4 requires
  `pnpm format --check` to pass; `prettier --write . --check` is a hard error, so
  a write-by-default `format` could never satisfy it. Prettier tolerates the
  duplicated `--check`, so both `pnpm format` and `pnpm format --check` work.
- **Prettier ignores Markdown entirely.** The globs are code+config only. A
  repo-wide `prettier --write` would rewrite `docs/`, `CLAUDE.md`, `.tasks/**`
  and other sessions' logs — every one of them outside my area and hand-wrapped
  to 80 columns deliberately.
- **`pnpm-lock.yaml` excluded from Prettier via a negative CLI pattern**
  (`"!pnpm-lock.yaml"`) rather than a `.prettierignore`. LAI-001's scope
  exception lists eight root files and `.prettierignore` is not among them;
  a CLI pattern gets the same result without creating an unauthorised file.
- **`dev` and `build` are `pnpm -r --if-present` pass-throughs.** No workspace
  package defines them yet. The alternative — inventing `server/src/index.ts` so
  `tsc` had inputs — would have written a file LAI-002 owns. Empty-but-correct
  beats colliding.
- **`node-linker=isolated` (pnpm default, set explicitly).** It is what makes
  "the lint passed because eslint got hoisted" impossible; worth stating rather
  than inheriting.

**Discovered**
- `pnpm format` is red on `plugin/.claude-plugin/plugin.json` — Builder-B's area,
  so I cannot fix it. Cosmetic (a `keywords` array Prettier collapses).
  → **LAI-014** filed (`area: plugin`, discovered-from: LAI-001).
- **`docs/SPEC.md` was rewritten in `7b98665` from a 13-section document to an
  8-section one, and every task file still cites the old numbering.** LAI-002
  says "read SPEC §10.2, §10.4, §6.3"; none exist now. LAI-011 says "read §5
  before implementing transitions" — §5 was *Task lifecycle* and has no
  successor. LAI-003 requires "every index in SPEC §4.11" and there is no index
  list any more, so that criterion is currently unverifiable. It is not only
  numbering: the tasks and the new spec disagree on project `key` vs `slug`,
  `orgs.signup_mode` vs `org.invite_only`, whether the error envelope carries
  `details`, and whether `ready` includes `todo`. This blocks honest work on
  LAI-002 onward. → **LAI-015** filed (`area: docs`, discovered-from: LAI-001).
- **`.gitignore` covering `server/public` (AC6 here) contradicts LAI-002 AC6**,
  which wants a committed placeholder `server/public/index.html` so the SPA
  fallback is testable. A clean clone would not have it and the test would pass
  only on the machine that wrote it. → **LAI-016** filed (`area: server`,
  discovered-from: LAI-001) with two concrete shapes to choose between.
- `moduleResolution: bundler` (mandated by AC3) means extensionless imports,
  which plain Node ESM rejects. Fine under `tsx`, but it constrains how LAI-002
  builds for production — `tsc` emit alone will not produce runnable ESM. Not
  filing a task: LAI-002 picks its build tool and will meet this head-on.
- Two id collisions in `.tasks/backlog/` in one sitting: Builder-B and I both
  reached for `LAI-013` within the same minute. I yielded and renumbered mine to
  014/015/016. "Next unused number" is not a lock — worth knowing before someone
  loses a task file to it.
- My own mistake, recorded so nobody repeats it: I ran
  `git add <new-path> <old-path>` when claiming. `git mv` had already removed the
  old path, so `git add` failed on it and staged **nothing** — the claim commit
  captured only the rename, and my frontmatter edit sat unstaged until PM's
  `[ops]` sweep (`7b98665`) picked it up along with everything else I had
  in-flight. Stage the new path only, and check `git show --stat` after a claim.

**State**
LAI-001 moved to `.tasks/review/`. Verified from a fresh `git clone` into a temp
dir: `pnpm install --frozen-lockfile` → `typecheck` → `lint` → `test` all exit 0.
`pnpm format --check` has exactly one finding, outside my area (LAI-014).
Nothing blocked for me. **`git push` fails — no credentials in this session**
(`could not read Username for 'https://github.com'`), so every commit above is
local only. Someone with `gh auth` needs to push.

## 2026-08-24T01:53:30+05:30 — session end

**What changed**
- nothing since the LAI-001 entry above; this records an attempted claim that
  found no eligible task

**Decisions**
- Did not claim anything. `.tasks/done/` is empty, and **all ten** remaining
  `area: server` tasks trace their `depends-on` back to LAI-001, which is sitting
  in `.tasks/review/`. CLAUDE.md §2 gates claiming on dependencies being in
  `done/`, and only PM moves `review/ → done/`. Claiming LAI-002 anyway would
  have meant building on a baseline nobody had reviewed — the whole point of the
  gate. Stopped instead of inventing work (`/claim` step 3).

**Discovered**
- The dependency graph is a chain, not a fan: LAI-002…LAI-011 and LAI-016 all
  reduce to LAI-001. Until PM reviews it, Builder-A has **zero** claimable work,
  and the same will be true of LAI-002 for everything after it. Not filing a
  task — it is a scheduling fact for PM, not a code change — but worth saying at
  standup: builder throughput here is bounded by PM review latency, not by
  builder capacity.

**State**
Blocked on PM. LAI-001 is in `.tasks/review/` with all seven criteria ticked and
five review notes appended (one of them asks PM to confirm the `format` script's
repo-wide scope). Next session should re-run `/claim` once LAI-001 is in
`.tasks/done/`; LAI-002 and LAI-003 both unblock together at that point and can
run in either order.

Reminder carried forward: **nothing in this session has been pushed** — `git push`
fails with `could not read Username for 'https://github.com'`. Six local commits
(`d6c6cb7`…`13e60ba`) are waiting on someone with `gh auth`.

## 2026-08-24T02:05:00+05:30 — claim attempt (no task claimed)

**What changed**
- moved to my own worktree `Laika-builder-a/` on branch `builder-a` (CLAUDE.md
  §4.2, new since my last entry); merged `master` — fast-forward to `7b828c9`
- `.tasks/backlog/LAI-015-spec-task-crossrefs.md` — appended an "Update after the
  spec merge" section; the task as I filed it is now factually wrong in PM's
  favour and would have understated the problem

**Decisions**
- **Claimed nothing.** `.tasks/done/` on `master` holds only `.gitkeep`. All ten
  `area: server` backlog tasks reduce to LAI-001, which is in `.tasks/review/`.
  `/claim` step 4 says report and stop rather than invent work, so I stopped.
- **Edited a task file I do not own, deliberately.** `.tasks/` is PM's (§1), but
  LAI-015 is a task *I* filed an hour ago, still `unclaimed`, still in backlog —
  no session's claim was touched (§6). Leaving it as written would have told PM
  "§4.11 has no successor" when the truth is now worse than that. Correcting my
  own filing beat filing a second task to correct the first. Flagging it here so
  PM can push back if they read the boundary more strictly than I did.

**Discovered**
- **PM's spec merge (`e78d10f`) fixed the missing sections and made the dangling
  citations more dangerous, not less.** Numbering shifted by one from §9 onward,
  so LAI-002's "§10.2" (meant: Hono middleware order) now lands on *Webhooks*,
  and LAI-003's "§4.11" (meant: the index list) now lands on the `invites` table
  — the index list moved to §4.13. A citation that resolves to a real but wrong
  section gives the builder no signal at all. Full table in LAI-015; I argued
  there for raising it to p1, since LAI-002 and LAI-003 are the next two claims.
- History was re-authored to the personal account between sessions, so my LAI-001
  commits carry new SHAs (`3180d79`…`146dbbd`, was `544be54`…`1df9675`). The work
  survived intact — verified the tooling files and my log are all present on
  `builder-a`. Worth knowing before anyone quotes an old SHA from my last entry.

**State**
Idle, blocked on PM. LAI-001 sits in `.tasks/review/` with no review notes
appended yet. Nothing else in my area is claimable until it reaches
`.tasks/done/`; LAI-002 and LAI-003 unblock together at that moment. Working
directory clean on `builder-a` at `badea39`.

## 2026-08-24T03:20:00+05:30 — LAI-002

**What changed**
- `server/src/index.ts` — process entry: read env, build app, bind port, wire signals
- `server/src/app.ts` — `createApp()`, the §11.2 chain, route mounting, `notFound`/`onError`
- `server/src/env.ts` — `PORT`/`HOST`/`NODE_ENV` parsing, throws `EnvError` on bad input
- `server/src/log.ts` — JSON-to-stdout logger with an injectable sink
- `server/src/paths.ts` — `SERVER_ROOT` by walking up to `package.json`; `PUBLIC_DIR`, `FALLBACK_DOCUMENT`
- `server/src/version.ts` — reads `version` from `package.json` at runtime
- `server/src/shutdown.ts` — `createShutdownHandler()`, drain then exit 0, forced-close backstop
- `server/src/http/errors.ts` — `ApiError`, `ERROR_STATUS`, the §6.3 envelope
- `server/src/http/error-handler.ts` — `ApiError` / `HTTPException` / unknown-throw paths
- `server/src/http/context.ts` — typed `AppEnv`
- `server/src/http/middleware/{request-id,logger,auth,rate-limit,error-boundary}.ts`
- `server/src/http/routes/health.ts` — `GET /api/v1/health`
- `server/src/http/static.ts` — static serving, SPA fallback, `isReservedPath`, `resolveWithinRoot`
- `server/src/static/fallback.html` — the committed no-SPA-yet document (LAI-016)
- `server/test/**` — 50 tests across 7 files
- `server/tsconfig.json` — `allowImportingTsExtensions`, `rewriteRelativeImportExtensions`
- `server/package.json` — `hono`, `@hono/node-server`, `dev` script

**Decisions**
- **`createApp()` is separate from the bootstrap.** Hono's test client needs the
  app as a value; a factory that also called `serve()` would make every HTTP test
  open a real socket. This is why AC9 is cheap and AC7 is possible at all.
- **`publicDir` and `fallbackDocument` are injectable options.** Without that,
  "the fallback works on a clean clone" is untestable on a machine that has ever
  run a build — the test would pass or fail depending on local state. Tests point
  `publicDir` at a directory that does not exist, which *is* the clean-clone
  condition. Rejected the alternative of deleting `server/public/` in a test
  fixture: destructive, and racy across parallel test files.
- **Paths resolve from `import.meta.url`, never from `process.cwd()`.** cwd is
  `server/` under `pnpm dev`, the workspace root under `pnpm test`, and `/app` in
  the container. Anything anchored to cwd works in exactly one of those.
- **`details` is always present in the error envelope, `null` when empty.** A key
  that sometimes vanishes makes every client feature-detect it. §6.3 lists three
  keys; this keeps all three, always.
- **The request id is echoed to the client only on 5xx** (§13.2) but set as an
  `X-Request-Id` header on every response. Inbound ids are honoured for proxy
  correlation, but length-capped at 128 and character-filtered — that value lands
  in log lines, and an unbounded attacker-controlled string in a log is how log
  injection starts.
- **CORS denies all cross-origin by default.** Same-origin in production,
  Vite-proxied in development. Widening it should be deliberate.
- **Static traversal is clamped, not rejected.** `/../package.json` normalises to
  a path *inside* `public/` rather than erroring. My first test asserted `null`
  and was wrong — the invariant worth testing is containment, so the test now
  asserts the result is never above the root. Null bytes and malformed
  percent-encoding are still rejected outright.
- **The git-backed LAI-016 tests skip when there is no work tree.** A Docker
  build context has no `.git`; a test that cannot run there should say so rather
  than fail.

**Discovered**
- **Hono routes only `Error` instances to `onError`.** A handler throwing a
  string or a plain object bypassed the error handler completely — under
  `@hono/node-server` that is an unhandled rejection and a dropped connection,
  not the `internal` envelope AC4 asks for. Found by a test written to check
  exactly that, and fixed with an `errorBoundary` middleware. This is a sixth
  middleware in a §11.2 chain that says "fixed order", so I flagged it at review
  as a visible deviation rather than burying it.
- **`SIGTERM` under `tsx` exits 143 and never runs the shutdown handler.** tsx
  forks a child and does not forward the signal. Under plain `node` the same code
  logs `shutdown.start` → `shutdown.complete` and exits 0. This cost me a
  confusing ten minutes staring at a correct implementation, and it is a live
  trap for LAI-008: a container whose entrypoint is `tsx`, `pnpm start` or a
  shell wrapper will have `docker stop` escalate to `SIGKILL` on every single
  deploy. → **LAI-019** filed with the finding written out.
- **§6.3's error vocabulary cannot express `413`**, which `bodyLimit` — mandatory
  on every route per §13.1 — now produces. There is no `405` either. I preserved
  the accurate status and reported the nearest code beside it; forcing agreement
  would report a `405` as a `422`. → **LAI-017** filed (area: docs).
- **Security headers (§13.1) had no task carrying them.** Not in LAI-002's
  criteria, not in LAI-006's. Left out deliberately — CSP depends on what Vite
  emits, and a policy guessed before the SPA exists is a policy that gets
  loosened the first time it breaks. → **LAI-018** filed.
- Node 22 runs the TypeScript sources directly under `--experimental-strip-types`
  once imports carry `.ts` specifiers. Useful to know, rejected as a production
  path, noted in LAI-019 as a fallback.

**State**
LAI-002 moved to `.tasks/review/`, all nine criteria ticked, seven review notes.
Verified on a fresh clone of `builder-a` with no SPA build: install, typecheck,
lint, 50 tests, plus a real boot answering health, the SPA fallback, and JSON
404s on `/api`, `/mcp` and `/webhooks`, then `SIGTERM` → exit 0. Claiming next.

## 2026-08-24T03:37:00+05:30 — LAI-003

**What changed**
- `server/src/db/schema.ts` — all 14 tables of SPEC §4 in spec order, every §4.13 index, CHECK constraints on every closed vocabulary
- `server/src/db/enums.ts` — the §4 vocabularies as `const` tuples feeding types, CHECKs and runtime validation from one declaration
- `server/src/db/client.ts` — better-sqlite3 with WAL / foreign_keys / busy_timeout / synchronous, plus `readPragmas` so tests assert what actually resolved
- `server/src/db/migrations/0000_initial_schema.sql` — generated, committed, with two hand-appended append-only triggers
- `server/src/db/migrate.ts` — forward-only, applied at boot, folder resolved from `import.meta.url`
- `server/src/db/activity.ts` — append-only repository: `appendActivity`, `listActivity`, `readPayload`, nothing else
- `server/src/db/numbering.ts` — `BEGIN IMMEDIATE` task-number allocation
- `server/src/db/dependencies.ts` — self/duplicate/cycle rejection via a recursive CTE
- `server/src/db/ids.ts` — ULID generation
- `server/src/env.ts` — `dbPath` resolution: `LAIKA_DB_PATH` → `DATA_DIR` → `/data/laika.db` → `./data/laika.db` (non-production only)
- `server/src/index.ts` — opens the db and migrates before binding the port
- `server/src/shutdown.ts` — `onClosed` hook, used to close SQLite after the last request drains
- `server/drizzle.config.ts`, `server/package.json` (`db:generate`), `server/tsconfig.json`
- `server/test/db/{schema,activity,dependencies,numbering}.test.ts`, `numbering.worker.ts`, `test/helpers/db.ts` — 90 tests total

**Decisions**
- **Built all 14 tables, not the 11 the task lists.** AC4 requires every §4.13
  index, and five of them name `sprints`, `unlisted_work` and `meeting_reviews`;
  §4.5 also gives `tasks` a `sprint_id` FK. D-011 says the spec wins over a task
  file, so the task's older enumeration yields. Flagged at review rather than
  silently expanding scope.
- **`activity.project_id` and `actor_id` are nullable, against §4.8's literal
  text.** Four event types §4.8 itself defines have no project, and several have
  no user actor. Kept `org_id` NOT NULL. → LAI-025.
- **Append-only enforced twice**: a repository with no mutation path *and*
  database triggers. The repository alone protects only callers who use it; the
  triggers make "attempts fail" something a test can actually assert at runtime.
  This reverses the position I took in the LAI-001 log ("triggers would be
  invisible to Drizzle") — the task asks for a failing attempt, and absence of a
  method cannot produce one.
- **`ON DELETE SET NULL`, never cascade, for activity's project and task FKs.**
  An audit log that erases its own history when a project is deleted is not an
  audit log.
- **`BEGIN IMMEDIATE` for task numbering, and the number derived inside the
  inserting transaction.** Gapless because a rollback consumes nothing; a
  counter table would leak a number every time.
- **Tests run against a temp *file*, not `:memory:`.** WAL is unavailable for
  in-memory databases, so testing there would exercise a journal mode the server
  never uses — and the interesting concurrency behaviour is exactly the part WAL
  changes.
- **Concurrency tested with real worker threads.** better-sqlite3 is synchronous;
  nothing single-process can produce the interleaving that matters.
- **`db:generate` also runs Prettier over the migrations folder**, because
  drizzle-kit rewrites its metadata JSON unformatted every time.

**Discovered**
- **The concurrency test was verified to fail before trusting it.** I swapped
  `BEGIN IMMEDIATE` for a deferred `BEGIN` and re-ran: 11 of 80 inserts died with
  "database is locked". Worth stating plainly — a green concurrency test that has
  never been seen red is not evidence of anything. `busy_timeout` does not save a
  deferred transaction, because it retries the *write* with a number read before
  the lock existed.
- **Drizzle wraps driver errors** in "Failed to run the query …" and hangs the
  real one off `cause`. My first three CHECK-constraint tests asserted on
  `.message` and passed for the wrong reason — they would have passed on a typo
  in the SQL too. `expectSqliteError` in `test/helpers/db.ts` now walks the cause
  chain.
- **`pnpm format:fix` from my worktree rewrote `plugin/.claude-plugin/plugin.json`
  — Builder-B's file.** Caught in `git status` and reverted before committing,
  but only because I looked. The repo-wide *check* is right (PM's LAI-001 call);
  the repo-wide *write* crosses exactly the boundary D-008's worktrees exist to
  protect. → **LAI-026** filed.
- **Id collision, second time.** LAI-017/018/019 — which I filed during LAI-002 —
  collided with PM's UI shell tasks of the same numbers on `master` (`cc6fbed`).
  PM's are integrated and referenced elsewhere, so mine renumbered to
  LAI-022/023/024 and I updated every reference, including a comment in
  `error-handler.ts`. "Next unused number" has now failed twice in one day; it is
  a convention, not a lock.
- **Ownership changed under me**: `server/web/` is Builder-B's now (D-016), so
  LAI-007 is no longer mine. Nothing I have written touches it.
- better-sqlite3 v13 ships prebuilds for darwin/linux × arm64/x64/musl, so the
  ignored postinstall script is harmless and no root `onlyBuiltDependencies` entry
  is needed. Verified from a clean clone.

**State**
LAI-003 moved to `.tasks/review/`, all nine criteria ticked, nine review notes.
Verified on a fresh clone: install, typecheck, lint, 90 tests, plus a real boot
that created a nested database directory, applied migrations, and shut down
cleanly. Claiming next.

## 2026-08-24T03:40:00+05:30 — claim attempt (no task claimed)

**What changed**
- nothing; merged `master` (already up to date) and evaluated the backlog

**Decisions**
- **Did not claim LAI-026, the only `area: server` task whose dependencies are
  satisfied.** It is ready on paper and unbuildable in practice: every shape of
  the fix edits the root `package.json`, and LAI-001's scope exception was
  granted to LAI-001 alone. Claiming it would mean claiming, discovering the
  wall, and releasing it — churn that makes the board lie about what is being
  worked on. Left it in the backlog for PM to either widen (naming the file, per
  CLAUDE.md §1) or take directly.
- I could have half-built it by adding a server-scoped `format:fix` to
  `server/package.json`, which *is* mine. Rejected: the dangerous script is the
  root one, and leaving it in place while adding a safer sibling satisfies no
  acceptance criterion and makes the trap harder to see, not easier.

**Discovered**
- nothing new; everything below is a consequence of the graph, not a surprise

**State**
Blocked on PM review. `.tasks/review/` holds **LAI-002** and **LAI-003**, both
mine, both with every criterion ticked and detailed review notes.

The unblock order, if it helps PM sequence: accepting **LAI-002** releases
LAI-023, LAI-024 (p1) and half of LAI-005; accepting **LAI-003** releases LAI-004
(p1) and the other half of LAI-005. Accepting both makes LAI-004, LAI-005,
LAI-023 and LAI-024 all claimable at once, which is the first point today where
this session has more than one thing it could pick up.

Also awaiting a decision: **LAI-022** and **LAI-025** are `area: docs` spec
questions that LAI-006 and LAI-011 will walk into (the error-code vocabulary, and
`activity` nullability). Neither blocks me yet.

Working tree clean on `builder-a` at `6fa33c9`. Nothing pushed this session —
still no push credentials.

## 2026-08-24T04:01:00+05:30 — LAI-026

**What changed**
- `package.json` — `format:fix` now formats only what this worktree changed;
  `format` untouched (verified against `HEAD:package.json`)
- `CLAUDE.md` §5 — interim "don't run format:fix" guard replaced with the settled
  behaviour, per the task's AC4
- `server/test/tooling/format-fix.test.ts` — 12 tests running the *real* script
  in a throwaway git repo

**Decisions**
- **Read the script out of `package.json` in the test rather than restating it.**
  A test carrying its own copy of the pipeline keeps passing after someone edits
  the real one, which for a regression test is worse than having no test.
- **Excluded the lockfile via git pathspec, not Prettier's negative glob.**
  Prettier *errors* on an explicitly-listed file that a negative glob would
  ignore — so reusing `'!pnpm-lock.yaml'` from `format` would break `format:fix`
  after every `pnpm add`. Filtering before Prettier sees the path avoids it.
- **Restricted the file list to the extensions `format` checks, by pathspec.**
  Not `--ignore-unknown` alone: Prettier knows Markdown, so an explicit list
  formats it, and this repo deliberately does not.
- **Did not add a root `scripts/` file.** The pipeline fits in `package.json`,
  and the grant this task carries covers root config that already exists — a new
  root path was not named. Noted at review in case PM would have preferred one.
- Kept `--diff-filter=d` and `-z`: deletions have no file to format, and a real
  tracked path in this repo contains spaces.

**Discovered**
- **My first working draft rewrote 22 lines of `CLAUDE.md`** — repaginating PM's
  ownership and id-range tables — as a side effect of a nine-line edit, because
  Prettier has a Markdown parser and an explicit file list bypasses the `format`
  glob that excludes `.md`. Caught it in `git diff --stat` before committing,
  reverted, and constrained the pathspec. Exactly the class of damage this task
  exists to prevent, committed by the fix for it; there is now a test.
- **Prettier exits 2 on `--write` when an explicitly-named file is covered by a
  negative glob.** Not documented behaviour I knew; found by the lockfile test.
- **`pnpm format` is red repo-wide for everyone, and not because of this task.**
  `docs/design/support.js` contains template markup in a `.js` file
  (`<sc-if value="{{ p.hasDiff }}">`, line 806), so Prettier fails to *parse* it
  and the check exits 2 on every branch. Arrived with the prototype import
  (`8180ac0`), PM's area. → **LAI-100** filed. This matters more than it looks:
  a check that is permanently red stops being read, which is how the
  `plugin.json` warning survived three tasks.
- First task using the new per-session id ranges (D-017) — took LAI-100, the
  lowest free in Builder-A's `LAI-100`–`LAI-199`.

**State**
LAI-026 moved to `.tasks/review/`; all eight criteria ticked (four original, four
added by PM). 102 tests pass, lint and typecheck clean. `pnpm format` still
reports two files, both outside this task: `docs/design/support.js` (LAI-100, an
error) and `plugin/.claude-plugin/plugin.json` (LAI-014, a warning).

Loop is running on a 20-minute cron; claiming next.

## 2026-08-24T04:05:00+05:30 — LAI-026 (round 2)

**What changed**
- `.prettierignore` — new root file (PM extended the grant): `docs/design/` and `pnpm-lock.yaml`
- `package.json` — both scripts now pass `--ignore-path .gitignore --ignore-path .prettierignore`; the two ad-hoc lockfile exclusions removed
- `server/test/tooling/format-fix.test.ts` — two more tests (ignored dir left alone even when changed; unparseable file no longer fails the run)
- `.tasks/backlog/LAI-100-*.md` — marked superseded

**Decisions**
- **Edited the `format` script even though AC1 says "unchanged".** Adding
  `.prettierignore` alone is a no-op: **Prettier replaces its default ignore
  paths when `--ignore-path` is given**, and `format` already passed
  `--ignore-path .gitignore`, so the new file was silently inert. AC1's property
  — whole-repo, check-only — is intact, and PM's added criterion "`pnpm format`
  is green" cannot be met without it. Flagged at review rather than quietly
  reinterpreting the criterion.
- **Consolidated the lockfile exclusion into `.prettierignore`.** It had been
  excluded two different ways for a non-obvious reason: Prettier *errors* on an
  explicitly-listed file that a negative glob would ignore, but silently skips
  one an ignore-file excludes. One mechanism beats two.
- **Did not touch `eslint.config.js`.** PM said not to assume that grant. Checked
  instead: the type-aware config only matches `**/*.ts`, so `docs/design/*.js`
  was never linted and no ignore is needed.

**Discovered**
- **`--ignore-path` replaces rather than adds to Prettier's defaults.** This is
  the second Prettier behaviour today that fails silently rather than loudly —
  the first being the negative-glob error on explicit files. Both were found by
  running the thing, not by reading the flag list.
- **PM and I filed the same finding within a minute of each other.** I filed
  LAI-100 at 04:01 for the `docs/design/` breakage; PM's amendment folding it
  into LAI-026 merged at 04:02, and the merge conflicted on the task file. The
  loop's 20-minute cadence and PM's 30-minute one will keep producing this.
  Cheap to resolve, but worth knowing it is normal rather than a mistake.
- **A task can gain criteria after it reaches review.** LAI-026 went
  review → in-progress → review in one sitting. Untick anything a blanket
  `sed 's/- \[ \]/- [x]/'` swept up from the new section — I nearly shipped PM's
  three new criteria pre-ticked.

**State**
LAI-026 in `.tasks/review/` with all 11 criteria ticked. `pnpm format` is **green
repo-wide for the first time today**; lint, typecheck and 104 tests all pass.
`docs/design/` byte-identical to `master`. LAI-100 superseded, needs closing
unworked. Claiming next.

## 2026-08-24T04:10:00+05:30 — LAI-004

**What changed**
- `server/src/policy/actions.ts` — the closed `Action` union, one entry per §3.1/§3.2 cell, plus `READ_ACTIONS`
- `server/src/policy/can.ts` — `can`, `assertCan`, `effectiveProjectRole`, `effectiveTokenScope`, `forcedTokenScope`, `projectRoleOnJoin`
- `server/test/policy/matrix.test.ts` — both matrices transcribed cell for cell
- `server/test/policy/can.test.ts` — structural rules, token narrowing, deny-by-default, `assertCan`

**Decisions**
- **Followed the spec where AC8 contradicts it.** The task asks for a test that
  "Admin cannot edit org settings"; §3.1 now grants it. D-011 says the spec wins,
  so `can()` allows it and the test asserts the allow with the reason inline.
  Flagged at review rather than silently inverting a criterion — this is exactly
  the drift D-011 was written about, arriving in a task file this time.
- **Listed the reads, not the writes.** `READ_ACTIONS` has four entries;
  everything else is a write by default. A new action added carelessly is then
  denied to read-only tokens rather than permitted — wrong in the safe direction.
- **Split spec rows that cover two operations.** "List / revoke anyone's token"
  became `token.list_any` and `token.revoke_any`, because one is a read and one
  is a write and a `read_only` token must be able to do the first and not the
  second. The row is a formatting choice in the document, not a claim they are
  the same operation.
- **Capped the org-viewer project role inside `can()` rather than trusting the
  membership row.** A row saying `viewer` + `lead` is either corruption or an
  escalation attempt; either way the authority should not believe it.
- **`assertNever` in every switch.** Exhaustiveness becomes a compile error, so
  adding an action without a policy cannot ship.
- **`can()` takes an already-resolved `projectRole`** — purity is what makes the
  matrix testable as a table, at the cost of putting the lookup on the caller.
  Noted for LAI-010/011 that they will want a `resolveActor` helper so no handler
  forgets.

**Discovered**
- **A second task-file/spec disagreement, same shape as LAI-015's.** AC8's
  "Admin cannot edit org settings" is a restatement of an older matrix. D-011
  predicted this precisely: task files that restate spec content drift from it.
  Not filing a task — it is one criterion, resolved spec-wins and flagged at
  review, and PM can correct the file when accepting.
- `pnpm format:fix` from LAI-026 behaved correctly on its first real use: it
  touched only the four files I had changed and left the rest of the repo alone.

**State**
LAI-004 in `.tasks/review/`, all nine criteria ticked, seven review notes.
165 tests pass; lint, typecheck and `pnpm format` clean. Claiming next.

## 2026-08-24T04:30:00+05:30 — LAI-005

**What changed**
- `server/src/auth/auth.ts` — better-auth on the Drizzle adapter, `users` remapped as its user table, invite gate, cookie attributes
- `server/src/auth/telemetry.ts` — `scrubTelemetryEnv()`
- `server/src/auth/invites.ts` — hashed-token lookup with expiry/acceptance/email-target in the query
- `server/src/auth/resolve-actor.ts` — `resolveActor`, `loadActor`, `withProject`
- `server/src/auth/avatar.ts` — deterministic palette
- `server/src/http/middleware/auth.ts` — the LAI-002 stub becomes real; `anonymousAuth` for auth-less apps
- `server/src/http/routes/me.ts` — `GET /api/v1/me`
- `server/src/http/context.ts` — `actor` on the Hono env
- `server/src/db/schema.ts` — `sessions`, `accounts`, `verifications`; `users` gains `email_verified`/`image`, Date-typed timestamps
- `server/src/db/migrations/0001_better_auth_tables.sql` — generated, then hand-corrected
- `server/src/env.ts` — `SERVER_SECRET`, `PUBLIC_URL`, `secureCookies`
- `server/src/index.ts`, `src/app.ts` — auth wired into boot and the middleware chain
- `server/test/auth/{flow,telemetry,actor}.test.ts`, `test/helpers/auth.ts` — 25 new tests

**Decisions**
- **Remapped better-auth's `user` model onto our `users` table** instead of
  letting it create its own. Two user tables would need syncing that nobody
  would remember; §4.1 already treats the user row as ours and credentials as
  better-auth's. Cost: `users` gained two columns and its timestamps became
  Date-typed in TypeScript. Storage is unchanged — still integer unix-ms.
- **Enforced invite-only in an endpoint hook, not a database hook.** Forced, not
  preferred: better-auth strips unknown body fields before database hooks run, so
  `inviteToken` was gone by then. The stripping is also what makes the
  privilege-smuggling test pass, so it is worth keeping.
- **Deleted the telemetry env vars rather than trusting the option** — see the
  discovery below. §13.4 says "absent, not opt-out", and the option alone cannot
  deliver that.
- **`SERVER_SECRET` required in production, ≥32 chars, `<redacted>` in errors.**
  A dev fallback outside production, because refusing to start is the right
  answer for a deployment and the wrong one for `pnpm dev`.
- **Tested the telemetry scrub, not "no beacon fired".** better-auth skips
  telemetry under `NODE_ENV=test`, so the obvious test would pass whether or not
  the guard existed — a green test that cannot fail is worse than none.
- **`resolveActor` re-reads the role from the database** every request rather than
  trusting the session, so a demotion takes effect immediately.

**Discovered**
- **`telemetry: { enabled: false }` does not disable better-auth telemetry.** The
  gate is `getBooleanEnvVar("BETTER_AUTH_TELEMETRY", false) || options.telemetry.enabled`
  — an OR, so the environment variable wins over the explicit `false`. On any
  host where that variable happens to be set, Laika would phone home while its
  own config said it does not. This is the single most spec-relevant thing I
  found today (§13.4 is emphatic and says such tasks are rejected at review), and
  it is not something reading the documentation would have surfaced.
- **drizzle-kit generates broken SQLite table rebuilds when adding NOT NULL
  columns.** It SELECTs the *new* column list from the *old* table:
  `no such column: email_verified`. Every future migration that adds a NOT NULL
  column to an existing table needs the same hand-correction. Worth remembering
  before assuming a generated migration is correct because it generated cleanly.
- **My own bug, caught by a test rather than by review:** the avatar colour was
  seeded from `user.id` inside `create.before`, where better-auth has not assigned
  an id yet. It surfaced as better-auth's generic "Failed to create user" 422 with
  the real `TypeError` swallowed — I had to turn on the library's debug logger to
  see it. Seeded from the email instead, and `avatarColorFor` no longer throws on
  a missing seed.
- `better-auth` declares `better-sqlite3@^12`; we run 13.0.3. Harmless with the
  Drizzle adapter, but pnpm warns on every install. Flagged at review rather than
  pinned, since downgrading would undo what LAI-003 tested.
- **LAI-026's `format:fix` cannot fix a file after you commit it** — the diff is
  against `HEAD`, so a committed-but-unformatted file has no fixing command.
  → **LAI-101** filed (p3, has a documented workaround).

**State**
LAI-005 in `.tasks/review/`, all eight criteria ticked, seven review notes.
198 tests; lint, typecheck and `pnpm format` clean. Verified over a real socket:
signup, sign-in, cookie attributes, `/me`, CSRF rejection from a foreign origin,
sign-out, and `/me` → 401 afterwards. Claiming next.

## 2026-08-24T04:32:00+05:30 — LAI-024

**What changed**
- `server/tsconfig.build.json` — emit config: `noEmit:false`, `rootDir: src`, `outDir: dist`, tests excluded
- `server/package.json` — `build`, `build:assets`, `start`
- `server/src/paths.ts` — `CODE_ROOT`; shipped assets anchored to the module, `public/` still to the package root
- `server/test/tooling/build.test.ts` — builds for real, runs `node dist/index.js` for real

**Decisions**
- **Two different path anchors, deliberately.** Assets that ship with the code
  (`static/fallback.html`, the `.sql` migrations) resolve from `import.meta.url`
  so they follow the build into `dist/`; `public/` resolves from the package root
  because it sits beside `src/` and `dist/` rather than inside either. Anchoring
  everything one way breaks the other.
- **Copied assets in `build:assets` rather than switching to a bundler.** A
  bundler is a new dependency and this task allows none; two `cp -R` lines are
  also easier to review than a config that inlines files invisibly.
- **The build test spawns a real process instead of asserting on file listings.**
  A listing test would have passed with a `dist/` that cannot boot.

**Discovered**
- **`tsc` copies nothing but TypeScript, and both omissions are production-only.**
  Missing `fallback.html` and missing migrations both pass every local test —
  because `src/` is still on disk — and fail only in a container shipping `dist/`
  alone. That asymmetry is the whole reason this task existed, and it is worth
  remembering for any future non-`.ts` asset.
- **Verified the test bites**: dropping the `src/static` copy failed both the
  artefact assertion and the running server. Same discipline as the LAI-003
  concurrency test; both times it was worth the two minutes.
- The `moduleResolution: bundler` risk I flagged in the LAI-001 log landed here
  and cost nothing, because `rewriteRelativeImportExtensions` was already set in
  LAI-002. It now has an explicit assertion so a later tsconfig edit cannot
  silently produce output Node refuses to load.
- **SPEC §11.7 says `SERVER_SECRET` is "auto-generated to `$DATA_DIR/secret` on
  first boot"; `server/src/env.ts` (mine, LAI-005) and `docker/entrypoint.sh`
  (Builder-B, LAI-008) both refuse to start without it, with the same 32-character
  minimum.** Two sessions reached the same answer independently and both diverged
  from the document. → **LAI-102** filed for PM; neither implementation is wrong
  on its own, but the spec should not disagree with both.

**State**
LAI-024 in `.tasks/review/`, all five criteria ticked, six review notes.
204 tests; lint, typecheck, format clean. Claiming next.

## 2026-08-24T04:41:00+05:30 — LAI-006

**What changed**
- `server/src/http/pagination.ts` — opaque `(sort_key, id)` cursors, `parseLimit`, `buildPage`
- `server/src/http/updated-since.ts` — watermark parsing, tombstones, inclusive comparison
- `server/src/http/validation.ts` — zod helpers; `strictObject` rejects unknown keys
- `server/src/http/rate-limit.ts` — token bucket with continuous refill and pruning
- `server/src/http/idempotency.ts` + `middleware/idempotency.ts` — SQLite-backed replay
- `server/src/http/middleware/rate-limit.ts` — per-actor limits, `Retry-After`
- `server/src/http/middleware/logger.ts` — real `actor_id` / `actor_kind` / `token_id`
- `server/src/db/schema.ts` + `migrations/0002_idempotency_keys.sql`
- `server/package.json` — `clean` step in `build`
- `server/test/http/{pagination,cursor-walk,conventions,idempotency}.test.ts` — 46 new tests

**Decisions**
- **Followed SPEC §6.3's rate limits over the task's.** AC6 says "600 general,
  60 writes, 30 heartbeats"; the spec says "120 per token, 600 per session, 30
  heartbeats" — a different axis entirely. D-011, spec wins, flagged at review.
  Third time this session a task file has restated spec content and drifted.
- **Token bucket over fixed window**, so a caller cannot spend two full
  allowances across a window boundary.
- **`updated_since` is inclusive.** A row returned twice is harmless; a row lost
  because it changed in the watermark millisecond is invisible data loss.
- **Idempotency stores only 2xx.** Pinning a 500 for 24 hours would turn one bad
  moment into a day-long outage for that caller.
- **Kept `idempotency_keys` out of SPEC §4.** It is transport bookkeeping, not
  product data. Raised at review rather than assumed.
- **`prune()` on the limiter.** Without it the bucket map grows once per distinct
  actor forever — a slow leak that only manifests in a long-running container,
  which is exactly Laika's deployment.

**Discovered**
- **`cp -R src/x dist/x` copies *into* an existing directory.** My LAI-024 build
  was not idempotent: the second build produced `dist/db/migrations/migrations`
  and the newest migration was missing from where the server looks. Caught by
  LAI-024's own build test when migration 0002 appeared — the test earning its
  keep two tasks after it was written. `build` now cleans `dist` first.
- **`eslint --fix` removed a type assertion I needed.** `no-unnecessary-type-assertion`
  stripped `as { title?: string }` from `await c.req.json()`, which then tripped
  `no-unsafe-member-access` — the autofix traded one error for two. Resolved with
  Hono's `c.req.json<T>()` generic, which needs no assertion at all. Worth
  knowing that `lint:fix` output still needs reading.
- **Anonymous requests all share one rate-limit bucket.** Fine for v1 (only
  `/health` and the SPA are unauthenticated) but it is a decision, not an
  oversight — a per-IP bucket needs a trusted-proxy policy. Noted at review; not
  filed, because nothing unauthenticated and expensive exists yet to protect.

**State**
LAI-006 in `.tasks/review/`, all eight criteria ticked, nine review notes.
250 tests; lint, typecheck, format clean. Claiming next.

## 2026-08-24T04:44:00+05:30 — LAI-028

**What changed**
- `server/test/tooling/build.test.ts` — four cases pinning build idempotency
- (the fix itself, `clean` before `build`, landed under LAI-006)

**Decisions**
- **Claimed this ahead of LAI-023, which is the lower id at the same priority.**
  The fix was already on my branch from LAI-006, so the next session to claim it
  would have found the bug gone and no test, and had to reconstruct why.
  Stranding a task in that state seemed worse than deviating from id order.
  Flagged at review so PM can disagree cheaply.
- **Four cases rather than one.** The identical-tree comparison states the
  property; the nesting check pins the specific shape; the journal count catches
  a duplicate that is currently inert but would not stay that way if migration
  resolution changed; the stale-file case covers the half of `clean` the other
  three do not touch.

**Discovered**
- Nothing new about the bug — LAI-006's log already records finding it. What is
  worth recording is that **LAI-024's build test caught a regression two tasks
  after it was written**, in a code path nobody was looking at, which is the
  entire argument for having written it against a real build rather than a file
  listing.
- **The build tests now dominate the suite's wall time** — four full builds,
  about 13 of 25 seconds. Acceptable for a production-only failure mode, and
  noted at review as the first thing to move behind a flag if it starts to hurt.

**State**
LAI-028 in `.tasks/review/`, all four criteria ticked. 254 tests; lint, typecheck
and format clean. Verified the new tests fail without the fix. Claiming next.

## 2026-08-24T04:45:00+05:30 — LAI-028 (closed by PM concurrently)

**What changed**
- resolved a merge conflict: PM moved LAI-028 to `.tasks/done/` on `master` while
  I moved it to `.tasks/review/` on `builder-a`. Took PM's version — only PM
  moves anything to `done/` — and dropped my review notes with it.

**Decisions**
- **Accepted PM's closure rather than reasserting my move.** The task file is
  theirs to place; my review notes are the only thing lost, and the four tests
  they described are committed either way (`fdeb913`).

**Discovered**
- **PM's closure corrects the severity call, and the correction matters more than
  the bug.** They filed LAI-028 as cosmetic on the grounds that the nested copies
  were "provably inert" — true for the tree as it stood, and false the moment
  LAI-006 added migration `0002`, which then landed only in the nested directory.
  A fresh database would have booted missing a table. Their note says it better
  than I would: "inert given today's files" is not "inert".
- We both worked the same task from opposite ends within one interval — PM
  verifying and closing, me adding the tests its AC4 asked for. Neither was
  wasted, but a claim commit landing on `master` faster than PM's review would
  have avoided the collision entirely. Third coordination collision today
  (LAI-013 ids, LAI-017/018/019 ids, this one); all cheap, all the same shape:
  two sessions acting on the same file between merges.

**State**
Conflict resolved, tree clean, 254 tests green. LAI-006 also accepted this round.
Claiming next.

## 2026-08-24T04:47:00+05:30 — LAI-023

**What changed**
- `server/src/http/middleware/security-headers.ts` — CSP, nosniff, frame options, referrer policy, cross-origin isolation, conditional HSTS
- `server/src/app.ts` — mounted immediately after `cors` in the §11.2 chain
- `server/test/http/security-headers.test.ts` — 12 tests

**Decisions**
- **Verified the CSP against the document actually served, since the one AC3
  names does not exist.** The test reads `fallback.html` and asserts it contains
  no `<script>` and no inline handlers, so `script-src 'self'` provably blocks
  nothing shipped. Asserting the policy string alone would have been a test that
  cannot fail.
- **`style-src 'unsafe-inline'`, `script-src` strict.** §13.1 says "no inline
  script"; the fallback page needs inline CSS to render legibly with no build.
  Both halves are asserted, so the allowance can be dropped the day that document
  stops needing it.
- **HSTS only over HTTPS**, honouring `X-Forwarded-Proto` because TLS terminates
  at Caddy. On localhost the header would pin a developer's browser to HTTPS for
  two years — a two-line guard against a genuinely annoying mistake.
- **Headers set after `next()`** so they cover error responses; the 500 path has
  its own test.
- **Hand-written rather than `hono/secure-headers`.** Two headers are conditional
  and the CSP is a policy this repo owns; a config object that mostly overrides
  the library's defaults reads worse against §13.1 than the list does.

**Discovered**
- **AC3 was unmeetable as written and that is now someone else's task.** It asks
  for verification against the built SPA; LAI-007 has not happened and, under
  D-016, `server/web/` is Builder-B's. → **LAI-103** filed (`area: web`) to
  re-verify against Vite's real output, including the note that dev and
  production differ — Vite injects inline scripts for HMR, so `pnpm dev` may need
  a visibly-marked relaxed policy.
- Nothing surprising in the implementation; this is the first task today that
  went in without a bug turning up.

**State**
LAI-023 in `.tasks/review/`, all seven criteria ticked, six review notes.
266 tests; lint, typecheck, format clean. Verified over a real socket that every
header lands on the SPA document and that HSTS appears only behind a proxy
claiming HTTPS. Claiming next.

## 2026-08-24T04:52:00+05:30 — LAI-030

**What changed**
- `server/src/http/middleware/rate-limit.ts` — `isLimited()`; the API surface is limited, `GET /api/v1/health` and static/SPA are not
- `server/test/http/rate-limit-scope.test.ts` — 8 tests
- `server/test/http/conventions.test.ts` — two cases moved off `/api/v1/health`, which is now exempt

**Decisions**
- **No per-IP buckets.** Behind a proxy every request carries the proxy's
  address; trusting `X-Forwarded-For` without a trusted-hop configuration lets a
  client mint a new identity per request and defeat the limiter entirely — worse
  than one shared bucket. Pinned by a test that two different `X-Forwarded-For`
  values share a budget, so the behaviour is deliberate rather than incidental.
- **Exempted the liveness probe and static assets.** §6.3 is the REST API's
  contract; a page load is a dozen files, and a probe that can be rate-limited is
  not a probe.

**Discovered**
- **The shared bucket was a live availability bug, not a theoretical one.**
  `docker/Dockerfile` runs `HEALTHCHECK --interval=30s --retries=3` against
  `/api/v1/health`, and that request was spending from the anonymous bucket. Enough
  anonymous traffic → `429` on the probe → three failures → **the orchestrator
  restarts a healthy server**, dropping in-flight requests, and the load arrives
  again. Self-inflicted outage under exactly the conditions you least want one.
  I could not have seen this in LAI-006 where I first raised the question — it
  needed Builder-B's Dockerfile sitting next to my limiter, and neither of us was
  looking at both. Worth remembering that the interesting bugs this week have all
  been at the seams between two sessions' work.
- p3 was right for the question as filed and wrong for what it concealed. Not a
  criticism of the triage — the severity was not visible from the task text.

**State**
LAI-030 in `.tasks/review/`, all three criteria ticked, five review notes.
274 tests; lint, typecheck, format clean. **LAI-104** filed for the §6.3 sentence
AC3 asks for, since that is `docs/`.

Remaining server work is now **structurally blocked**: LAI-009 → LAI-007 (web,
Builder-B) → LAI-019 → LAI-018 → LAI-017; and LAI-010/011 sit behind LAI-009. The
only unblocked server task left is LAI-101 (p3). Claiming that next; after it,
Builder-A has nothing claimable until the UI track reaches LAI-007.

## 2026-08-24T04:57:00+05:30 — LAI-204

**What changed**
- `server/src/env.ts` — `LAIKA_PUBLIC_DIR`, defaulting to `undefined`
- `server/src/index.ts` — passes it through to `createApp`
- `server/test/tooling/build.test.ts` — `withBuiltServer()` helper; the one
  spawn-and-assert case became three, each owning its public directory
- `server/test/env.test.ts` — two cases for the new variable

**Decisions**
- **Added an env var rather than moving the developer's `public/` aside.**
  Shuffling a directory the test does not own is destructive if the test crashes
  and racy if anything else is running. An override also has value outside the
  test — a deployment can serve the SPA from elsewhere — for four lines.
- **Split the assertion into two named cases.** "Serves the fallback when no SPA
  is built" and "serves the SPA when one is" are different behaviours; collapsing
  them meant the test could not tell which one it was looking at and asserted
  whichever the machine happened to be in.

**Discovered**
- **Builder-B found a bug in my test that I could not have seen from inside
  `server/`.** It passed on a clean clone and failed after `pnpm build` — same
  commit, two answers, decided by a gitignored directory. It would have failed
  for anyone who ran `pnpm build` since LAI-024; the SPA landing just made it
  deterministic. Their framing — "a test whose outcome depends on untracked local
  state" — is what made the fix obvious; "the assertion is wrong" would have led
  me to change the matched string and leave the real problem in place.
- It is the **same failure mode LAI-016 was filed to prevent**, arriving from the
  opposite direction: LAI-016 stopped `server/public/` being committed, and this
  was a test quietly depending on whether it existed. Two tasks, one underlying
  mistake — assuming something about a directory nobody owns.
- Every genuinely interesting bug today has been at a seam between two sessions:
  the tsx signal problem (server/docker), the health-probe rate limit
  (server/docker), and now this (server/web). Work inside one area has been
  comparatively uneventful.
- `pnpm install` was needed to pick up `server/web` as a new workspace member
  before lint and typecheck would run — worth knowing after any merge that adds a
  package.

**State**
LAI-204 in `.tasks/review/`, all four criteria ticked. 278 tests pass **both**
with and without `server/public/` present — run both ways, since that is the
point of the task. Lint, typecheck and format clean.

## 2026-08-24T04:59:00+05:30 — LAI-031

**What changed**
- `.gitignore` — `.playwright-mcp/`, `playwright-report/`, `test-results/`, with a comment explaining why they are named individually

**Decisions**
- **Named the directories rather than using a `.*` rule.** A blanket pattern
  would hide `.prettierrc`, `.npmrc`, `.nvmrc`, `.prettierignore` and `.gitignore`
  itself. Verified all five are still tracked after the change rather than
  assuming.
- **Three entries, not one.** The other two are Playwright's default output
  directories — same tool, same class of artifact, and they would cause the
  identical failure the first time someone runs a test run instead of an MCP
  session. Stopped there rather than speculating about tools nobody uses.

**Discovered**
- **Third time the repo-wide format gate has gone red for a non-source reason**
  (`docs/design/` in LAI-026, `.playwright-mcp/` here). The shape is always the
  same: something writes into the working tree and a `**/*` glob finds it. Noted
  at review that if it recurs the answer is probably to invert the `format` glob
  into an allowlist of source directories — but two data points and a working fix
  is not yet a reason to redesign, so I did not.

**State**
LAI-031 in `.tasks/review/`, all three criteria ticked. 278 tests; lint,
typecheck and format clean. Reproduced the failure before fixing it and confirmed
the fix with `git check-ignore -v`.

## 2026-08-24T05:00:00+05:30 — claim attempt (no task claimed)

**What changed**
- nothing; merged `master` and evaluated the backlog

**Decisions**
- **Did not claim LAI-101**, the only `area: server` task whose dependencies are
  satisfied. Every one of its acceptance criteria requires editing the root
  `package.json`, and root config has no standing owner (D-017) — PM grants it
  per task by name, and this task has no grant. I wrote that constraint into the
  task myself when filing it during LAI-005, so it is not a surprise; claiming it
  now would mean claiming and immediately releasing. Same shape as LAI-026 this
  morning, which PM unblocked by adding the grant.

**Discovered**
- nothing new

**State**
Blocked, and this time structurally rather than by review latency.

- **LAI-101** (p3) — ready except for a root-`package.json` grant only PM can give.
- **LAI-009 → LAI-010 → LAI-011** — the rest of the server track, all of it behind
  **LAI-007**, which is `area: web` and therefore Builder-B's under D-016.
  LAI-007 is itself behind LAI-019 → LAI-018 → LAI-017 on the UI track.

So Builder-A has nothing to build until either PM adds the LAI-101 grant, or the
UI track reaches LAI-007. Everything I have shipped today is either in
`.tasks/done/` or in `.tasks/review/` awaiting PM.

Tree clean, 278 tests green, lint/typecheck/format all clean. The 20-minute loop
stays armed and will re-check; no manual restart needed.

## 2026-08-24T05:30:00+05:30 — LAI-032

**What changed**
- `server/src/env.ts` — `LAIKA_SECRET` / `LAIKA_DATA_DIR` / `LAIKA_PUBLIC_URL`; dev secret fallback removed; `LAIKA_PUBLIC_URL` required in production, defaults to `http://localhost:$PORT` otherwise
- `server/src/db/schema.ts` — comment reference updated
- `server/test/env.test.ts` — rewrote the secret and public-URL blocks; 29 cases
- `server/test/tooling/build.test.ts` — spawned server passes the new names

**Decisions**
- **Removed the development secret fallback entirely**, not just renamed it.
  §11.7's `LAIKA_SECRET` row says "required — no default" with no environment
  qualifier, while the `LAIKA_PUBLIC_URL` row explicitly distinguishes dev from
  production. Reading that asymmetry as deliberate is the only way both rows make
  sense. Flagged the DX cost at review — `pnpm dev` now needs the variable set —
  with an offer to revert if PM disagrees.
- **`LAIKA_PUBLIC_URL` defaults from the parsed port**, not a hardcoded 3000.
  §11.7 writes the default as `http://localhost:$PORT`, and a constant would have
  produced a default silently disagreeing with the port actually bound.
- **Two negative tests asserting the old names are dead.** AC1 asked for no
  fallback; the only way to know a rename finished is to assert the old spelling
  fails.

**Discovered**
- **`LAIKA_DISABLE_INVITE_ONLY` is documented in §11.7 and read by nothing.**
  §11.7 says everything in the table must be read, and D-018 named exactly this
  drift as the reason for the rule — it fixed the read-but-undocumented half and
  left this one. An operator setting it today gets silence.
  → **LAI-105** filed, arguing *against* reflexively implementing it: it is an
  authentication-bypass switch, D-004 makes invite-only the default posture, and
  first-run setup already handles the case it would exist for. Removing the row
  may be the better answer.
- Renaming the env surface made the whole list visible in one place for the first
  time, which is how the unread variable surfaced. Worth remembering that a
  mechanical rename is a decent excuse to audit the thing being renamed.

**State**
LAI-032 in `.tasks/review/`, all six criteria ticked, five review notes.
283 tests; lint, typecheck, format clean. Verified five boot behaviours against
the real process, including that `SERVER_SECRET` is no longer honoured.

LAI-033 (docker, Builder-B) is the other half — the container still passes the
old names, and will now exit loudly rather than start misconfigured, which is the
ordering the task asked for.

## 2026-08-24T05:52:00+05:30 — LAI-037

**What changed**
- `server/src/services/me.ts` — the worked example; takes an `Actor`, throws `ApiError`, knows no HTTP
- `server/src/http/routes/me.ts` — transport only, three lines of body
- `server/src/errors.ts` — `ApiError` moved out of `http/` (13 files updated)
- `eslint.config.js` — five `no-restricted-imports` blocks encoding CONVENTIONS §2
- `server/test/services/me.test.ts` — 5 tests
- `server/test/tooling/toolchain.test.ts` — moved from `test/`

**Decisions**
- **Moved `ApiError` to `src/errors.ts`.** `policy/can.ts` imported it from
  `http/`, so "policy/ stays pure" was either a wrong rule or wrong code. The
  error envelope belongs to no layer — policy, services, http and later MCP all
  throw it — so making it layer-neutral lets the table be encoded literally
  instead of with an exception.
- **Used `@typescript-eslint/no-restricted-imports` for `allowTypeImports`.**
  `policy/` naming `db/`'s enum unions as types emits nothing at runtime and
  cannot create a cycle; that is not the coupling the table forbids. The
  alternative — moving the enums out of `db/` — is a larger change than this task
  should make. Not a new dependency.
- **The service takes the already-resolved actor.** The auth middleware did that
  query; loading again would be two reads of one row per request, and passing it
  in is what keeps the service testable with no database.
- **Added a `forbidden` for deactivated accounts**, which the old route did not
  have — it returned `is_active` as data. Deactivation that does not invalidate a
  live session is deactivation that waits for the user to cooperate. Small
  behaviour change, flagged at review, tested.

**Discovered**
- **My first attempt to prove the rules reported all six as not firing — and the
  prover was broken, not the rules.** The harness ran `pnpm lint` after
  `cd server`, where no `lint` script exists, so the grep matched nothing and
  every probe looked like a silent rule. Running ESLint on the probe file
  directly showed all six firing correctly. This is exactly the trap AC4 is
  written against: a broken prover and a broken rule produce the same summary
  line, and only reading the raw output distinguishes them. Read the output, not
  the tally.
- `allowTypeImports` is a **per-pattern** option in
  `@typescript-eslint/no-restricted-imports`, not a top-level one. Putting it at
  the top level fails config validation with a schema error rather than being
  ignored — a good failure, but the message does not say where it belongs.

**State**
LAI-037 in `.tasks/review/`, all six criteria ticked, six review notes.
288 tests; format, lint and typecheck clean. Every rule in the CONVENTIONS §2
table proved able to fail, and type-only imports proved still allowed.

## 2026-08-24T06:14:00+05:30 — LAI-205

**What changed**
- `server/src/http/security-headers.ts` — new pure half: `extractStyleHashes`, `buildContentSecurityPolicy`, `isHttps`, `HSTS_VALUE`
- `server/src/http/middleware/security-headers.ts` — now a factory taking the prebuilt policy
- `server/src/app.ts` — hashes the fallback document once at boot
- `server/test/http/security-headers.test.ts` — 19 tests, 7 new for the split

**Decisions**
- **Split `style-src` rather than tightening it**, per the task's revised
  recommendation: `style-src-elem` hashed, `style-src-attr 'unsafe-inline'`,
  plain `style-src` retained for pre-CSP3 engines.
- **Kept the hash OUT of the plain `style-src`.** CSP2+ ignores `'unsafe-inline'`
  in any list that also contains a hash, so the obvious belt-and-braces move
  would have silently broken inline style attributes on exactly the old engines
  that line exists for. Has its own test, because the mistake is invisible on a
  modern browser.
- **Computed the hash at boot** rather than pasting the literal, so it cannot
  drift from the file.
- **Paired-module split** (CONVENTIONS §3), policy built once at startup.

**Discovered**
- **Drove the cached headless Chromium over CDP with Node's built-in
  `WebSocket`** — the shared Playwright MCP browser was in use by Builder-B and
  refuses concurrent instances. Roughly 40 lines in the scratchpad, no
  dependency, and it gave a real browser for computed styles and console errors.
  Worth remembering: a second session holding the MCP browser is not a dead end.
- **The negative control was the whole value of the exercise.** With a
  deliberately wrong hash the page fell back to Times New Roman on a transparent
  background — while the `<style>` tag remained in the DOM. Asserting the tag's
  presence would have "passed" against a completely broken policy, which is
  precisely what AC3 warned about and why it asked for a computed style.
- **Could not test Gecko or WebKit** — only Chromium is on this machine. Verified
  both *code paths* in Chromium instead (split, and legacy emulated by serving
  only the plain directive) and said so plainly at review rather than implying
  coverage I do not have. The structural argument stands on its own: both
  branches carry `'unsafe-inline'` for attributes, so no engine can block them.
- PM's correction here was load-bearing. My own LAI-023 comment said to tighten
  "to hashes or `'self'`", which would have broken avatar colours on any engine
  enforcing the attribute case — and it would have presented as a UI bug, not a
  CSP one. Second time today a review caught something invisible from inside
  `server/` (LAI-204 was the first).

**State**
LAI-205 in `.tasks/review/`, all five criteria ticked, five review notes.
295 tests; format, lint, typecheck clean. Six browser measurements recorded in
the review notes, including the control.

## 2026-08-24T06:30:00+05:30 — LAI-038

**What changed**
- `server/test/tooling/structure.test.ts` — 8 checks over `server/src`, plus a 28-entry exemption list with reasons
- `server/test/errors.test.ts` — moved from `test/http/` (LAI-037 moved the module)
- `server/test/auth/resolve-actor.test.ts` — renamed from `actor.test.ts`
- `server/test/http/routes/health.test.ts` — moved to match the route's depth

**Decisions**
- **The reverse check asserts directory correspondence, not file.** §4 names no
  home for a test spanning several modules, and seven legitimately do. Forcing
  those to mirror one file would mean renaming good tests after whichever module
  they most resemble — worse than the drift it prevents. Directory-level still
  catches a stray file and needs no exemption list.
- **Two guards on the exemption list itself**: every entry must name a file that
  exists, and every reason must be more than a token. Without those the list rots
  into a place where deleted files linger and new ones inherit an exemption that
  was never about them.
- **Only moved test files whose misplacement I had caused.** The task warned
  against adopting this via a mass rename, and I did not — `server/src` was
  already fully kebab-case, so naming needed no changes at all.
- **Kept the `.tsx` rules live despite `server/` having no components.** LAI-039
  extends this file for `server/web/`; having the rule proven in advance means the
  first component is checked rather than setting a precedent.

**Discovered**
- **The exemption list is 28 of 42 modules**, which is worth saying plainly
  rather than presenting the rule as fully enforced today. Its value now is
  forward-looking: a *new* module cannot arrive untested by accident. Most
  entries are transport plumbing covered by an integration test, and the reason
  column names which — a middleware asserted in isolation proves it compiles, not
  that it is wired into the chain correctly.
- **CONVENTIONS §4 has a gap**: it does not say where a cross-cutting integration
  test belongs. I interpreted it as "a directory that exists under `src/`" and
  said so at review rather than filing a task, since it is a one-line
  clarification PM can fold in when accepting.
- Proving eight rules took about as long as writing them, and two of the messages
  were wrong on the first attempt — the barrel probe initially matched nothing
  because my first `looksLikeBarrel` treated a leading comment line as a
  statement. Worth the time; a structural test nobody has seen fail is decoration.

**State**
LAI-038 in `.tasks/review/`, all five criteria ticked, six review notes.
303 tests; format, lint, typecheck clean. All eight rules proven able to fail,
with the exact message recorded for each.

## 2026-08-24T06:52:00+05:30 — LAI-042

**What changed**
- `server/src/errors.ts` — `payload_too_large` (413) and `method_not_allowed` (405); ten codes
- `server/src/http/error-handler.ts` — maps both; per-code default message for message-less framework exceptions
- `server/src/http/allowed-methods.ts` — asks the router which methods a path has
- `server/src/app.ts` — `notFound` distinguishes 405 from 404 and sets `Allow`
- `server/test/http/allowed-methods.test.ts`, plus cases in `errors.test.ts`, `conventions.test.ts`, `middleware.test.ts`

**Decisions**
- **Implemented 405 rather than reporting it impossible.** The task allowed
  saying "Hono cannot" — and Hono genuinely returns 404 for a method mismatch —
  but `app.router.match` distinguishes them cleanly: chain middleware matches
  every method equally, so a longer handler chain means a real route. That is the
  router answering, not a handler guessing, which is the line the task drew.
- **Compared handler-chain depth across methods, not against a constant.** The
  number of `app.use('*')` middleware varies; comparing within one path makes it
  irrelevant. Tested with a bare app and a six-middleware app giving identical
  answers.
- **Added default messages for framework exceptions.** Hono's `bodyLimit` throws
  a bare 413, so the envelope was shipping `"message": ""`.
- **Parsed §6.3's table out of `docs/SPEC.md`** for AC5, with a loud failure if
  the table cannot be found — a doc-parsing test that silently matches zero rows
  is worse than no test.

**Discovered**
- **Fixing 413 exposed the empty-message bug**, which had been shipping since
  LAI-002 and was invisible because nothing asserted on `message` for a
  framework-raised error. Worth noting that the useful find came from the boring
  half of the task, not the interesting one.
- **`app.router.match` is usable and stable enough to build on**, including for
  routes mounted via `app.route()` on a sub-app — tested. Worth knowing for M3,
  where the MCP endpoint will want similar introspection.
- Two pre-D-021 assertions had to change (413→`unprocessable`, 405→`bad_request`).
  Both were mine from LAI-002/LAI-006 and both were correct at the time; the
  decision moved under them.

**State**
LAI-042 in `.tasks/review/`, all five criteria ticked, five review notes.
317 tests; format, lint, typecheck clean. All three behaviours verified over a
real socket, including that an unknown path still answers 404 rather than 405.

## 2026-08-24T07:10:00+05:30 — LAI-043

**What changed**
- `server/test/tooling/env-contract.test.ts` — 5 checks comparing `server/src/env.ts` against `docker/docker-compose.yml`, both directions

**Decisions**
- **Discovered both sides by running the code, not parsing it.** A `Proxy` in
  place of `process.env` records every variable `readEnv` touches; the required
  set is found by calling `readEnv` from an empty production environment and
  adding whatever the thrown `EnvError` names until it boots. A regex over
  `env.ts` would have been quicker and would have rotted exactly the way the
  contract it checks did.
- **Both parsers throw rather than return empty.** A doc- or config-parsing check
  that silently matches nothing passes vacuously forever, which is worse than not
  having it. Probed that too.
- **Seeded the exemption list empty.** The task expected the first run to find
  drift; it found none — compose sets five variables and the server reads all
  five. Reporting that beats padding a list to look thorough.

**Discovered**
- **The forward-looking probe is the one that convinced me the check is worth
  having.** Making a *new* variable required in `env.ts` — the same mistake as
  the outage, from the other side, by a future task — fails immediately and names
  it. That is the case no amount of care prevents, because the person adding the
  requirement has no reason to open `docker-compose.yml`.
- `LAIKA_DISABLE_INVITE_ONLY` is deliberately *not* caught here: it is a
  SPEC↔server gap (LAI-105), and compose does not set it, so neither direction of
  this check applies. A third check against §11.7's table would cover that class
  — noted at review as a possible task rather than folded in.
- Nothing under `docker/` was modified; verified with `git status docker/` after
  each of the four probes, since three of them edited it temporarily.

**State**
LAI-043 in `.tasks/review/`, all five criteria ticked, four review notes.
322 tests; format, lint, typecheck clean. Four probes recorded, including the
exact 35-minute regression that prompted the task.

## 2026-08-24T07:29:00+05:30 — LAI-044

**What changed**
- `server/src/db/enums.ts` — `ACTOR_KINDS` gains `system`; `ACTIVITY_TYPES` gains `org.created`
- `server/src/db/schema.ts` — the D-022 biconditional check; the §4.8 deviation comment replaced
- `server/src/db/migrations/0003_system_actor_kind.sql` — generated, then hand-extended to recreate the append-only triggers
- `server/test/db/activity.test.ts` — 9 new cases; one existing case corrected

**Decisions**
- **Recreated the append-only triggers inside the migration** rather than
  treating their loss as acceptable. See the discovery below — this was not a
  judgement call so much as a rescue.
- **Asserted all four combinations of the biconditional**, not just the happy
  path. The `system`-with-an-actor rejection is the easy one to omit, and without
  it `system` becomes a label anyone can attach to a human-authored row.
- **Added a test that `activity` has exactly two triggers**, so a re-run or a
  future rebuild cannot leave duplicates or a partial set.

**Discovered**
- **This migration silently destroyed the append-only guarantee, and LAI-003's
  tests caught it within seconds.** drizzle-kit implements any `activity` change
  as `CREATE __new_activity` … `DROP TABLE activity` … `RENAME`, and **SQLite
  drops a table's triggers along with the table**. So the §4.8 "no updates, no
  deletes, ever" enforcement from migration 0000 was gone, while the schema
  comment still claimed it. Three tests went red immediately.

  My own LAI-003 note had predicted exactly this ("any later migration that
  needs to rewrite `activity` must drop and recreate them deliberately") — but I
  would not have remembered it eight tasks later. The test remembered. This is
  the strongest argument I have seen today for the repo's insistence that a guard
  be proven able to fail: a happy-path-only `activity.test.ts` would have shipped
  a mutable audit table under a fully green gate.
- **`agent` and `system` are genuinely different**, which one existing test had
  conflated: `agent` is a token-authenticated *person* and still has an actor; a
  webhook has none. D-022 forced that distinction the first time a row met the
  constraint.
- Any future migration touching `activity` needs the same trigger rescue. Left a
  comment in 0003 saying so, since the next person will hit it the same way.

**State**
LAI-044 in `.tasks/review/`, all eight criteria ticked, five review notes.
331 tests; format, lint, typecheck clean. Verified against a fresh database that
all four migrations apply, the constraint is present in both directions, and both
triggers survive.

## 2026-08-24T07:56:00+05:30 — LAI-009

**What changed**
- `server/src/services/setup.ts` — `setupRequired`, `createFirstOrg`, `removeOrphanedOwner`, `slugify`, `defaultPrefix`
- `server/src/http/routes/setup.ts` — `GET /setup/status`, `POST /setup`
- `server/src/http/middleware/setup-gate.ts` — API gated with `conflict` until an org exists
- `server/src/http/static.ts` — SPA document redirects to `/setup`
- `server/src/app.ts`, `src/index.ts` — wiring
- `server/test/services/setup.test.ts` (13), `test/http/routes/setup.test.ts` (17)
- `test/auth/flow.test.ts`, `test/http/idempotency.test.ts` — seed an org, now that the gate exists

**Decisions**
- **`conflict`, not `not_found`, for gated routes.** The endpoint exists and the
  request is well-formed; it is the instance's state that blocks it, and that is
  something the caller can fix. A 404 sends them hunting a typo.
- **`/health` is never gated**, for the same reason it is never rate-limited
  (LAI-030) — the container probe would restart a server that is merely waiting
  to be configured.
- **The redirect lives in the SPA handler, not a middleware**, so it applies only
  to paths that would serve the document. A blanket redirect would stop the setup
  screen's own assets loading.
- **better-auth creates the Owner account**, then one `BEGIN IMMEDIATE`
  transaction does org + role + project + memberships + activity. Hand-writing
  the password would mean owning Argon2id, which LAI-003 explicitly forbade.
- **The loser of a setup race has its account deleted**, so the email is not
  permanently held by a failed attempt.
- **Gave the Owner an explicit project membership** rather than relying on the
  implicit lead their org role grants — otherwise a later demotion silently
  strips project access.

**Discovered**
- **My own LAI-037 layering rule caught me.** `routes/setup.ts` reached into
  `db/schema` to delete the orphaned account and `pnpm lint` refused it. The
  cleanup moved to the service. Two tasks after landing, the rule caught the
  person who wrote it — which is the only real test of a rule like that.
- **`trackPresence` is a live cross-session mismatch.** Builder-B's
  `FirstBootSubmit` carries it; SPEC §4.2 has no column. Because §6.3 requires
  unknown fields to be *rejected*, wiring the form as-is will 422. Correct
  behaviour, and exactly the kind of thing that would otherwise be "fixed" by
  quietly ignoring the field. → **LAI-106** filed with the full request contract
  so the wiring session does not have to reverse-engineer it.
- **AC5 was unbuildable by this session** — the setup UI is `server/web/`, which
  D-016 moved to Builder-B after this task was written. Third task today whose
  criteria predate an ownership change (LAI-023's CSP and LAI-007 were the
  others). The pattern is worth naming: tasks written before D-016 assume one
  builder owns the whole stack.
- Two existing test files needed an org seeded. Both were correct before; the
  gate changed the meaning of "no org row" from "nothing to see" to "not set up
  yet".

**State**
LAI-009 in `.tasks/review/`, all eight criteria ticked, eight review notes.
361 tests; format, lint, typecheck clean. **M1's exit criterion verified against
the built server**: empty database → redirect to setup → POST → signed in as
owner with a project membership → second POST refused → `org.created` and
`project.created` in the audit trail.

## 2026-08-24T08:14:00+05:30 — LAI-010

**What changed**
- `server/src/services/projects.ts` — list/create/get/update, members add/change/remove, public join, last-lead guard
- `server/src/http/routes/projects.ts` — transport for all of it
- `server/src/db/enums.ts` — `project.updated`, `project.archived`, `member.removed`
- `server/src/db/migrations/0004_project_activity_verbs.sql` — generated, then hand-extended to recreate the append-only triggers
- `server/src/app.ts` — mounted
- `server/test/services/projects.test.ts` (29), `test/http/routes/projects.test.ts` (14)

**Decisions**
- **Added three activity verbs** because AC6 requires one per mutation and §4.8's
  closed vocabulary had none for update, archive or member removal. Followed the
  LAI-044 precedent (enum + migration here, §4.8 edit by PM → LAI-107).
- **`project.archived` is its own verb.** Archiving is what removes a project
  from every active view; making an audit reader diff a payload to find that out
  defeats the point of a verb.
- **Filtered visibility in code, not SQL.** `can()` is the only authority (§3.3);
  a `WHERE` clause encoding the same rules is how two authorities drift apart.
  Costs reading rows the actor cannot see — right trade at M2 scale, worth
  revisiting with a real project count.
- **Guarded demotion as well as removal for the last lead.** Changing the last
  lead to `member` leaves a project leaderless just as surely.
- **Refused org-viewer roles at the write path**, not merely capped on read: a
  stored row silently downgraded on every read is a lie in the database.
- **Added `PATCH /:slug/members/:userId`** beyond AC4's three, because §6.4 lists
  it and `member.role_changed` exists for nothing else.
- **Did not add `projects.repo`** despite §4.3 listing it — no criterion asked
  for it. → LAI-108.

**Discovered**
- **The migration dropped the append-only triggers again**, exactly as LAI-044
  predicted for any future `activity` rebuild. LAI-044's test caught it in
  seconds; that test has now paid for itself twice, in two different tasks.
- **Third instance of a task needing a value a closed vocabulary lacked** —
  LAI-022 (error codes), LAI-044 (`org.created`), now these three verbs. The
  pattern is consistent enough that a mechanical `enums.ts` ↔ §4.8 check would
  probably pay, the way `errors.test.ts` now parses §6.3's table. Suggested in
  LAI-107 rather than filed, since it is PM's call.
- **Membership removal cannot produce a §6.3 tombstone.** The row is hard-deleted,
  so `updated_since` has nothing to mark — the `member.removed` activity row is
  the only signal a catching-up client gets. Worth §4.8 saying so.
- My first draft of the test helper tried to hand-roll actor construction and was
  a mess; `loadActor` already does it and using the real thing made the tests both
  shorter and more honest about what the middleware produces.

**State**
LAI-010 in `.tasks/review/`, all eight criteria ticked, eight review notes.
413 tests; format, lint, typecheck clean. Verified against the built server:
create, both conflict cases, detail, patch, archive-as-tombstone, last-lead
refusal, anonymous 401, and the activity trail.

## 2026-08-24T08:30:00+05:30 — LAI-105

**What changed**
- `server/test/tooling/env-contract.test.ts` — SPEC §11.7 ↔ env-read checks, both directions, plus a fix to the read-discovery probe
- (no production code: the decision is to remove the variable, and nothing read it)

**Decisions**
- **Remove `LAIKA_DISABLE_INVITE_ONLY` rather than implement it.** The argument
  that settled it: the escape hatch does not open the door it appears to. A signup
  made while invite-only is disabled gets `org_role: member` (`input: false` in
  `auth.ts`), so the operator gains an account that still cannot invite, administer
  or reach settings — the lockout is unchanged and only the door is open. LAI-009
  already handles bootstrapping, and §4.11 link invites need no SMTP, which is the
  other scenario people reach for it to solve.
- **Verified both halves of that argument by grep rather than from memory** — the
  `input: false` default and §4.11's nullable invite email.
- **Built the §11.7 ↔ env check instead of production code**, since the decision
  itself removes rather than adds. It is the third direction I flagged in LAI-043's
  review; this task is what justified building it.

**Discovered**
- **My LAI-043 read-discovery probe had a blind spot, and this check found it.**
  `LAIKA_DATA_DIR` came back as documented-but-unread. It *is* read — but
  `resolveDbPath` reads `LAIKA_DB_PATH` first and returns early, so a single Proxy
  pass supplying every variable never reaches the fallback. The probe was blind to
  every precedence chain in the file, and would have stayed blind indefinitely
  because the compose direction happened not to exercise one.
  Fixed by suppressing each discovered variable in turn until the set settles;
  confirmed the fix matters by reverting to a single pass and watching the false
  failure return.
- Two instances of the same drift class existed simultaneously
  (`LAIKA_DISABLE_INVITE_ONLY`, `LAIKA_DATA_DIR`), one of them invisible to the
  tooling I had already written. Worth remembering that a check written against
  one known instance can still miss its siblings.
- Third task today where the useful output was a *decision plus a guard* rather
  than a feature. The guard is what stops the decision needing to be remembered.

**State**
LAI-105 in `.tasks/review/`, all five criteria ticked, six review notes.
417 tests; format, lint, typecheck clean. Three probes recorded. **LAI-109** filed
for the `docs/` half (the §11.7 row and the DECISIONS entry), carrying the full
reasoning ready to lift.

One loose end, stated at review: the `DOCUMENTED_BUT_UNREAD` exemption must be
deleted when PM removes the row. The staleness guard fails until it is — that
failure is the reminder, and it is a one-line deletion in `server/`.

## 2026-08-24T08:48:00+05:30 — LAI-052

**What changed**
- `server/test/tooling/env-contract.test.ts` — deleted the stale
  `LAIKA_DISABLE_INVITE_ONLY` exemption (commit `2f1eec2`, made before the claim)

**Decisions**
- **Fixed the red gate before claiming anything.** The staleness guard fired on
  the routine `git merge master` at the top of this cycle. Leaving `master` red
  while starting unrelated work would have been the wrong order regardless of
  which task the board offered next.
- **Kept the map and both guards**, per AC1, and rewrote the comment above them to
  say the emptiness is intentional. An unexplained empty map invites deletion, and
  the map is the mechanism that caught this.

**Discovered**
- **The guard I wrote in LAI-105 worked exactly as designed**, one cycle later and
  against a real omission. It fired on the first merge after PM's spec edit and
  named the entry and the reason, so the fix took under a minute.
- **The reusable lesson, which PM named in the task and I agree with:** my inline
  comment said "delete this entry with the row", but it lived in
  `env-contract.test.ts` while the person who needed it was editing
  `docs/SPEC.md`. Two files apart, so it could never be seen at the moment it
  mattered. What actually carried the message across the boundary was the failing
  test — because its message says *what to do*, not merely that something is
  wrong. For a cross-area handoff, put the reminder in the receiving session's
  gate, not as a comment in the sending session's file.
- Verified from clean clones of both branches rather than trusting the working
  tree, since the criterion is specifically about `master`.

**State**
LAI-052 in `.tasks/review/`, all three criteria ticked. 417 tests green on
`builder-a`; `master` red until this merges, which is the earliest it can be —
the file is in `server/`.

## 2026-08-24T09:14:00+05:30 — LAI-011

**What changed**
- `server/src/services/task-lifecycle.ts` — the §5 transition table as data, and `isReady`
- `server/src/services/tasks.ts` — create/list/get/update, claim CAS, status change, dependency add/remove
- `server/src/http/routes/tasks.ts` — project-scoped and id-scoped routers
- `server/src/db/enums.ts` + `migrations/0005_task_dependency_removed.sql` — the missing verb, plus the trigger rescue
- `server/test/services/{task-lifecycle,tasks,tasks.concurrency}.test.ts`, `tasks.worker.ts`, `test/http/routes/tasks.test.ts` — 60 new tests

**Decisions**
- **Wrote the reverse transition edges §5 omits, as data with reasons.** Allowing
  work to move backwards matters: forbidding it pushes people to cancel and
  recreate, losing the history `activity` exists to keep. `done` reopens only
  into `in_progress`, and `done → cancelled` is refused because reopening is what
  was actually wanted.
- **Refused no-op transitions** — they would write an activity row claiming a
  change happened.
- **Followed §5 over AC4 on who may send to review** (lead included). D-011.
- **Filtered `ready` after the query, not in SQL.** It is derived from three
  things including dependency statuses; a SQL predicate would be a second
  definition of readiness.
- **`assignee=none` sentinel** for unassigned, since a query string cannot
  distinguish `""` from absent.
- **PATCH cannot change status.** A generic field update would route around §5's
  validated transitions.

**Discovered**
- **Fourth missing value in a closed vocabulary** — `task.dependency_removed`
  this time (after LAI-022's error codes, LAI-044's `org.created`, LAI-010's three
  project verbs). Four occurrences is a pattern, not a coincidence: tasks are
  written against a spec section that lists *nouns* without checking the verbs
  each mutation will need. LAI-107 already suggests a mechanical `enums.ts` ↔
  §4.8 check; it looks better every time.
- **Third trigger rescue.** Every `activity` change is a table rebuild and SQLite
  drops triggers with the table. The LAI-044 test caught it again — third for
  third. The rescue block is now byte-identical across migrations 0003, 0004 and
  0005, which is exactly the shape of something that should be generated rather
  than remembered.
- **Verified the claim CAS test can fail** by removing the `assignee_id IS NULL`
  predicate: two winners. Without that check the test would have been proving
  that six sequential calls happen sequentially.
- My first draft of the transitions test block left a placeholder helper and
  confused setup; rewrote it around real project members so the lead-vs-member
  distinction is actually exercised rather than asserted through an org admin who
  bypasses everything.

**State**
LAI-011 in `.tasks/review/`, all nine criteria ticked, eight review notes.
477 tests; format, lint, typecheck clean. Verified against the built server:
keys, readiness flipping on a dependency, cycle rejection, claim conflict with
the holder named, illegal transition listing what is allowed, and the activity
trail. Unblocks LAI-047, LAI-048 and LAI-050.
