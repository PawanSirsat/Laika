# PM — 2026-08-24

## 2026-08-24T01:30:00+05:30 — bootstrap

**What changed**
- `CLAUDE.md` — working agreement: ownership table, task protocol (claim = `git mv` + commit), logging requirement, git conventions, code rules, hard boundaries
- `README.md` — repo orientation and the ownership map
- `docs/SPEC.md` — v1 spec: product, roles + permission matrix, data model (11 tables), REST conventions and endpoints, MCP tools, presence, webhooks, stack, LLM provider, cross-cutting, open questions
- `docs/ROADMAP.md` — M1–M7 with an exit criterion per milestone and sequencing notes
- `docs/DECISIONS.md` — D-001 SQLite-only, D-002 single container, D-003 SSE over WebSockets, D-004 invite-only default, D-005 metadata-only heartbeats
- `.tasks/TEMPLATE.md`, `.tasks/README.md` — task format and board semantics
- `.tasks/backlog/LAI-001..LAI-012` — 12 seeded tasks (9 server, 1 docker, 1 plugin, plus LAI-011 tasks CRUD)
- `.claude/skills/laika-workflow/SKILL.md` — claim/finish/discovered-from protocol and ownership boundaries
- `.claude/skills/laika-logging/SKILL.md` — log format with a worked example
- `.claude/commands/{standup,claim,review}.md` — the three session commands
- `.sessions/{pm,builder-a,builder-b}.md`, `.sessions/README.md` — identity files
- `server/README.md`, `plugin/README.md`, `cli/README.md`, `docker/README.md` — area stubs with expected layout
- `logs/`, `.tasks/{backlog,in-progress,review,done}/` — created with `.gitkeep`

**Decisions**
- **`ready` is derived, never stored** (SPEC §4.5) — `backlog` + unassigned + all
  dependencies `done`. A stored flag needs recomputing on every dependency
  transition and will eventually be wrong; a derived one cannot drift. Costs a
  join on `list_ready_tasks`, which is cheap at our scale.
- **`discovered-from` is provenance, not a dependency.** Kept as a column on
  `tasks`, separate from `task_dependencies`, so a discovered task can be worked
  before its parent closes. Conflating the two would make every discovery block
  itself.
- **Claiming is compare-and-swap at the API level** (SPEC §5) and `git mv` at the
  file level — deliberately the same idea twice, so the protocol builders follow
  by hand matches the one the product enforces. Second claimant gets `409` with
  the current assignee, not a silent overwrite.
- **Root config files are not in any builder's area.** Rather than granting
  Builder-A the repo root, LAI-001 lists the eight root files it may create, and
  `CLAUDE.md` §1 states that scope exceptions must name files individually.
  "Whatever else is needed" grants nothing.
- **`can()` is pure and synchronous**, with the caller loading the actor and
  resource. Keeps it exhaustively unit-testable against the §3.1 matrix, which is
  the only way that table stays true.
- **LAI-012 (plugin skeleton) seeded now despite being M4 work.** It has no
  dependencies, and without it Builder-B has only LAI-008 during M1 — which
  itself waits on LAI-001/LAI-002. This is the difference between Builder-B
  working and Builder-B waiting.
- **Task ids renumbered against the roadmap** so M1 is a contiguous LAI-001..009
  and M2 starts at LAI-010; the out-of-milestone plugin task took the last id
  rather than sitting in the middle of the M2 range.

**Discovered**
- `docs/SPEC.md` §13 carries five genuinely open questions rather than fake
  answers: Member-can-assign-others, per-project task templates, whether
  `blocked` should be a stored status, task attachments, and multiple concurrent
  LLM providers. None block M1. Each becomes a `DECISIONS.md` entry when forced.
- Heartbeat branch→task resolution has to happen server-side — the plugin cannot
  know a deployment's project keys. Written into SPEC §8.2 rather than filed as a
  task, since M5 is not broken down yet.
- Two different `.claude`-shaped directories exist (`plugin/` is the shipped
  plugin; `.claude/` configures the sessions building Laika). Called out
  explicitly in `.sessions/builder-b.md` and LAI-012 — it is exactly the kind of
  thing that produces an edit in the wrong tree.
- Repo has no commits and is on `master` while `main` is the nominal default.
  Left as-is; renaming a branch is not a PM decision to make silently.

**State**
Bootstrap complete and committed. Backlog seeded with LAI-001..LAI-012, all
unclaimed. `.tasks/in-progress/`, `.tasks/review/` and `.tasks/done/` are empty.
Next: Builder-A claims LAI-001 (everything server-side waits on it); Builder-B
claims LAI-012 (the only task with no dependencies in their area).
