
## 2026-09-19T23:33:30Z — LAI-607

**What changed**
- server/web/src/components/shell/SessionGate.tsx — the boot gate is a spinner,
  not a second skeleton for the same wait
- server/web/src/components/LoadingState.tsx — a card skeleton is two bars, not
  three
- server/web/src/components/space/PresenceStrip.tsx — nothing at all while
  presence loads
- server/web/test/browser/column-reorder.test.ts — `settledOpacity`, the
  lane-head guards, and the retired count-colour rule

**Decisions**
- **Cut loaders rather than add them.** The owner: *"we refresh showing very
  different loader then again there lot more unnecessary loader."* Measured
  across one refresh: three treatments in sequence, 34 animated elements at
  peak, stragglers 3.5s past usable. LAI-295 added feedback where it was
  missing; this removes feedback that was never needed, and it is the same job.
- **A skeleton is a promise about what is coming.** The boot gate cannot make
  one — any route may be behind it — so it has no business drawing a card
  layout that the board then replaces with a different shape.
- **Ambient chrome gets no loading state.** Presence resolves after the board;
  a spinner beside a usable board reads as *stuck*, not *coming*.
- **Retired my own count-colour assertion** rather than inverting it. The owner
  reversed the one-colour rule (LAI-606 gives In-progress and Done their own
  badges), so an assertion that the count never varies would block the correct
  implementation. Kept the dot guard, which is a live instruction.

**Discovered**
- **`column-reorder.test.ts` hangs forever without `--test-timeout`.** Two
  10-minute runs produced *zero bytes*. With `--test-timeout=25000` it reports
  instead: all four **drag** suites fail, and they pre-date my work in the file.
  The keyboard-reorder test beside them passes, so reordering is fine and the
  drag helper is waiting on something the LAI-606 refactor moved. This is what
  has been stalling gates for both sessions. Handed over.
- **I committed that suite before ever seeing it pass.** Two background runs
  verifying it were killed and I did not notice the gap before staging. It was
  red. Mine, and now fixed.
- **Two races, same shape, one after the other.** `settledOpacity` fixed a
  *fade* race (read `0.177275` where `1` was expected) and introduced a
  *render* race — it read before the board existed, got `missing`, and failed
  an assertion whose message was about opacity. It passed full-file and failed
  under `--test-name-pattern`, purely from ordering. The helper now waits for
  `.lane-head` before polling. **An assertion whose failure message names the
  wrong thing costs more than no assertion.**
- **My own vacancy guard earned itself**: the dot test failed *"only 0 dots —
  this proves little"* rather than passing over an empty set.
- **Cleanup that runs last does not run.** A scratch test file left in
  `test/browser/` reddened the repo gate for both sessions because the `rm -f`
  was the final line of a command that timed out. Same shape as the `set -e`
  mutation harness reaching no restore, one day later, not generalised.
  It should not have been in `test/browser/` at all — the scratchpad exists.
- **Killing a wrapper does not kill its children.** Four orphaned
  `pnpm -r run test` trees survived killed gates and competed for CPU, which is
  part of why gates ran long for both of us. Cleaned by explicit pid. One
  orphan from 2026-09-18 running `theme-toggle.test.ts` is not mine and was
  left alone.

**State**
LAI-607 in review. Three commits: `8e7babb`, `18b42ba`, `64a994b`; my three
tests pass three runs running. Full repo gate **not** run — the tree carries the
other session's LAI-606 refactor, so a repo gate measures their work, not mine;
`tsc --noEmit` is exit 0 and the four loading suites pass. LAI-298, LAI-603 and
LAI-607 all in review; nothing in progress.

## 2026-09-20T15:56:00Z — LAI-176

**What changed**
- cli/test/plugin-mcp.test.ts — the tool roster test names all eighteen tools ("these eleven" → "these eighteen"), with a comment noting assign_task is deliberately absent (folded into update_task.assignee)
- plugin/README.md — both "eleven" claims now say eighteen since LAI-611
- .tasks/backlog/LAI-611-*.md — stale duplicate removed after the master merge (done/ holds the accepted copy)

**Decisions**
- Deployed to AWS before moving LAI-176 to review, at the owner's direct instruction — the cli/plugin half is not in the Docker image, so the deploy did not ship an untested state. The image carries master's LAI-611 plus shell's UI work.

**Discovered**
- The live /mcp on the AWS instance announces all eighteen tools after the container restart — verified with an authenticated tools/list against production, not assumed from the code.

**State**
LAI-176 in review. LAI-611 deployed to production (52.72.203.206). Known open protocol debt: LAI-606 still sits in in-progress with two unhandled-error gate reds, and the LAI-605 restyle work has been committed against an unclaimed backlog task — both need regularising in a quiet moment.

## 2026-09-20T19:05:00Z — LAI-605 / LAI-606 closure

**What changed**
- server/web/src/theme/token-list.ts — radius-column joins the inventory
- server/web/test/theme/type-roles.test.ts — weight rule follows the prototype to 800; import paths fixed after the mirror move
- server/web/test/browser/{board-lane-scroll,card-anatomy,space-bar,column-reorder,view-settings}.test.ts — the twelve deferred suites re-aimed at the prototype's values (lane 248, card 13/13/11 + 13.5 titles, tabs 36 active-700, header-as-drag-handle, tinted count badges, grouping counts draggable headers)
- server/web/src/routes/screens/meeting-review/meeting-review.css — DECISION tag to blue; the prototype palette folded pink into red and it had become identical to DEAD
- .tasks/: LAI-602/604/605/606 all to review; LAI-614 filed (toolbar fold + evidence pack)

**Decisions**
- LAI-605 goes to review with its two chrome criteria unticked and split to LAI-614, rather than either ticked dishonestly or held in-progress forever — CHIEF's merge condition is "nothing in progress", and the honest split satisfies it without claiming unfinished work.
- DECISION→blue chosen over reviving a separate pink family: the prototype's palette is the contract and blue was the one unused hue on that screen.

**Discovered**
- Chrome 121+ discards ::-webkit-scrollbar styling when scrollbar-width/color are set — recorded in board.css; the same trap will bite any future scroller.
- Prettier disagreement with hand-generated JSX cost a format red an hour after the suite ran: gate after the last edit, again.

**State**
in-progress/ is EMPTY. In review: LAI-176, 602, 604, 605, 606. All four gates green, each exit code read standalone. Shell is fully reviewable for CHIEF; LAI-176's landing unblocks master's cli red and the origin push.
