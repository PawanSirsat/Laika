# CHIEF — 2026-09-21

## 2026-09-21T00:10:00Z — LAI-176 (review; continued from 2026-09-20)

**What changed**
- `CLAUDE.md` §2 — the tool-count figures in the "wrong number" passage were
  present-tense and my own LAI-611 landing had falsified them. Past-tensed
  (`§7.1 listed eleven and the server served eleven`), with a new paragraph
  recording the measurement: §7.1's first column returns **18**, the registry
  returns **18**, the backticked-identifier grep returns **73** rather than 22.
  Commit `1914d11`.
- Nothing else. **No merge, no push.** See below.

**The verdict on LAI-176: it passes on its contents, and I did not land it.**

Those are two separate findings and collapsing them would be the whole error.

**Why it passes.** All four criteria verified, and the substantive one was
verified *mechanically* rather than read:

| | checked how |
| --- | --- |
| AC1 — the eighteen names | extracted all three lists and diffed them |
| AC2 — `plugin/README.md`'s two "eleven" claims | both changed; paragraph re-reads correctly |
| AC3 — `cli` exits 0 | see the caveat below |
| AC4 — nothing under `server/` or `docs/` | `git show --name-only` over all four LAI-176 commits: none |

The three lists the two assertions compare — `server/src/mcp/`'s registry,
§7.1's table, and the hardcoded roster in `cli/test/plugin-mcp.test.ts` — are
**byte-identical at eighteen names**. I confirmed first that `server/src/mcp/`
and `docs/SPEC.md` are identical between `master` and `shell`, so the comparison
is against the tree the assertion will actually run on.

**AC3 carries a caveat I am not going to paper over.** I could not run `cli`'s
suite against `shell`'s tree, because §4.2 forbids me checking out another
session's branch, and the fix is not on `master`. What I verified is the
*substance* of the assertions — the three lists agreeing — which is what makes
them pass. The run itself is SHELL's report and the lead's. **That is a
reasoned inference plus somebody else's measurement, not mine**, and it is
recorded that way rather than as a tick I earned.

**Why I did not land it.** Accepting means merging `shell`, and §4.4 is explicit
that **a merge lands a branch, not a task**. Measured:

```
git diff --stat master...shell   →  212 files, 23,118 insertions, 2,883 deletions
```

spanning ~25 task ids. Three of them — **LAI-602, LAI-604, LAI-606** — are still
in `.tasks/in-progress/` on `shell`: not merely unreviewed, but **unfinished by
the builder's own declaration**. SHELL's own log for 2026-09-20 says LAI-606
"still sits in in-progress with **two unhandled-error gate reds**", and that
LAI-605's restyle work "has been committed against an **unclaimed backlog
task**". Both are their words, and both match what I found independently.

§4.4's sentence is *"if a merge brings work whose task is not in review, say so
and review it, or wait."* Reviewing ~25 tasks is not this run, and the lead
pre-authorised the alternative. **So: merge nothing, and say so.**

**One thing I checked before reporting it, and it is not a violation.** `shell`
carries ~5,100 insertions under `server/src` and `server/test` from LAI-266 —
CORE's area, including `services/task-lifecycle.ts` and the §5 transition table.
That looks alarming and is fine: LAI-266's task file opens with a **Scope
exception** section, the owner granted the crossing directly, and the file
**names every `server/` path one by one**, which is exactly what §1 requires. I
nearly reported it as a crossing on the strength of the commit subjects alone.
Reading the task file first is the difference between a finding and a false
alarm.

**Decisions**
- **Merged nothing.** The alternative was merging `shell` wholesale to turn
  `master`'s `cli` green, which would have landed three in-progress tasks with
  known gate reds in order to fix one red. That trades a *named, understood*
  red for a larger unnamed one.
- **Did not push.** `master` is not green: `cli` exits 1. The push was
  conditional on green and the condition is not met. `origin/master` never sees
  a red — and the only way to satisfy the push instruction today would have been
  the merge I just declined.
- **Fixed `CLAUDE.md` rather than filing it.** It is CHIEF's own file, the
  staleness was caused by the landing I made the day before, and it is the same
  "CHIEF applies the docs half" move as the SPEC edit. I past-tensed the old
  figures rather than overwriting them: **rewriting them would have destroyed
  the evidence** for the paragraph's own lesson.

**Discovered**
- **The passage in `CLAUDE.md` warning that prose numbers go stale had gone
  stale, and the guards had not.** `parity.test.ts` and `plugin-mcp.test.ts`
  assert names from both sides; they went red when the seven tools appeared and
  green when both documents named them, needing no edit. Every *number* written
  into prose rotted the day the surface moved. I added that contrast to §2
  because it is a better argument for "assert names, never a count" than the
  rule's own wording was.
- **`cli/test/plugin-mcp.test.ts`'s header is NOT stale, and I nearly filed it
  as such.** Lines 10–14 still say "eleven" — but the sentence is *"Measured
  against both sources on 2026-09-01"*, a dated record, and SHELL added a dated
  2026-09-20 line beside it. A dated measurement that is still true of its date
  is the honest form. **The test to apply is present-tense-or-not, not
  grep-for-the-old-number** — which is what made `CLAUDE.md` a defect and this
  one not.
- Nothing new filed. LAI-480 (from 2026-09-20) still stands as the open finding.

**Gate** — per-workspace standalone, on `master` at `1914d11`, per LAI-480.

| | exit | result |
| --- | --- | --- |
| `pnpm lint` (root) | **0** | clean |
| `pnpm format` (root) | **0** | clean |
| `pnpm test` in `server/` | **0** | 107 files, 1979 passed |
| `pnpm test` in `server/web/` | **0** | 897 passed, 0 failed |
| `pnpm test` in `cli/` | **1** | 74/75 — `not ok 3 - and they are these eleven` |

**State**
LAI-176 **passes review, stays in `.tasks/review/` on `shell`**, not moved to
`done/` because accepting it requires a merge I am declining. `master` at
`1914d11`, 13 commits ahead of `origin`, **not pushed**, `cli` red. The red
clears the moment LAI-176's two files reach `master` — which needs either the
UI debt regularised so `shell` can merge whole, or an owner decision on how to
handle it. Production is ahead of the record and stays that way today.

## 2026-09-21T01:05:00Z — LAI-176 + the restyle pass, merged and accepted

**What changed**
- Merged `shell` into `master` (`ebecfc4`, `--no-ff`) — 213 files, ~23,250
  insertions, no conflicts and no two-copy collisions.
- `.tasks/done/` — **five** accepted: LAI-176, LAI-602, LAI-604, LAI-605,
  LAI-606, each with an accept note recording what was actually verified.
- **Fifteen tasks deliberately left in `.tasks/review/`.**

**The correction that shaped this pass.** I was told five tasks sat in review.
`.tasks/review/` on `shell` held **twenty** — LAI-266, 290, 291, 292, 293, 295,
296, 297, 299, 600, 601, 603, 607, 608 and 609 have been waiting there too, and
a merge lands every one of their commits. **Five was the number I was asked to
look at, not the number riding along**, and those are different quantities
whenever the unit is a branch (§4.4).

So I split it honestly rather than pretend to a review I did not do: the code
lands because it is green and already in production, **but a task is accepted
only if I reviewed it.** The fifteen keep `status: review` on `master`, where
the next pass can see them — strictly better than waiting on an unmerged branch,
and it does not launder them into `done/` on the strength of a green gate.

**What I actually checked**

*In depth, the five.* LAI-176's three lists diffed byte-identical at eighteen
names. LAI-602 and LAI-604: every criterion ticked, frontmatter complete;
their identical `started`/`finished` pairs are the late paperwork, disclosed.
LAI-605 and LAI-606: **two unticked criteria each**, and I read all four —
superseded, not skipped, with closure notes saying precisely what is and is not
claimed, and the remainder filed as **LAI-614**, which I read and which genuinely
carries the toolbar fold, the screenshot matrix, the delta list and light-theme
verification. I was told about 605's two; **606 also had two and that was not
mentioned.** Finding them by reading rather than by being told is the only
reason they are recorded here.

*Structurally, across the whole diff.* Ownership, dependencies, and the §5.1 UI
rules — the checks that scale to 23,000 lines when a line-by-line read does not.

**Decisions**
- **Accepted LAI-605/606 with criteria unticked.** Ticking them would have been
  the defect; `task-file-state.test.ts` checks status-against-directory,
  frontmatter and ids, not criteria, so the honest state lands green and the
  outstanding work stays visible. The alternative — holding two tasks whose
  remainder is already filed — buys nothing.
- **Merged despite the fifteen.** §4.4 says *review it, or wait*; waiting here
  meant `master` staying red on `cli` and the record staying behind production
  indefinitely. Accepting only what I reviewed is the third option and the one
  that keeps both properties.

**Discovered**
- **LAI-266's scope grant is two files short of what it authorises.** Its table
  names every `server/` path one by one, as §1 requires — and the branch also
  carries `server/src/db/board-columns.ts` (new, 115 lines) and
  `server/src/http/routes/projects.ts` (+2). Both are within the *intent*: the
  db file exists because the repo's own `db/` below `services/` layering rule
  demanded it for authorised work (`b3eada2`), and the route change exposes the
  `projects` column the task did authorise. **Neither is named, which is the
  condition §1 actually sets.** LAI-266 stays in review and this is the note its
  pass needs — I am not adjudicating someone else's crossing inside a merge.
- **`@fontsource-variable/inter` is a new dependency and it is authorised** —
  named in LAI-606's own file, which is what §5 requires. I checked because the
  lockfile moved, not because anything flagged it.
- **D-032 verified directly rather than assumed.** `not-in-bundle.test.ts`
  asserts no fixture string reaches the built bundle **and** carries a
  `needles.length > 0` self-check, so it cannot pass by testing nothing —
  the one property that makes a bundle guard worth having. `DEMO_ENABLED` is an
  opt-in reduced to dead code in a normal build.
- **The §5.1 fixture grep returns only comments saying what must not ship**, and
  `route-table.ts` records that there is deliberately no `SYSTEM` group. The
  `SYSTEM_NAME` hits are activity attribution, not a nav group.
- The LAI-046 and LAI-100 id collisions pre-existed **identically** on `master`
  and `shell`; the merge did not create them and they are already exempted.

**Gate** — per-workspace standalone (LAI-480), after `pnpm install`, on the
merged tree:

| | exit | result |
| --- | --- | --- |
| `pnpm lint` (root) | **0** | clean |
| `pnpm format` (root) | **0** | clean |
| `pnpm test` in `server/` | **0** | 110 files, **2040 passed** |
| `pnpm test` in `server/web/` | **0** | **1057 passed**, 0 failed |
| `pnpm test` in `cli/` | **0** | **75/75 — the eighteen-tools red is cleared** |

`pnpm install --frozen-lockfile` first, because the merge moved the lockfile;
gating without it would have measured a tree that did not match its manifest.

**State**
Green on every workspace. Pushing `master` to `origin` — the record catches up
with production for the first time since LAI-611. Fifteen tasks await a real
review pass; LAI-614 and LAI-480 are the open backlog items worth picking up.

## 2026-09-21T13:40:00Z — LAI-615 accepted, LAI-481 filed, master HELD RED

**What changed**
- Merged `shell` (`e3b9c1b`, `--no-ff`) — LAI-615's two scripts plus `416a0fc`.
- `.tasks/done/LAI-615-*` — accepted, with four findings recorded in the file.
- `.tasks/done/LAI-481-*` — **filed retroactively by me** to record `416a0fc`,
  which had no task of its own.
- **Nothing pushed.** `origin/master` stays at `fe84f47`, green.

**The gate caught a regression that the builder's own run did not.**

`server/web` is **red: 3 failures**, all in
`server/web/test/browser/space-bar.test.ts` — a suite `416a0fc` did not
re-aim. Reported to me as "both sidebar suites re-aimed, 19/19 green"; that is
true of `spaces-sidebar.test.ts` and `board-follows-space.test.ts`, and
**`space-bar.test.ts` is a third file that also asserts on the rail wordmark.**

```
not ok 1 - renders the design’s identity row from real data      (:130)
not ok 2 - names the space from the by-slug response, not the list (:205)
not ok 1 - wide with the rail open, both name it — deliberately   (:495)
```

**Deterministic, not flaky — I checked rather than assumed**, because three
`waitForFunction` timeouts look exactly like flakiness and this suite has a
documented history of a real race. The assertion is:

```js
document.querySelector('.sidebar-wordmark')?.textContent === 'Laika Core'
```

`416a0fc` makes that element render the literal `Laika` on every route. **The
test waits fifteen seconds for text that can no longer appear.** The timeout
*is* the failure mode here, not a symptom of load.

The third test names it outright — *"wide with the rail open, **both** name it
— deliberately"* — and its comment records the LAI-293 decision it was written
for: *"the owner asked for the rail to name the project and the bar to stop
repeating it."* `416a0fc` reverses exactly that. **These tests are not wrong;
they encode the previous contract and nobody re-aimed them.**

**Decisions**
- **Held the merge, did not push.** `origin/master` never sees a red. The
  product change is right — it is owner-directed and dated — but a third suite
  still asserts the old behaviour, and a green `origin` matters more than a
  fast one.
- **Kept the merge locally rather than resetting.** §4.4 step 3 explicitly
  expects an unpushed local `master` mid-procedure and expects other sessions to
  merge it. SHELL needs this state to fix `space-bar.test.ts` against it.
  Resetting would have discarded LAI-481 and the LAI-615 accept note to buy
  nothing.
- **Filed LAI-481 myself.** `416a0fc` was committed under **LAI-614's** id while
  LAI-614 sat unclaimed in `backlog/` — its criteria are the toolbar fold and
  evidence pack, which this is not. Rather than block a person who is waiting,
  I reviewed the diff and wrote the task that should have existed, saying in the
  file that the review is real and the paperwork retroactive.
  **Third instance of this shape** (LAI-605, and SHELL's own log flagged it as
  debt "needing regularising in a quiet moment"). Three times is a pattern, not
  a quiet-moment problem.

**Discovered**
- **LAI-615's scripts handle the secret correctly**, which was the thing worth
  checking hardest given this repo is public: no token literal in the diff,
  `umask 077` set **before** the heredoc creates `~/.laika/env` so there is no
  world-readable window, nothing written inside the repository. `sh -n` re-run
  here, both `0`.
- **The installer's default board URL is plain HTTP** (`http://52.72.203.206`),
  so every teammate onboarded this way sends a personal access token over the
  network in cleartext. SPEC §11 says *"TLS is somebody else's job —
  `docker/Caddyfile.example` ships as a reference"*, so the architecture is not
  at fault; the deployment has not done it, and this script is about to make the
  cleartext path **the documented default in a public repository**. Owner
  decision, not a code defect.
- **The token is echoed as it is typed** — `read -r TOKEN` with terminal echo
  on, during exactly the walkthrough scenario this script exists for.
  `stty -echo` is the POSIX fix.
- Two smaller ones in the LAI-615 file: `cat "$TMP" > "$RC"` rewrites a user's
  shell rc non-atomically, and one `echo "\n…"` in a `#!/bin/sh` script.

**Gate** — per-workspace standalone, after `pnpm install --frozen-lockfile`:

| | exit | result |
| --- | --- | --- |
| `pnpm lint` (root) | **0** | clean |
| `pnpm format` (root) | **0** | clean |
| `pnpm test` in `server/` | **0** | 110 files, 2040 passed |
| `pnpm test` in `server/web/` | **1** | **1054/1057 — 3 failed, all `space-bar.test.ts`** |
| `pnpm test` in `cli/` | **0** | 75/75 |

**State**
Local `master` at the merge, **red in `server/web`, not pushed**.
`origin/master` remains `fe84f47` and green. LAI-615's scripts are therefore
**not yet on origin**, so the teammate is still blocked — the unblock is one
file: re-aim the three `space-bar.test.ts` assertions to the new contract, and
I merge and push. Everything else in this landing is green and accepted.

## 2026-09-21T15:20:00Z — the red cleared, merged and pushed

**What changed**
- Merged `shell` (`1c341e2`) — `a79dae5` alone: `space-bar.test.ts` re-aimed,
  `install.sh` review fixes.
- Appended follow-up notes to `.tasks/done/LAI-481-*` and `.tasks/done/LAI-615-*`
  recording where that work landed, since both were already accepted.

**What I checked, and why this one mattered.**

The risk in a "tests now pass" fix is that the tests were **weakened** rather
than re-aimed — the red goes away either way, and only one of the two is a fix.
So I read the diff before the gate rather than after.

Each assertion was **redirected, not deleted**. The by-slug race still waits on
a *value* rather than an element (`.space-name`'s text, not its presence) —
which was the original point of that test and survives the move intact. The
"No space" regression and the never-nameless-at-any-width rule moved the same
way. And the rail gained a **positive** assertion that it reads `Laika`, with a
message, so the suite still fails if the old behaviour comes back. A weakened
version would simply have dropped the wordmark check; this adds one.

The test that had to be renamed was renamed honestly — *"both name it —
deliberately"* became *"the bar names it and the rail is the product"* — rather
than keeping a title that no longer described it.

`install.sh`: three of my four findings fixed, and the `stty` work is careful —
`[ -t 0 ]` guard, state saved with `stty -g`, a `trap` on `EXIT INT TERM` so
Ctrl-C at the prompt cannot leave a terminal with echo off, explicit restore and
`trap -` after, and the `printf '\n'` for the newline the suppressed Enter no
longer echoes. `sh -n` re-run here: `0`.

**Decisions**
- **Recorded `a79dae5` against both already-accepted tasks rather than
  reopening them.** It carries LAI-481 and LAI-615 work under one id. Reopening
  two `done/` tasks to hold one follow-up commit would churn the record for no
  gain; a note in each says where the work landed and what it was.
- **Left finding 2 open.** The plain-HTTP default is with the owner as a
  decision — HTTPS now, or ship the pilot with an explicit warning. It stays
  visible in LAI-615's file so the choice is made rather than closed by silence.
  It was never a builder's to fix.

**Discovered**
- **The cause of the miss is the day's recurring shape in a new place.** SHELL's
  impact grep ended in `head -8` and truncated before `space-bar.test.ts`
  appeared — *"I searched, got a partial answer, and reported it as complete."*
  Same family as the root gate bailing and hiding two workspaces (LAI-480) and
  the `grep "Tests "` blind to `Failed`: **a truncated instrument reporting as a
  complete one.** Third distinct instance this session, in three different
  tools.
- The untruncated grep returns **four** files touching `.sidebar-wordmark`. The
  fourth, `test/components/shell-chrome.test.ts`, only asserts the class exists
  and was always green — so the blast radius was three, and all three are fixed.

**Gate** — per-workspace standalone (LAI-480), after `pnpm install --frozen-lockfile`:

| | exit | result |
| --- | --- | --- |
| `pnpm lint` (root) | **0** | clean |
| `pnpm format` (root) | **0** | clean |
| `pnpm test` in `server/` | **0** | 110 files, 2040 passed |
| `pnpm test` in `server/web/` | **0** | **1057 passed, 0 failed** |
| `pnpm test` in `cli/` | **0** | 75/75 |

**State**
Green everywhere and **pushed**. `plugin/scripts/` is on `origin/master`, so the
`git clone` → `install.sh` → `laika-claude` path works for a teammate with no
checkout — which was the thing actually blocking a person. Open: the HTTPS
decision (owner), LAI-614, LAI-480, and fifteen tasks still awaiting a real
review pass.

## 2026-09-21T16:10:00Z — session end

**What changed**
- `CLAUDE.md` §5 — new rule: **"Use `head` for a sample, never for a census."**
  Commit `29af83c` `[ops]`.

**The rule, and why it is a rule rather than advice.** Three instruments failed
the same way in one day, in three different tools: `pnpm -r` bailing at the
first failing workspace and hiding 2876 assertions; the `grep "Tests "` §5
already documents; and a builder's impact grep ending in `head -8`, which
truncated before the third test file their change broke. Their own account is
the line I kept verbatim — *"I searched, got a partial answer, and reported it
as complete."*

The operative half is that **a truncated answer is indistinguishable from a
complete one.** Nothing in `# pass 19` says nineteen was not the total; nothing
in `EXIT 1` says two workspaces never started. Care does not fix that, which is
the argument for writing it down. §3's id sweep had already learned it from the
other end — `git log` and `ls-tree` each miss ids the other finds and neither
reports being short — so this is the second place in the file to reach it, and
the first to name the shape.

**Every number in the passage was checked against its source before committing**,
which felt worth doing in a paragraph about false numbers: `six times` against
§5's own line 784, `2876` against 1979 + 897 from my own gate runs, and `four`
by re-running the untruncated grep here rather than taking it on report.

**Decisions**
- **Wrote it as `[ops]` rather than filing a task**, on the lead's judgement that
  one paragraph in a file I own is lighter than a task file. Agreed — and it is
  the §2 ops exception working as intended rather than being stretched.

**Discovered**
- Nothing new. The session's findings are LAI-480, LAI-614, the fifteen
  unreviewed tasks, LAI-266's two files outside its named grant, and the HTTPS
  decision — all recorded where they belong.

**Gate** — per-workspace standalone, after the last edit:

| | exit | result |
| --- | --- | --- |
| `pnpm lint` (root) | **0** | clean |
| `pnpm format` (root) | **0** | clean |
| `pnpm test` in `server/` | **0** | 110 files, 2040 passed |
| `pnpm test` in `server/web/` | **0** | 1057 passed, 0 failed |
| `pnpm test` in `cli/` | **0** | 75/75 |

**State**
Session ending clean. `origin/master` green and level with local. The teammate
onboarding path is live on origin. **Next session's real work is the fifteen-task
review pass** (LAI-266, 290–299, 600–609) — LAI-266 carries my note about the
two `server/` files outside its named scope grant, and that one wants reading
before the rest. After that, LAI-614 and LAI-480. The HTTPS decision sits with
the owner and is not ours to close.

## 2026-09-22T09:05:00Z — LAI-616 accepted; LAI-482 filed; my LAI-615 review examined

**What changed**
- Merged `shell` — LAI-616: `laika-claude` installs as a symlink on PATH, and
  the launcher resolves symlinks before deriving the plugin directory.
- `.tasks/done/LAI-616-*` — accepted, with the verification recorded.
- `.tasks/backlog/LAI-482-*` — filed: the same `$0` defect, sixteen lines above
  the fix, in the "not configured yet" message.

**A defect shipped from work I accepted, and my review is the reason.**

I reviewed LAI-615's scripts closely — token handling, `umask` ordering,
`echo` portability, the rc rewrite — and **never asked whether an alias was the
right mechanism at all.** The script's own closing line, *"Start a new terminal"*,
was the tell: it is a workaround for a mechanism that needed one, sitting in
plain sight in a file I read twice. The owner hit `command not found` in the
terminal they already had open, twice.

**What I actually did wrong is narrower and more useful than "missed a bug": I
reviewed the implementation and not the mechanism.** Every finding I raised was
about *how well the alias was written*. None asked whether it should be an
alias. A review that only audits the inside of a decision cannot see a wrong
decision.

**And I never ran it.** The second defect — `dirname "$0"` naming the symlink's
directory — is invisible to reading and obvious in one execution. I had a
terminal.

**So this review was done by running it**, in a sandbox `HOME` with a stub
`claude`, and the fix is right: `--plugin-dir` resolves to the real checkout and
flags pass through in order.

**Then I reproduced the shipped defect against the same symlink**, because a
passing test proves nothing until you have watched it fail:

```
master's launcher →  --plugin-dir /…/home/.local     EXIT 0
```

Not the plugin, and **exit 0** — the silent-wrong-answer shape, which is the
same family as everything else this week: an instrument that returns success
while doing nothing.

**Decisions**
- **Accepted rather than sent back**, despite finding a live defect in the same
  file. Every criterion is met and verified; §2 freezes criteria once work is
  submitted, so a finding no criterion covers is a **new task**, not a failure.
  Filed as LAI-482 at p1.
- **Did not fix the one line myself.** CHIEF writes no code, and "it is one
  line" is exactly the reasoning that rule exists to refuse.

**Discovered**
- **`$(dirname "$0")/install.sh` in the unconfigured message** names
  `~/.local/bin/install.sh` once installed on PATH. Measured, not reasoned.
  It fires for the person with the least context — someone running
  `laika-claude` before installing — and hands them a path with nothing at it.
  → **LAI-482** (discovered-from: LAI-616).
- **AC3 resolves two ways and both are fine.** Manifest removed, launcher
  reachable: the named guard fires, exit `1`. Whole checkout moved: the symlink
  dangles and the OS refuses first, exit `127`, so the script's message never
  runs. The OS is clearer than the script could be there.
- **Two instances of one bug in one script is a signal to sweep, not patch.**
  Written into LAI-482 as a criterion, with the reminder that the sweep must not
  end in a `head` (§5, which I wrote yesterday for this exact reason).

**Gate** — per-workspace standalone, after the merge:

| | exit | result |
| --- | --- | --- |
| `pnpm lint` (root) | **0** | clean |
| `pnpm format` (root) | **0** | clean |
| `pnpm test` in `server/` | **0** | 110 files, 2040 passed |
| `pnpm test` in `server/web/` | **0** | 1057 passed, 0 failed |
| `pnpm test` in `cli/` | **0** | 75/75 |

**State**
Pushed. Teammates cloning from now get the PATH install rather than the alias.
LAI-482 is the open p1. The shell scripts have no test surface in any workspace,
which is why both of these defects reached a person — worth a task if a third
one appears.
