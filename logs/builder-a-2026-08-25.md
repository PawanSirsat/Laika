# Builder-A — 2026-08-25

## 2026-08-24T19:05:00Z — LAI-071

**What changed**
- server/src/services/invites.ts — new. Create / list / revoke / preview / consume,
  `INVITE_TTL_DAYS = 7`, `newInviteToken()` (32 random bytes, base64url), and the
  `BEGIN IMMEDIATE` transaction that spends an invite exactly once.
- server/src/http/routes/invites.ts — new. The five §6.4 endpoints. `POST /accept`
  and `GET /:token` take no actor and call no `can()`; the module comment says why.
- server/src/auth/auth.ts — sign-up `after` hook consumes the invite and applies the
  role; new `sqlite` option for the transaction.
- server/src/http/error-handler.ts — maps better-auth's `APIError` onto the §6.3
  envelope. It is not a Hono exception, so it was reaching clients as `internal`.
- server/src/http/middleware/logger.ts — `redactPath()`; `/api/v1/invites/:token`
  no longer writes the plaintext token into the access log.
- server/src/app.ts, server/src/index.ts — mount the routes, thread `LAIKA_PUBLIC_URL`.
- server/test/services/invites.test.ts — new, 38 tests.
- server/test/http/routes/invites.test.ts — new, 28 tests.
- server/test/http/middleware.test.ts — `redactPath` cases.
- server/test/helpers/auth.ts — `sqlite` and `publicUrl` wiring; injectable `now`
  so a test can reach the window between validating an invite and spending it.

**Decisions**
- **Consumption lives in better-auth's sign-up hook, not in the accept route.**
  `POST /api/v1/auth/sign-up/email` is public and already took an `inviteToken`
  (D-004's gate reads it) — it validated the invite and consumed nothing, so the
  invitee landed on the default `member` role and the token stayed spendable for
  ever. An accept endpoint that consumed invites on its own would have been a
  polite door beside an open window. Rejected: doing it in the route and leaving
  the public path alone, which is what "build the accept endpoint" literally asks
  for and would have shipped the replay hole AC3 exists to close.
- **`user.invite` for create, list and revoke; plus `user.set_role` on create.**
  §3.1's row names one capability, unlike "List / revoke **anyone's** token" which
  the actions list splits because that row names two operations. The second check
  is how `(not to Owner)` is enforced — rejected leaving it out, which lets an
  Admin mint an Owner account and makes the caveat on the direct role change
  decorative.
- **Revoke deletes the row.** Rejected adding `revoked_at`: §4.11 has no such
  column, so `schema.ts` would go ahead of the spec and LAI-051 would go red —
  the exact failure PM hit with §4.16 this week. A revoked pending invite is
  indistinguishable to every caller from one that never existed and nothing
  references it. An accepted invite answers 409 instead; that row is the record
  of how somebody got in.
- **Accept answers 403 where the preview answers 404**, both uniform across
  unknown / expired / spent. Accepting is an action the token has to authorise —
  and 403 is what the public sign-up path already answers for the same input, so
  the two cannot disagree. Rejected reusing `previewInvite` in the accept route,
  which is what I built first; it made a replay a 404 and split the two paths.
- **The service re-exports `ORG_ROLES` / `PROJECT_ROLES`** so the route can
  validate a role without importing `db/`. The house idiom (`routes/tasks.ts`,
  `routes/sprints.ts`) retypes the tuple in the route file — a fourth copy of a
  closed vocabulary that nothing checks against `db/enums.ts`. Rejected adding a
  fifth. Filed LAI-119 to converge the other two rather than leaving two idioms.
- **7 days, stated in the code with its reasoning.** Matches §4.12's meeting-review
  window, and §11.6 already sweeps both from one cron.
- **No `invite.created` / `invite.revoked` verbs.** §4.8 has none, AC7 asks only
  for `member.added`, and PM has asked that the missing-verb wart stop being filed.
  A decision, not an oversight.

**Discovered**
- **The replay hole was already open on the public path**, and had been since
  LAI-005. `auth.api.signUpEmail` runs `hooks.before`, so the gate fired — but
  nothing marked the invite accepted and nothing read `invites.org_role`. Anyone
  holding one token could have created unlimited `member` accounts. Closed here;
  no separate task because it is AC3.
- **§6.4 already listed four of the five endpoints** (lines 518-519), so AC8's
  premise is mostly false — the same shape as LAI-060 AC5 and LAI-110 AC1. Only
  `DELETE /invites/:id` is missing. → LAI-120 filed (discovered-from: LAI-071).
- **Six of twenty break-probes did not bite**, and each said something true:
  - the replay guard is defended twice (pre-read *and* conditional `UPDATE`), so
    breaking either alone left every test green. Added a test that exercises the
    conditional `UPDATE` with the pre-read stepped around, and confirmed breaking
    **both** fails. The code comment calling the second one "unreachable" is now
    demonstrably accurate rather than hopeful.
  - `resolveInviteForAccept` is a second query with its own `WHERE`; my boundary
    tests only covered `previewInvite` and `consumeInvite`. Real gap, now tested.
  - the orphan cleanup was unreachable in every test, because the `before` hook
    rejects before an account exists. Reached it with a stepping clock — the hooks
    read `now()` twice, so the invite can lapse between validating and spending it.
  - the better-auth `APIError` branch was unreachable too, until I found the case
    that is not hypothetical: **a link invite has no address, so nothing at create
    time can check one** — accepting it with an already-registered address was a
    500 before this branch existed.
  - registration order for `/accept` before `/:token` defends nothing today (they
    differ in method). Hono *does* let a `/:param` registered first shadow a later
    literal — I checked — so the ordering is kept and the comment now says what is
    actually true instead of claiming a collision that cannot happen.
- **zsh does not word-split unquoted parameters.** A `$J` holding curl header
  flags collapsed into one argument and every request silently failed against the
  live server, which read as "setup is broken". Third harness bug this week to
  masquerade as a product bug (after the `code()` helper and bare `wait`).

**Verified against the built server** (`node dist/index.js`, real socket, port 8791):
setup → invite at `admin` → preview and accept **with no cookie at all** → `/me`
returns `org_role: admin` → replay refused `403` on `/invites/accept` **and** on
`/auth/sign-up/email` → `token_hash` matches SHA-256 and the plaintext appears
nowhere in the database file → zero occurrences of the token in `server.log`, which
shows `"path":"/api/v1/invites/:token"` → Admin inviting an Owner refused `403` on
`user.set_role` → revoke `204`, again `404`, accepted invite `409`.

800 tests pass (was 731). Format, lint and typecheck clean.

**State**
Moved to .tasks/review/. AC8 left unticked with its half filed as LAI-120 —
`docs/` is PM's. Nothing blocked.
