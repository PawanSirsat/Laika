# CHIEF — 2026-09-18

## 2026-09-18T18:40:00Z — [ops] drag-and-drop task reordering

**What changed**
- docs/DECISIONS.md — D-060: manual card order is a stored per-project
  `position`; a drag is the only thing that writes it.
- .tasks/backlog/LAI-472-task-position-and-reorder-endpoint.md — CORE's half:
  the `position` column, `POST /api/v1/tasks/:id/reorder`, lane order read from
  it, migration that preserves the current visible order.
- .tasks/backlog/LAI-473-drag-to-reorder-cards-in-a-lane.md — SHELL's half: the
  drag, the drop indicator, the keyboard path, optimistic move with rollback,
  feed suppression. `depends-on: [LAI-472]`.
- CLAUDE.md §3 — the id sweep and the duplicate-check sweep both replaced; the
  old ones could not see a whole class of task file.
- .claude/skills/laika-workflow/SKILL.md — same fix in the copy of the sweep
  that lives in the skill.

**Decisions**
- **Two tasks, not one, and the SPEC row lands with the column.** This is
  CLAUDE.md §4.4 — `schema-spec-drift.test.ts` reads every §4 table against
  `schema.ts`, so adding the §4.5 `position` row now would redden `master`
  before any column exists. The two SPEC lines are quoted verbatim in LAI-472
  and I apply them at merge time (§4.4 step 5), rather than writing them today.
- **No new `ACTIVITY_TYPES` verb.** A reorder writes `task.updated` with
  `{ field: 'position' }` — the shape §4.8 already uses for tags. The
  alternative, a `task.reordered` verb, is **always three owners** (the client
  mirrors the list, the dashboard needs wording), and §4.8's own test fails for
  it: *"when did this card become third in the lane"* is not a question the feed
  is for. The row is still written, because §11.5 names SSE frames after the
  §4.8 type and a second viewer needs one; the **feed suppresses** it instead.
  Storing and displaying are separate questions, answered differently on purpose.
- **One sequence per project, not one per lane.** A per-lane position is only
  meaningful beside a `status`, so every status change would have to invent a
  new one. The cost is that a cross-lane drop must write both status and
  position — named in D-060.3 and made a criterion rather than left to be
  discovered, because without it a card dropped at the top of `TODO` surfaces in
  the middle of it.
- **The representation is left to CORE**, with behaviour pinned instead: one row
  moves, 60 repeated drops into the same gap still work, two concurrent drags do
  not collide. Naming "fractional midpoints" in the task would have been me
  deciding CORE's implementation from a guess about `REAL` precision; naming the
  **trap** lets them choose and proves it either way.
- **The keyboard path ships with the drag, not after it.** A board whose
  ordering is mouse-exclusive has made the order itself unreachable, and
  retrofitting means rebuilding the interaction.

**Discovered**
- **The id sweep in CLAUDE.md §3 is blind to any task file that arrived by
  merge**, which is how every cross-session filing reaches `master`.
  `git log --name-only` prints no file list for a merge commit. I filed these
  two tasks as LAI-416 and LAI-453 — both "lowest free" by that sweep, both
  already taken, both tracked in `.tasks/backlog/` on `master`. Renumbered to
  LAI-472 / LAI-473 before the first commit, so nothing references the dead ids.
  Measured:

  | sweep | ids | misses |
  | --- | --- | --- |
  | `git log` alone | 315 | `LAI-416`, `LAI-453` |
  | `ls-tree` over every branch | 315 | `LAI-114`, `LAI-117` |
  | union | 317 | — |

  Neither half contains the other, so the fix runs both. **This is §2 step 3's
  lesson arriving three weeks late in a different paragraph** — that check was
  moved off `git log` for exactly this reason (*"History says who touched it;
  only the current path says who has it"*) and the id sweep was left behind.
- **It was `task-file-state.test.ts` that caught it, not me** — the repo's own
  id-collision guard failing against the repo's own instructions. Worth saying
  plainly: the gate caught a defect in the working agreement, which is the
  strongest argument for running it on a `docs/`-and-`.tasks/`-only change.
- **I wrote two unmeasured numbers into CLAUDE.md** (280 / 316) and replaced
  them with measured ones (315 / 315 / 317) before committing. §3 already
  carries a paragraph about exactly this — *"every wrong number here was a grep
  that counted the wrong cells"* — and I did it inside the edit that cites it.
- **The `git log --all --format='%s'` half of the new sweep caught nothing** the
  other two missed. Kept as insurance for an id that reaches a commit message
  without ever being a file, and **labelled in CLAUDE.md as insurance rather
  than evidence**, so the next reader does not credit it with a catch.
- Cross-lane drag already works — `KanbanView.tsx` has the drop handlers and
  `TaskCard.tsx:62` is already `draggable`. LAI-473 is an addition, not a
  rewrite, and the task says so to stop someone reaching for a DnD library.
- The board's `updated_at asc` lane order is the LAI-260 sidebar defect one
  screen over. Same owner complaint, same shape; recorded in D-060 rather than
  filed again.

**State**
Two tasks in `.tasks/backlog/`, unclaimed, LAI-472 ready and LAI-473 blocked on
it. Gate green on `master` — `TEST 0 / LINT 0 / FMT 0`, 106 files, 1938 tests.
Not pushed. Next: CORE claims LAI-472; I apply the two SPEC lines when its half
merges.

## 2026-09-18T19:10:00Z — [ops] the sidebar shows two spaces

**What changed**
- docs/DECISIONS.md — D-061: two recent spaces, not three. Amends D-059.1;
  D-059's other three points untouched.
- docs/SPEC.md §11.4.2.1 — the sidebar row, `three recent spaces` → `two`.
- .tasks/backlog/LAI-474-sidebar-shows-two-spaces.md — SHELL's task.

**Decisions**
- **I edited the SPEC line now rather than at merge.** §4.4's hold-the-docs-half
  procedure exists for changes where each half reddens the gate alone; **nothing
  parses §11.4 prose**, so neither half does. Holding it would have bought
  nothing and left a spec line contradicting a decision recorded two paragraphs
  away. Said so in D-061 and in the task, because a spec line no test can
  falsify is the kind that goes stale silently.
- **`More spaces` stays unconditional.** With exactly two projects it would
  vanish if made conditional, and the popover is the *directory*, not the
  leftovers. Written into the task as a "do not", because it is the obvious
  tidying-up someone would do unasked.
- **Named the three tests that hardcode `3`, with line numbers**, rather than
  writing "update the tests". Most assertions read `RECENT_LIMIT` and follow the
  constant for free; the literals are the ones asserting the *old design*, and
  they are the whole of the work. A criterion that cannot be reviewed without
  re-deriving which tests were meant is not a criterion.
- **Called out that the browser reload test's *purpose* must survive its edit.**
  It distinguishes storage being read back from the fill-up loop refilling by
  chance, and reasons from *"four projects and three slots"*. At two slots it can
  still do that, but only if the expected set is re-derived — the cheap fix is
  to delete a slug from the expectation, which leaves a green test that no
  longer tells those two apart.
- **Pinning is pre-empted, in D-061 and in the task.** If a two-slot list churns
  too much, a third slot is the wrong answer and pinning is the right one — said
  now so nobody reaches for it inside this task.

**Discovered**
- **The whole SPACES sidebar is on `shell`, not `master`.** `git grep` for
  `recentSpaces` on `master` returns nothing. Everything I cited lives on the
  builder's branch, which is why the task quotes `shell:`-qualified paths.
- **SHELL has 22 tasks sitting in `.tasks/review/`** — LAI-248 through LAI-279,
  including the two this touches. That is a review queue I have not worked, and
  it is the largest single risk on the board right now: LAI-474 changes code
  whose own task has not been accepted yet. **Not filed as a task** — clearing
  the queue is my standing job, not a discovery.
- **`RECENT_LIMIT` is genuinely one constant**, read by both the display slice
  and `promote()`'s storage cap. Rare; usually a number like this is spread.
  Credit to LAI-248 for naming it.
- **Nothing binds SPEC §11.4.2.1 to the code.** §4's schema tables have
  `schema-spec-drift.test.ts`; §11.4's prose has nothing. So the sidebar row
  could have said "three" for ever after the code said two, and no gate would
  have known. Worth a guard, but a guard over prose is a real design question —
  **not filing it on a hunch while mid-task.**
- **The owner is overriding their own D-059.1**, which itself came from reading
  the live design. Recorded as an amendment rather than a correction: the
  earlier reading was not wrong, the want changed.

**State**
LAI-474 in `.tasks/backlog/`, unclaimed, p1, no dependencies. Gate green on
`master` — `TEST 0 / LINT 0 / FMT 0`, 106 files, 1938 tests. Not pushed.
Next: SHELL's review queue is 22 deep and wants clearing before more is filed
into it.
