# SHELL — 2026-09-01

## 00:25 +05:30 — LAI-412 released, LAI-423 nav drops the project → review

**Released LAI-412** back to `.tasks/backlog/` on CHIEF's reprioritisation, with
`assignee: unclaimed`. Its **client half is already built and committed** — API
module, budget, spec-quoted copy, 18 tests — and the task file now says so in a
table, because the next person's first instinct will be to write it again.

**Changed (LAI-423)**
- `server/web/src/routes/nav-url.ts` — **new.** `navHref`, `withProjectParam`.
- `server/web/src/api/pick-project.ts` — **new.** One fallback rule.
- `server/web/src/routes/route-table.ts` — `orgLevel?: true`, marked on
  `/organisation`, `/capacity`, `/tokens`.
- `server/web/src/components/Sidebar.tsx` — builds the href, and navigates to
  the same URL it advertises.
- `BoardScreen.tsx`, `sprints/SprintsScreen.tsx`, `timeline/TimelineScreen.tsx`,
  `dashboard/DashboardScreen.tsx` — one shared rule, written into the URL.
- `server/web/test/routes/nav-url.test.ts`, `test/api/pick-project.test.ts` —
  **new**, 19 tests. `test/routes.test.ts` — one assertion corrected.

**Decisions**
- *Most recently active, not most recently opened.* `last_activity_at` is
  already on every `ProjectView`, so it costs no request and answers "where is
  work happening" rather than "what sorts first". Rejected `localStorage`: the
  Notes are right that the selection lives in the URL, and a stored fallback is
  the ambient selection under another name — two browsers would disagree about
  what a bare `/board` means.
- *`/members` asks rather than resolving.* AC2 permits either. A board with no
  project is better resolved than refused; a members list silently scoped to a
  project you did not pick is a permissions screen showing the wrong
  permissions. Flagged on the task for CHIEF to overrule if they disagree.
- *`orgLevel` marks the exceptions, carrying the project is the default.* The
  same inversion as `Route.public`: forgetting to mark a new route keeps the
  reader's context, which is harmless. The opposite default loses it silently,
  which is this defect.

**Discovered**
- *An existing test asserted the presence of the bug.* `routes.test.ts` "nav
  items are real links" matched `href={route.path}` **verbatim**. Its stated
  intent — anchors, not click-handler divs — is right, but it pinned the exact
  expression that was the defect, so it would have **blocked the fix while never
  catching the bug**. Rewritten to assert an `href` expression plus the absence
  of a div-with-onClick. Same shape as the `nav-truth.test.ts` gap the task
  names, one level down: green, specific, about the right subject, and pointed
  at the implementation instead of the property.
- *`BoardScreen` could not use the shared rule at all.* It imported a **second**
  `listProjects` — `api/tasks.ts:263` returns a narrow `ProjectSummary` with no
  `last_activity_at` and no tombstone handling. Two client functions, same name,
  one endpoint. Board now uses the real one; filed as **LAI-226** rather than
  collapsed here. `view-type-drift.test.ts` does not catch it because
  `ProjectSummary` is paired with nothing.
- *Typecheck caught four things the tests could not.* Two screens destructured
  only `params` and not `setParams`; three imports went unused; and Board's type
  mismatch is what exposed LAI-226. `node --test` never loads a `.tsx`, so none
  of it was visible to 518 passing tests.

**Verification**
- 518 web, 1303 server, typecheck, lint, `format` clean.
- **Verified by clicking**, with two projects where the rules disagree — `atlas`
  sorts first, `laika-core` is most recently active. Every nav click keeps the
  project; every bare entry point resolves to `laika-core` and rewrites the
  address bar. Org-level links stay bare.
- **Five mutations, five reds**, anchors asserted before mutating, baseline
  confirmed green first.

## 01:05 +05:30 — LAI-424 the card hit area → review

**Changed**
- `server/web/src/routes/screens/board/board.css` — `.card` positioned and
  `cursor: pointer`, `.card:hover`, `.card-open::after` (the stretched hit
  area), `.card-above`.
- `server/web/src/routes/screens/board/TaskCard.tsx` — `card-above` on the
  elements carrying tooltips.
- `server/web/src/routes/screens/BoardScreen.tsx` — the open task reads from
  `?task=` instead of `useState`.
- `server/web/test/routes/screens/board/card-hit-area.test.ts` — **new**, 10
  tests, run red against the unmodified card first.

**Decisions**
- *Stretch the existing button, do not add a second handler.* The obvious fix —
  `onClick` on the `article` — gives assistive tech two ways in and fires twice
  for anyone using both. Stretching `button.card-key` with `::after { inset: 0 }`
  keeps one control, one Tab stop, one activation.
- *`cursor: pointer` on the design's authority, not mine.* The design file uses
  it 46 times and `grab` never. `grab` was ours: honest about dragging, silent
  about what people actually do. Dragging still works.
- *No shadow on hover.* I wrote `var(--shadow-lift, var(--shadow))` and removed
  it — there is no `--shadow-lift`, so the fallback would have resolved to the
  same shadow, a no-op dressed as a change, and inventing a token is not mine
  (D-020).
- *`?task=` done here rather than filed.* `setParam` already existed, so it was
  three lines.

**Discovered**
- *A test satisfied by its own explanation.* My cursor assertion matched a
  **comment** inside the `.card` rule reading "the design file uses
  `cursor:pointer` 46 times", so it stayed green when the declaration was
  mutated back to `grab`. I only found it because the mutation's red never
  arrived — the check CHIEF asked me to keep. `ruleFor` now strips comments
  before matching. **Any source-text assertion can be satisfied by prose about
  the thing instead of the thing.**
- *The CSS was right and the geometry was wrong.* I added
  `.card-key { position: relative }` for the focus ring; it made the key the
  containing block for its own overlay, so `inset: 0` sized the hit area back to
  the key — restoring the 3.6% while **every CSS assertion still passed**. Only
  `elementFromPoint` in a browser caught it.
- *AC3 described controls that do not exist.* The `+`, avatar and blocker are
  `<span>`s with tooltips and no handlers; the card's only interactive element
  was the key. Their real requirement is that the overlay must not swallow their
  **tooltips**. Said so on the task rather than inventing controls to satisfy the
  wording.
- *Playwright refusing to click was the proof.* It would not click `.card-title`
  because `button.card-open` "intercepts pointer events" — which is exactly the
  fix working. Clicking `article.card` succeeded, because the interceptor is a
  descendant.
- Filed **LAI-227**: no web test in this repo can click anything, because
  `node --test` cannot import a `.tsx`. Two p1 interaction defects in a row and
  528 passing tests said nothing about either. The task names the two candidate
  harnesses and points out that jsdom would have caught **neither** of today's
  mistakes, since it has no layout.

**Verification**
- 528 web, 1303 server, typecheck, lint, `format` clean.
- Browser: card body → panel opens and URL gains `?task=`; that URL reloaded →
  panel open; Enter on the focused key → opens once; `+` → tooltip, no panel;
  `draggable` still true. Both themes.
- **Six mutations, six reds**, anchors asserted before mutating, baseline
  green first.

## 01:45 +05:30 — LAI-425 board band order and nav order → review

**Changed**
- `server/web/src/routes/screens/BoardScreen.tsx` — `SprintStrip` moved above
  `ScreenHeader`.
- `server/web/src/routes/route-table.ts` — Timeline before Sprints in WORK.
- `server/web/test/routes/nav-truth.test.ts`, `test/routes.test.ts` — both
  order expectations edited deliberately.
- `server/web/test/routes/screens/board-bands.test.ts` — **new**, 2 tests.

**Decisions**
- *Measured the prototype instead of trusting the task's table.* Served
  `docs/design/` on a local port (Playwright blocks `file:`) and read band
  positions out of the prototype at 1600×1100. It agreed — but the point is that
  it was checked. Prototype sidebar measured too: Board, Timeline, **Calendar**,
  Sprints, **Capacity**, … so Board → Timeline → Sprints is what remains once the
  two out-of-scope entries are removed.
- *Left the theme control alone and said why on the task.* Matching the
  prototype's single `☾ Switch to dark` button removes **System**. That trades a
  real capability for visual fidelity; the owner decides, not me.
- *Added a band-order guard.* There was none — the inversion had shipped
  unnoticed. Source order, since `node --test` cannot render a `.tsx` and so
  cannot read a computed `y`.

**Discovered**
- *An empty project looks exactly like a broken change.* My dev instance had no
  sprints, so `SprintStrip` rendered nothing and the board's children were
  header → presence → lanes — indistinguishable from the move having failed. I
  created two sprints and the strip appeared, first. This is the same shape as
  the defect the task is about: **a screen missing its data looks like a screen
  that is broken**, which is exactly what LAI-423 did to the owner.
- *Both nav guards noticed the reorder before I finished it.* `nav-truth.test.ts`
  and `routes.test.ts` each pin the order with `deepEqual`. AC4 asked whether the
  first would; it did, and so did a second I had not been pointed at. Both
  expectations then edited deliberately — that is what those lines are for.

**Verification**
- 530 web, 1303 server, typecheck, lint, `format` clean.
- Band order and strip surfaces measured in **both themes** through the real
  theme control; order identical in each.
- **Two mutations, two reds** — header before strip, and Sprints before
  Timeline. Baseline green first.

## 02:20 +05:30 — LAI-426 timeline task rows → review as **not building**

**Changed**
- `server/web/src/routes/screens/timeline/TimelineScreen.tsx`,
  `timeline-derive.ts`, `timeline.css`, and the derive test — built
  (`6d3e1ed`), then reverted.
- The subtitle change is **kept** and adapted to the sprint-per-row render.

**Decisions**
- *Built it as specified before judging it.* CHIEF asked for both renders side
  by side and explicitly said not to tune it. A complete implementation with a
  green gate, reported as a failure, is a more useful artefact than an argument
  — it took one round instead of five.
- *Left every criterion unticked.* They were all met by the reverted commit.
  Ticking them would claim the screen was delivered; it was not.
- *Kept the implementation in history rather than squashing it.* The next person
  to open the prototype's timeline will propose this again, and `6d3e1ed` plus
  the measurements is the answer.
- *Did not move it to `.tasks/done/` as instructed.* CLAUDE.md §6: a builder
  never does. It is in `review/` with the full record for CHIEF to close. Said
  so plainly rather than silently doing something different.

**Discovered**
- *The decisive fact was one I did not have when I raised the concern.* I argued
  that identical bars waste vertical space. The stronger fact is that **a task
  belongs to exactly one sprint, so a sprint-derived bar cannot represent a task
  crossing a sprint boundary at all** — `LAI-152`, 15–24 Aug, starts in S3 and
  ends in S4, and `LAI-131` spans the S1→S2 gap. That moves it from a trade to
  an impossibility, and I only found it by rendering the prototype and reading
  its bars rather than reasoning about the design.
- *17 bars, 2 distinct geometries* against the design's 13 and 13, measured off
  both rendered pages. The number is what made it undeniable in one round.
- *The reverted render was worse than what it replaced, not merely equal.* It
  asserts precision the data lacks — crisp edges on dates that describe a sprint
  — and costs ~2.5 screens of scrolling at 41 tasks to read two sprint dates.
- *Finding the prototype's timeline took longer than building the screen.* It is
  rendered by JS behind a nav click, so grepping the file for its data model
  found nothing and I briefly concluded the design had no timeline. Clicking its
  own nav was what worked. **A design file that renders one screen at a time
  cannot be read with `grep`.**

**Verification**
- 530 web, 1303 server, typecheck, lint, `format` clean after the revert.
- Reverted screen re-rendered: 2 sprint bars, 0 task rows, legend, tray and the
  TODAY marker all present, and the new subtitle on screen.

## 02:45 +05:30 — LAI-412 project context editor → review

**Changed**
- `server/web/src/routes/screens/projects/ProjectContextPanel.tsx` and
  `project-context.css` — **new**, the slide-over.
- `server/web/src/routes/screens/ProjectsScreen.tsx` — a `Context` button per
  card, the panel, and a `me` prop for the lead+ decision.
- `server/web/src/components/AppShell.tsx` — passes `me`.
- `server/web/src/api/project-context.ts` — `readableContextError` now reads
  both `422` shapes and takes the length.
- `server/web/test/api/project-context.test.ts` — two tests for the zod shape.

**Decisions**
- *A slide-over on Projects, not a route.* SPEC §11.4.2 maps
  `get_project_context` to Projects, and the task detail is the precedent for a
  panel that is deliberately not a nav destination. No route-table or sidebar
  change, so no nav entry for something that is not a screen.
- *A viewer gets no Save button at all*, not a disabled one, plus a line saying
  editing needs `lead`. A disabled control still advertises a capability.
- *`beforeunload` for the browser's navigation, `confirm` for in-app closing.*
  Two different exits and only one of them is ours to intercept.
- *The client supplies the length the server omits* — marked with LAI-228 so it
  is removed when the server is fixed, rather than quietly becoming permanent.

**Discovered**
- *AC4 was unmeetable as the server stands, and only rendering showed it.* §7.3
  wants the refusal to name the limit **and the actual length**;
  `updateProjectContext` raises exactly that and **it is unreachable over REST**
  because the route's zod `.max(CONTEXT_MD_LIMIT)` refuses first. On screen it
  read *"Invalid request body"*. Filed **LAI-228**.
  The general shape is worth keeping: **a bound enforced in two layers is
  enforced by whichever runs first**, and here the winner is the one carrying
  less information. The schema's own comment claims the bound is the service's.
- *My unsaved-changes guard proved itself by breaking my verification.* Browser
  navigation began timing out; the cause was `beforeunload` waiting on a confirm
  dialog because the textarea held 100,400 unsaved characters. AC5 verified by
  being obstructed by it.
- *A `.tsx` needs no mirrored test file.* The structure test requires the
  **directory** to exist under `web/src/`; only `.ts` modules need a mirror. So
  the panel has no test of its own and the suite is content — which is the
  LAI-227 hole again, not a gap I can close here.

**Verification**
- 532 web, 1303 server, typecheck, lint, `format` clean.
- Browser, both themes: load 741 chars from the real endpoint; save 741 → 785
  with author and timestamp updating; budget quiet at 50%, amber at 92% with
  `8,000 left`, red at `400 over`; refusal reads *"Too long by 400 characters —
  100,400 of 100,000 allowed."*; viewer read-only with no Save; empty project
  shows the §7.3 guide and `Never edited.`

## 03:15 +05:30 — LAI-410 token management screen → review

**Changed**
- `server/web/src/api/tokens.ts` — **new** client: list, create, revoke,
  `forcedTokenScope`, `mayChooseScope`, `tokenState`, `lastUsedLabel`.
- `server/web/src/routes/screens/tokens/TokensScreen.tsx` + `tokens.css` — **new**.
- `server/web/src/routes/route-table.ts` — `/tokens` is `ready`, and moved
  before Organisation to match the prototype's SETTINGS order.
- `server/web/src/components/AppShell.tsx` — mounts it.
- `server/web/test/api/tokens.test.ts` — **new**, 13 tests.
- `test/routes/nav-truth.test.ts`, `test/routes.test.ts` — both nav
  expectations edited deliberately.

**Decisions**
- *A viewer gets no scope control at all, with a sentence saying why.* The
  server **forces** rather than refuses, so an offered choice would not error —
  it would mint something other than what was asked for. A test asserts
  `mayChooseScope` and `forcedTokenScope` agree, so nobody can be offered a
  choice the server overrides.
- *A revoked token stays on the list, dimmed, with its date.* Removing it would
  hide that it ever existed.
- *`/tokens` earns its nav entry now the screen exists* — the LAI-082 rule
  working rather than an exception to it.

**Discovered**
- *The once-only secret was not once-only.* Dismissing removed it from the DOM,
  `innerText` and both storages — and **left it in React state**, at hook 9, as
  `{ token, secret }`. React double-buffers: a hook's previous value stays
  reachable until another render replaces it. Fixed by blanking **through**
  state — `{ token, secret: '' }` and only then `undefined` — so the retained
  render holds an empty string.
- *A probe that looked at nothing reported "clean", and I nearly believed it.*
  My second scan walked `root.current`, but the `__reactContainer` key **is**
  the root fibre, so `.current` was `undefined` and it scanned zero fibres. It
  returned an empty result that looked like a pass. Caught only because probes
  one and three disagreed with it. **Confirming a negative requires proving the
  probe can see a positive** — the final version asserts it finds the secret
  while one is on screen (148 fibres) before asserting it cannot afterwards.
  This is the same family as the console-vs-server-log confusion on LAI-411 and
  the `grep` on the design file yesterday, and it is now three for three.
- *Four prefix characters are enough.* CHIEF asked me to stop and say if not.
  Four tokens on one account gave `Qb5L`, `Nyc2`, `iRMK`, `Zg0D` — distinct and
  distinguishable at a glance, with the name doing the real identifying. SPEC
  §4.9 needs no amendment; reported rather than worked around.

**Verification**
- 545 web, 1349 server, typecheck, lint, `format` clean.
- Browser, both themes: viewer sees no scope control and gets a forced
  `read_only` token; owner sees two; secret shown once and gone from DOM, text,
  storage and React state after dismissal; `Never used` on a fresh token.

## 03:55 +05:30 — LAI-413 unlisted work triage → review (M3's last task)

**Changed**
- `server/web/src/api/unlisted.ts` — **new** client, `mayTriageUnlisted`,
  `unlistedState`, `mayPromote`.
- `server/web/src/routes/screens/unlisted/UnlistedScreen.tsx` + `unlisted.css` —
  **new**.
- `server/web/src/routes/route-table.ts` — `requires?: 'audit_log.export'`, the
  `/unlisted` route, and `routesInGroup` / `navRoutes` take a `holds` predicate.
- `server/web/src/components/Sidebar.tsx`, `components/AppShell.tsx` — the
  actor's role reaches the nav.
- `server/web/src/routes/screens/screen-copy.ts` — the queue's empty state.
- `server/web/src/routes/screens/tokens/TokensScreen.tsx` — placeholder
  `mira-cli` → `my-laptop` (CHIEF's request; the mockup's persona).
- `test/api/unlisted.test.ts` **new**; `test/routes/nav-truth.test.ts` gains six
  gating tests and one widened guard.

**Decisions**
- *The route names a permission, not a role.* `requires: 'audit_log.export'`
  rather than `requiresAdmin`, because admin-up is what that action resolves to
  **today**. One mirror of `policy/can.ts` (`mayTriageUnlisted`), used by both
  the sidebar and the screen.
- *A missing predicate hides gated entries rather than revealing them.* The
  default a forgetful caller gets must be the safe one; the endpoints answer
  `403` regardless, so a visible entry could only mislead. Tested explicitly,
  because it is the mistake this design invites.
- *Promoted beats dismissed in `unlistedState`.* A row that became a task and
  was later dismissed still produced work someone can open.
- *The promotion link is remembered in component state.* The row carries
  `promoted_task_id` — an id — and a person needs the **key**, which only the
  promote response returns.

**Discovered**
- *A second over-specific guard, same family as LAI-423's.* `nav-truth.test.ts`
  matched `routesInGroup(group)` **verbatim** to assert that empty groups are
  skipped. Adding the permission argument failed it with the behaviour
  unchanged. Widened to match the property. **That is twice now in nav tests:
  an assertion that pins one spelling of a call rather than what the call must
  do.** Worth watching for a third.
- *The sidebar had no permission concept at all.* Every nav decision until now
  was "does a screen exist" (LAI-082). This is the first entry where the answer
  differs per reader, and it needed a new field rather than a special case.

**Verification**
- 559 web, 1354 server, typecheck, lint, `format` clean.
- Browser, both themes, as **owner and as viewer**: owner sees the entry and
  2 pending rows; "Show dismissed" reveals a third that offers no promote;
  promoting created **LAI-18** and the row flipped to `promoted` with an
  `Open LAI-18` link and no second promote; the viewer's nav has no entry at
  all and `GET /unlisted` answers `403` for her.

## 05:10 +05:30 — LAI-227 a web test that can click → review

**Changed**
- `server/web/package.json` — `playwright@1.62.1`, pinned. `pnpm-lock.yaml`
  follows.
- `server/web/test/browser/harness.ts` — **new**: built SPA over a loopback
  server on an ephemeral port, stubbed API, one shared Chromium per file.
- `server/web/test/browser/card-click.test.ts`,
  `test/browser/nav-keeps-project.test.ts` — **new**, the two regressions.
- `server/test/tooling/structure.test.ts` — `browser/` named alongside
  `helpers/` as a directory organised by *how* it tests (my section, D-026).

**Decisions**
- *`playwright` as a library, not `@playwright/test`.* A library adds a
  capability; a framework adds a way of writing tests, and this repo has two
  runners already. The browser tests are `node:test` like everything else.
- *The API is stubbed; the server is not booted.* These test what the client
  renders and how it answers a click. Booting `@laika/server` would make this
  suite own a database, and drift between stub and reality is what LAI-213
  already guards in both directions.
- *An unstubbed route 404s loudly.* A missing fixture must not look like an
  empty screen — that is this week's recurring failure and the harness should
  not manufacture it.
- *`browser/` is named in the structure rule rather than exempted.* Mirroring
  `src/` would scatter three files and hide what matters about them: they are
  the slow ones that launch Chromium.

**Discovered**
- *I got the cost wrong, generously.* I told CHIEF "seconds to tens of seconds".
  It is **1.07s → 1.48s** for +3 tests, and 2.42s from a fresh checkout with no
  build output. One browser per file, SPA already built, headless launch ~300ms.
  The estimate should not be quoted; the measurement should.
- *Two shell mistakes while proving the reds, and both produced results that
  looked valid.* `$PRE:server/...` — zsh read `:s` as a substitution modifier,
  so `git show` and then the build failed and the test went red **for the wrong
  reason**. Then `for f in $FILES` — **zsh does not word-split unquoted
  variables**, so the loop never ran, the pre-fix files were never installed,
  and the test **passed**. A green that proved nothing. Both redone with the
  iteration in Python.
  I have hit the zsh word-splitting one before and logged it; hitting it again
  is the argument for doing this kind of loop in Python by default.
- *The structure test caught the new directory immediately*, which is the rule
  working — `test/browser/` has no `src/browser/` to mirror, and saying why in
  the rule is better than adding a silent exemption.

**Verification**
- 562 web, 1360 server, typecheck, lint, `format` clean.
- **Both regressions shown red against the real pre-fix commits** — `e14f246`
  for the card hit area (*"the pixels over the card title do not belong to the
  open control"*) and `e1b6192` for the nav (*"the href drops the project:
  /sprints"*) — then green again after restoring. Files restored via
  `git show`, SPA rebuilt each way.

## 05:40 +05:30 — LAI-141 the web test script now typechecks → review

**Changed**
- `server/web/package.json` — `"test": "pnpm run typecheck && node --test …"`,
  plus `test:watch`.
- `server/web/test/browser/harness.ts` — builds the SPA **every run**, into a
  **temp directory**, and asserts the build produced an `index.html`.

**Decisions**
- *A composite in `package.json`, not a `pretest` hook.* The server's reasoning
  holds: a hook is invisible at the call site, and the defect was precisely a
  check that was not where people look.
- *Build every run rather than assert freshness.* CHIEF preferred an mtime
  assertion and I did the other one, on measurement: an unconditional build is
  **0.42s** against a 1.8s suite, and it needs no heuristic that can be wrong in
  either direction. Building into a temp dir also means a test run cannot
  disturb a dev server serving `server/public/`, and leaves nothing stale behind.
- *Quoted my own proportional cost rather than the server's.* +12% there, **+88%
  here** — the same fixed ~1.8s typecheck against a much smaller suite. Reusing
  the server's percentage would have been true of a different repo.

**Discovered**
- *The harness could test week-old code, and CHIEF found it by using it.* Their
  `server/public` was from 25 August, so the browser tests served a stale bundle.
  **Green on broken code** is the direction that matters, and it is the same
  hole my loud-404 decision closes one level up: a fixture that cannot be found
  must not look like an empty screen, and a build that is not current must not
  look like passing code. I designed out one and shipped the other in the same
  file.
- *Proof the hole is closed:* breaking `inset: 0` in `src/` **without building**
  now fails the card test. Before, it passed against the cached bundle.
- *`$?` after a pipeline is the pipeline's exit code, not the command's.* My
  first attempt to show the type-error failure printed `exit code: 0` while
  `ELIFECYCLE` was plainly in the output. Re-ran with the exit captured directly:
  **2**, with **zero** test-summary lines — the tests never ran. Small, but it is
  the same family as everything else this week: a measurement that reads as a
  pass because it measured the wrong thing.

**Verification**
- 562 web, 1360 server, typecheck, lint, `format` clean.
- Type error introduced and removed: exit `2`, `src/api/tokens.ts(109,9)` named,
  no tests run; clean run exits `0` with 562 passing.
- `server/public` deliberately absent for a full run — 562 pass, and the
  directory is **not** recreated.

## 06:05 +05:30 — LAI-429 the web half of `blocked_by` → review (§4.4 landing)

**Changed**
- `server/web/src/api/tasks.ts` — `TaskView.dependencies` → **`blocked_by`**.
- `api/board-derive.ts`, `routes/screens/board/{TaskCard,TaskDetailPanel,ListView}.tsx`,
  `routes/screens/dashboard/dashboard-derive.ts` — every read.
- Four test files — fixtures and test names.
- `test/browser/card-click.test.ts` — a new test that a blocked task still
  renders as blocked, in both themes.

**Decisions**
- *Left LAI-213 red and stopped.* It fails in **both** directions —
  `TaskView.dependencies` served but undeclared, and `Task.blocked_by` declared
  but unsent. That is the check binding the two halves; exempting it would
  silence the one test this landing exists to satisfy. Not touched.
- *Rewrote the prose rather than substituting it.* A blunt replacement left
  *"The blocked_by that are not done yet"*. A comment that survives a rename
  mechanically is one nobody reads twice.
- *Asserted the exclusions rather than trusting the file list.* The rename
  checks that `invite-roles.ts`, `states.test.ts` and `use-board-filter.test.ts`
  still contain `dependencies` **and contain no `blocked_by`**, so a future
  broad substitution cannot quietly take them.

**Discovered**
- *`test/` held two more excluded occurrences than the task listed*, because the
  task scoped its count to `src/`. They are the other two categories exactly:
  *"zero test dependencies"* (npm) and *"it must be in the dependencies"* (a
  React hook's array). Both left. Worth noting for CHIEF's count: **16 in `src/`
  is right; the whole-package figure is larger and includes two that must not
  move.**
- *AC4 earns its place.* `blocked_by` feeds `blockedState` and `blockers`, so a
  card that stopped showing "blocked by" would typecheck perfectly. The browser
  test is mutation-proven: making `blockedState` never report blocked turns it
  red. This is the first task where LAI-227's harness caught something a
  compile could not — which is the argument I made for buying it.

**Verification**
- **561 of 563 passing; the 2 failures are LAI-213, deliberately.** 0 type
  errors, lint and format clean.
- The blocked card verified in both themes through the real theme control.

## 06:40 +05:30 — LAI-121 the client Task catches up → review (four-half landing)

**Changed**
- `server/web/src/api/tasks.ts` — `started_at`, `completed_at` (CORE's fold from
  LAI-126).
- `routes/screens/sprints/sprint-derive.ts` — `SprintTask`, `readSprintId`,
  `withSprintIds` **removed**; `groupBySprint` takes `Task`.
- `sprints/use-sprints.ts`, `SprintCard.tsx`, `AssignTasksPanel.tsx` — use
  `Task` directly.
- Four test files — fixtures gain the two fields; the tests for the removed
  functions go with them.

**Decisions**
- *Gated with the repo-root `pnpm test`*, per D-045. The web filter is what let
  a red half look green, and it would have hidden nothing here — but the point
  is the habit, not this run.
- *No timeline change.* The two fields make a task-level Gantt **possible**;
  D-040 says it is not **wanted**. Declaring them is the whole task, and the
  timeline's guard against task-derived values on the axis stays as it is.

**Discovered**
- *CHIEF predicted four drift failures; there are two.* `started_at` and
  `completed_at` do not fail, because **LAI-126's server half is already on
  `master`** — `2d22398` — so they are served *and* declared. Its **task file**
  is still in `.tasks/review/`, which is what made it look unlanded. Worth
  separating: **the code is on master, the task is not accepted.** A reviewer
  checking this half against "expect four" would have concluded it was wrong.
- *Three of the four criteria were already satisfied before I started.*
  `sprint_id`, `TaskFilter.sprint` and the drift test were all delivered by
  **LAI-213**, which built the general guard this task asked for as a special
  case. **The task that asked for the check is being closed by the task that
  answered it** — I ticked them with that written on the task, because a tick
  that reads as new work is a small lie in a place people trust.

**Verification**
- **558 of 560 passing; the 2 failures are LAI-429's drift, quoted on the task
  and named to LAI-099.** 0 type errors, lint and format clean.
- Root `pnpm test`, not the workspace filter.

## 06:20 +05:30 — LAI-147 the client mirrors §4.8's vocabulary → review

**Changed**
- `server/web/src/api/stream-types.ts` — nine verbs, in `ACTIVITY_TYPES`' order.
- `routes/screens/dashboard/dashboard-derive.ts` — nine labels, plus
  `FEED_SILENT` and `shownInFeed`.
- `routes/screens/dashboard/DashboardScreen.tsx` — the feed renders `shown`; the
  counts still see everything; the empty state no longer contradicts the count.
- `test/routes/screens/dashboard/dashboard-derive.test.ts` — two tests.

**Decisions**
- *Nine verbs, not the seven the task names.* It was filed against LAI-113 alone;
  `core` also carries `user.deactivated` and `user.reactivated` from LAI-222, and
  the assertion is **exact**, so seven of nine would unblock neither task. Order
  taken from `git show core:server/src/db/enums.ts`, not inferred.
- *`sprint.tasks_changed` declined, and the decline made legible.* It keeps its
  label — the decision is about display, not vocabulary — and sits in
  `FEED_SILENT` **with a reason**. A silently-absent verb and a deliberately
  declined one look identical to the next reader, and only one is a decision.
- *The counts are not filtered.* "52 events, 3 by agents" is a claim about what
  happened; editing the list must not quietly change the arithmetic beside it.

**Discovered**
- *The dashboard rendered blank, and it was not my change.* `TypeError:
  e.blocked_by is not iterable` — my dev server was still on **pre-LAI-429**
  code, sending `dependencies` to a client expecting `blocked_by`. **Third time
  a stale dev process has given me a false result**, after LAI-410's 404 and
  LAI-411's. Restarting fixed it. Worth noting that this is precisely the
  client/server contract LAI-213 catches at build time — the runtime version of
  it is a white screen.
- *I could not render the declined verb.* `sprint.tasks_changed` is not in
  `master`'s `CHECK` constraint, so the database **rejects the insert** — I
  tried, and got `activity_type_check`. The decline is proven by test and by the
  code path, not by a row on screen; said so on the task rather than implying
  otherwise, and it is worth re-checking when `core` lands.

**Verification**
- **561 of 562 passing; the one failure is `STREAM_TYPES equals ACTIVITY_TYPES`,
  submitted red under D-045** and named to LAI-113/LAI-222. 0 type errors.
- Root `pnpm test`, not the workspace filter.
- **Three mutations, three reds** on the decline: a reason too short to be one,
  `shownInFeed` always true, and the verb declined *and* unlabelled.
- Real dashboard: 52 events, feed renders, count reads 52, filter breaks nothing.

### 06:55 +05:30 — LAI-147 reopened: the parse was at fault, not the mirror

CHIEF assembled the landing, found it red on `STREAM_TYPES equals
ACTIVITY_TYPES`, and said the fix was mine. It was — but not where they thought.

**Their diagnosis was "splits on commas without stripping comments". It does not
split on commas**: it matched `/'([^']+)'/g` — *any* single-quoted run — and
LAI-113's comment inside the array contains "§4.13's indexes". **The apostrophe
in `4.13's` opened a match** and swallowed prose as a type.

That distinction changed the fix. Stripping comments alone would have fixed
today; matching the shape alone would have fixed today; **neither alone is
enough**, because a comment that *names* a real type is correctly shaped. So
`test/helpers/enums.ts` does both, and throws rather than returning `[]` when the
array is missing — an empty vocabulary satisfies every caller vacuously.

**Both mirrors now use it.** The dashboard's regex was already shaped and
survived today, but would not survive a comment naming a type; fixing one and
leaving the other would have left the same trap one file over.

The criterion is `test/helpers/enums.test.ts`, whose fixture carries the hazards
deliberately — an apostrophe in prose, a block comment naming a real type, and a
test that adds another comment and asserts the parse is unchanged.

**Root gate: 568 web pass, 0 fail.** The two remaining failures are in
`server/test/tooling/` — CORE's stale exemption entries, self-expiring
exemptions doing exactly what they are for.

## 07:05 +05:30 — LAI-434 the timeline gets task rows → review

**Changed**
- `timeline-derive.ts` — `taskBar`, `taskActuals`, `sprintSummary`;
  `timelineRange` widens to cover actuals.
- `TimelineScreen.tsx` — one row per task, sprint bands behind, summary strip;
  the old sprint chart and legend removed.
- `timeline.css` — the task track, and the fill/border distinction.
- `timeline-derive.test.ts` — the D-014 guard **replaced** by its successor,
  plus the summary tests. `test/browser/timeline-bars.test.ts` — **new**.

**Decisions**
- *The solid/outline distinction is carried by fill and border style, never by a
  colour token.* Colour carries status; it does not carry
  measured-versus-planned, because that difference has to survive greyscale and
  a colour-blind reader. Verified as computed values in both themes, and
  mutation-proven with the exact forbidden defect.
- *A bar is `actual` only when **both** ends were measured.* An unmeasured end
  demotes the whole bar to the sprint and the row says so.
- *The axis widens rather than bars being clamped.* Clamping would show a start
  date nobody gave us — the one thing D-049 kept from D-014.
- *Removed the old chart and legend* rather than leaving them under the new
  grid; the same sprints twice is worse than either alone.

**Discovered**
- *The fallback is not hypothetical.* `LAI-3` on the running instance is `done`
  with a `completed_at` and **no `started_at`** — it was already `in_progress`
  before LAI-126 landed, so it never passed the first-entry stamp. Legacy rows
  like it are exactly why "both ends or it is a plan" is the right rule rather
  than a fussy one.
- *Screenshots caught two things every test passed on.* The subtitle still said
  tasks are **not** placed on the axis — false in the same commit that placed
  them — and titles truncated to two characters, because avatar/title/key/status/
  range on one line leaves the title nothing. Split into two lines as the
  prototype has it. **Neither is expressible as an assertion I would have
  thought to write; both were obvious the moment I looked at the picture.**

**Verification**
- **Root `pnpm test` exit 0** — 1514 server, 583 web, typecheck, lint, format.
- Four derive mutations, four reds; one browser mutation red with the right
  message.
- Rendered against real data covering all three states, both themes.

## 07:35 +05:30 — LAI-151 the cron verbs → review

**Changed**
- `api/stream-types.ts` — four verbs in `ACTIVITY_TYPES` order.
- `dashboard-derive.ts` — four labels; three added to `FEED_SILENT`, one kept.
- `dashboard-derive.test.ts` — two tests, one for each half of the judgement.

**Decisions**
- *Three declined for **attribution**, not volume.* `sprint.tasks_changed` was
  noise; these carry `actor_kind: system` and no actor, and a feed of "what
  changed and who changed it" has nobody to name. Operator lines belong in the
  activity API, not a dashboard.
- *`task.stale_flagged` kept.* It is the one cron verb a person asks about —
  "why is this marked stale" — and the flag provokes the question.
- *Asserted the keep as well as the declines.* A decision to keep something is as
  easy to reverse by accident as a decision to drop it, and only the drop had a
  guard. Mutation-proven: adding it to `FEED_SILENT` turns the test red.

**Discovered**
- *A mutation that never applied looked exactly like a passing guard.* My first
  attempt anchored on `'meeting_review.expired':`, which appears **twice** — in
  the labels and in `FEED_SILENT` — so the assert failed, nothing was written,
  and the run came back green. I read it as "the guard did not catch it" for a
  moment before noticing the traceback above it. Redone with a unique anchor;
  red as it should be. **A no-op mutation and a hollow assertion are the same
  false result wearing different clothes**, and this is the second time today.
- *Generation would not close this.* Three recurrences invite generating
  `STREAM_TYPES` from `enums.ts`. Worth saying on the task that it would fix one
  of the two red tests: the labels and the `FEED_SILENT` judgement are per-verb
  decisions a person has to make either way.

**Verification**
- 584 of 585 passing; the one failure is `STREAM_TYPES equals ACTIVITY_TYPES`,
  submitted red under D-045 and named to LAI-431. 0 type errors, lint, format.

## 08:20 +05:30 — LAI-422 `npx laika init` → review (M4's exit)

**Changed**
- `cli/src/{index,init,api,config,prompt,failures}.ts` — the whole command.
- `cli/test/{cli,init-e2e}.test.ts` — **19 tests**, where the package reported
  `0` and exited green.

**Decisions**
- *Config to `~/.claude/settings.json`* (§8, D-046), `0600`. Outside the repo is
  the only "never committed" a later `git add -f` cannot undo.
- *No `--password` flag and no way to pass one.* Shell history, `ps`, terminal
  logs.
- *Idempotence checked **before** anything is asked.* Minting and then
  discovering a config exists leaves a stray credential nobody will revoke.

**Discovered — three defects, none visible to a unit test**
- *Each question closed stdin for the next.* Every `ask()` made its own
  `readline` and closed it. **Each call was correct alone; the sequence failed.**
- *The password was echoed in full.* Only a **pty** shows what a user sees. A
  *paused* readline still owns the terminal mode, so `setRawMode(true)` never
  took. **AC2 was the criterion most obviously met on paper and was not met.**
- *`laika init < answers` could not work at all* — a piped readline closes at EOF,
  which arrives mid-fetch. Not just a testing problem: every script and CI use.
  Stdin is drained once now, which is also what let the suite drive the CLI.

**And a fourth, in my own test:** it deadlocked. `execFileSync` blocks the event
loop, so the stub board in the same process could never answer the child. The
symptom was a timeout with no failing assertion — **a hung test that reads as a
hung CLI**. Async now.

**Verification**
- **Root `pnpm test` exit 0** — 1605 server, 585 web, **19 cli**.
- Fresh repo + empty HOME + real pty → token minted, `0600`, then
  `list_ready_tasks` over `/mcp` using only what `init` wrote.
- Idempotence measured on the board: 3 tokens before the second run, 3 after.
- Three of four failure messages produced against real conditions; the fourth
  (`403` on mint) is stub-tested and flagged, because a viewer's token is forced
  to `read_only` rather than refused, so an active account cannot produce it.

## 13:55 +05:30 — LAI-153 → review (taken mid-LAI-418, at a boundary)

**Changed**
- `server/web/src/api/users.ts` — `OrgUser.avatar_color` removed.
- `server/web/test/api/users.test.ts` — the fixture that set it.
- `server/web/src/theme/avatar-color.ts`, `server/web/test/tokens.test.ts` —
  two dead citations of a §4.1 sentence LAI-148 deleted.

**Decisions**
- *Released LAI-418 to backlog rather than holding two claims.* Six accepted
  tasks were held on one assertion; the hooks work is reproducible, they were not.
- *Fixed the second dead citation rather than filing it.* Same defect, my file,
  already in it. Flagged to CHIEF as a scope call **before** doing it, not after.
  CHIEF: *"the criterion named one instance because I had only found one, not
  because the others were out of scope"* — I had read a named instance as a
  boundary.
- *Reported `docs/design/README.md:67` and stopped.* **The file AC3 sends the
  citation to carries the same dead citation.** D-020 makes it CHIEF's.

**Verification**
- Root gate: **1651** server, **585** web, **49** cli; format, lint, typecheck 0.
- **The drift check is load-bearing, measured not assumed**: re-declaring the
  field turns `the client declares nothing the server does not send` red with the
  message the task quotes, and restoring turns it green.

**Discovered — I committed a file with a mutation still in it**

A mutation harness hit a 10-minute timeout and was **killed between applying the
edit and its `finally`**, so `echo "laika: not configured" >&2` stayed in
`heartbeat.sh`. I checked `git status`, saw `?? plugin/hooks/heartbeat.sh`, read
it as clean, and committed.

**`git status` cannot report a modification to an untracked file** — `??` looks
identical whether or not you have edited it since writing it, which is precisely
the case when the harness is testing brand-new code. The suite went red on the
next run and **I misread that too**, because the same run was slower under the
harness's load, so "2 failing" read as a timeout rather than as the assertion it
was. The repo-root gate is what found it, two commits later.

Three separate instruments and each was the wrong one: `git status` could not see
an untracked edit, the harness could not clean up after being killed, and I let a
timing story explain a red I had not read. **The fix is mechanical — checksum the
file against a saved copy, `git add` new files before running anything over them,
and read the assertion before blaming the harness.**

**Ports:** none started. Nothing was run against a board this entry.

## 15:40 +05:30 — LAI-418 → review. M4's last piece.

**Changed**
- `plugin/hooks/heartbeat.sh` (new, `0755`), `hooks.json`, `README.md`.
- `cli/test/plugin-hooks.test.ts` — 30 tests.
- Filed **LAI-230** (the `plugin` workspace entry) and **LAI-231** (the red gate).

**Decisions**
- *`git branch --show-current`, not `git rev-parse --abbrev-ref HEAD`.* See below.
- *The token goes to curl on stdin (`--config -`), never in `argv`.* §8's snippet
  shows `-H`, which is readable in `ps` by anyone on the machine.
- *`Stop` shares PostToolUse's 5 minutes.* §9.3 defines presence as a heartbeat
  in the last five minutes, so a second post inside that window tells the board
  nothing. SessionStart is never throttled — sitting down is the moment it
  matters. The stamp is keyed on repo **and** branch, so moving either posts at
  once.
- *No configurable throttle.* A knob whose only user is a test.

**Verification — real sessions, `--plugin-dir`, my own board on :3371**
- **Four heartbeats from one session** with the stamp held clear from outside:
  SessionStart, two PostToolUse, Stop. **One** from the identical session with
  the throttle left alone.
- **Dead port**: exit 0, 14s against 12s configured. **Unconfigured**: exit 0,
  zero bytes from the hook — confirmed against a **control session with no
  plugin**, which produced the same 157 bytes of Claude Code's own stderr.
- **`repo` verbatim**: this worktree's remote carries **userinfo** and reached
  the board byte-for-byte, and `/presence` resolved it to the project holding
  `PawanSirsat/Laika`. The scp form too. D-043 on a shape no plugin author would
  have thought to normalise.
- Root gate **EXIT 0** — 1701 / 585 / 49, zero `Unhandled|Errors|Failed|not ok`.
- **11 mutations, all red.** Two anchors failed loudly and were re-run by hand.

**Discovered**

*A command can fail and still print a usable-looking answer.* `git rev-parse
--abbrev-ref HEAD` exits **128** on a repository with no commit and writes `HEAD`
to **stdout**. `|| true` swallows the failure, `$(...)` keeps the output, and the
board is told the branch is called `HEAD`. **§8 mandates the fail-silent, and the
fail-silent is what makes the wrong value invisible.** Not a git quirk to route
around — the general rule is that *failed* is not always *empty*.

*I committed a file with a mutation in it.* The harness was killed between the
edit and its `finally`. **`git status` cannot report a modification to an
untracked file** — `??` is the same before and after — and that is exactly the
case when a harness is testing new code. Then I misread the resulting red as
flakiness. Now: checksum against a saved copy, `git add` new files before running
anything over them, and read the assertion before blaming timing.

*My suite hung instead of failing, and only when something was wrong.* Every stub
board was closed on the last line of its test, so it was closed **only when the
test passed**. A throwing assertion skipped the close, the handle held the event
loop open, and `node --test` never exited. It hung the root gate once and two
mutation runs. **A red that presents as a hang is worse than the red it hides**,
because nobody reads a stopwatch as a stack trace. Servers are tracked and closed
in `after()` and `unref`'d at creation: 600s+ → **10s**, failing on the right
assertion.

Three instruments in one day that could not see the thing they were pointed at —
`git status`, a pass-count grep, and a suite that hangs. The fix each time was a
different instrument, not more care.

**Ports:** started my own on **3371** (uptime_ms 3969 on first check, so mine and
not somebody else's), killed it after. Never touched `localhost:3000`.

## 16:45 +05:30 — LAI-157 → review. The third §11.4.1 marker.

**Changed**
- `server/web/src/api/tasks.ts` — `stale_flagged_at` declared, clearing CORE's red.
- `src/routes/screens/board/TaskCard.tsx`, `board.css` — the marker, and two
  layout fixes it forced.
- `src/api/board-derive.ts` — `staleFor()`.
- `test/browser/stale-marker.test.ts` (new), plus `board-derive` and
  `task-card` tests; three fixtures gained the field.
- `.tasks/done/LAI-049-board-ui.md` — AC2 ticked (named by LAI-157's AC5).

**Decisions**
- *The design's vocabulary, not an invented one.* The prototype has **no
  card-level stale marker** — its clock, `--amb` and "Stale · N days quiet" are
  the *dashboard* panel's. Borrowed rather than invented, and said so in the CSS.
- *Amber is shared with `.marker-unknown`.* The alternative was giving a spec'd
  state a colour the design never chose, to separate it from one the design never
  mentions.
- *No status gate.* §11.6's rule is the job's three conditions; hiding the marker
  on non-`in_progress` tasks would be a third rule. The lag is deliberate.

**Discovered — two defects that every assertion passed through**

Found by **looking at the rendered board** in both themes, after three green
browser tests: the pill **wrapped into a two-line oval** (`stale 9d` is the first
marker label with a space in it), and it **hung past the card's right edge** into
the lane gutter, because two markers fitted the footer row and the third had
nowhere to go.

Fixing the row then exposed a third: the earlier `.card-foot` block in the same
file sets `justify-content: space-between`, which the later one only partly
overrides, so once the row wrapped the first line spread and threw the key right.
`.card-spacer { flex: 1 }` is what right-aligns the avatar and always was.

**The browser test now asserts containment** — the marker's box inside the
card's — which is the defect the screenshot actually showed. `.card-markers` is
dead; `TaskCard` does not use it. Left alone.

**And mutation caught two of my own comments claiming more than the code did**

`staleFor`'s clamp is **not** what prevents `-1m`: a negative `minutes` is `< 1`,
so the branch order returns `now` and `Math.max` never gets a say. I wrote
*"load-bearing, not defensive tidiness"* two files from `relativeTime`, which
documents the identical pair correctly. Same shape for `white-space: nowrap`:
the wrap was real, but `flex-wrap` on the row is what fixes it now.

Both comments now say which half the test can see. **Five of six mutations red;
the sixth is reported as uncaught rather than quietly dropped.** This is CHIEF's
rule from this morning landing on me twice in one task — in the session where I
quoted it at somebody else.

**Ports:** none. The browser harness builds into a temp directory.

## 18:05 +05:30 — LAI-436 → review. Blocked bars and sprint selection.

**Changed**
- `timeline.css` — blocked survives being planned; the tab row.
- `TimelineScreen.tsx` — selection state, tabs, the labelled countdown.
- `timeline-derive.ts` — `daysLeft` → `SprintCountdown`, `countdownFor`.
- `src/components/LockIcon.tsx` (new), `ProjectStats.tsx` uses it.
- `test/browser/timeline-blocked-and-tabs.test.ts` (new), derive tests rewritten.

**Decisions**
- *Blocked colours the border on an outline, the fill on a solid bar.* Blocked is
  orthogonal to measured-versus-placed; a started blocked task must not lose its
  fill to say so.
- *Selection is a background; being current is a glyph.* They coincide by
  default, so a colour for both would make them indistinguishable exactly then.
- *`LockIcon` extracted, not pasted a third time.* `TaskCard` left alone on
  purpose — its copy is `strokeWidth="2"` against `2.2`, so converting it changes
  a board glyph in a timeline task.

**Discovered — three things**

**1. Two classes rendering identically.** Measured before touching anything: a
blocked planned bar and an unblocked one both computed `rgba(0,0,0,0)` fill on
`rgba(255,255,255,0.2)` border. **Every class-name assertion would have passed
for ever**, which is why the browser test compares the two computed colours
against each other rather than against a constant.

**2. The board and the timeline disagreed about the last day of a sprint.**
Reusing `sprint-derive.daysLeft` rather than counting again is what exposed it —
`1` on the board, `0` on the timeline, same sprint, same day. The timeline's test
justified `0` with *"`ends_on` is inclusive (§4.15)"*; **§4.15 says only that the
dates are date-only**, and the conclusion contradicts its own premise. The board's
reading wins and the assertion that said `0` now says `1`. **Reuse found a bug
that duplication had been hiding** — the opposite of the usual argument for it.

**3. A Rules of Hooks violation that the whole suite passed.** The selection
`useState` read better beside the code using it, and this component returns early
for loading, error and an empty range. React: `Minified React error #310`, blank
screen. **Typecheck green, every unit test green** — none of them mounts the
component. Only rendering it showed anything.

**And a correction to the task file.** Its Goal says the prototype has *"tabs
across the top — `S1 … S4` … `11/11`-style counts"*. **It does not** — no
`S1`-style tab row and no `N/N` count in any design file. What is there is a
sprint *picker card list* with `{{ r.frac }}`, a meter and `🔒{{ r.blocked }}`.
The behaviour AC4 asks for is right; the description of its source was
paraphrase. Built from the elements that are actually in the file.

**Ports:** none. The browser harness builds into a temp directory.

## 18:50 +05:30 — LAI-160 → review. Thirteen pairs, not fourteen.

**Changed**
- `server/web/test/api/view-type-drift.test.ts` — `PAIRS` 7 → **20**.
- `server/web/src/api/setup.ts` — `SetupSystemStatus`, and `SetupStatus.system`.
- `server/test/tooling/response-type-coverage.test.ts` — thirteen `UNPAIRED`
  rows deleted (the named crossing this task authorises).

**Decisions**
- *`SetupStatusBody.system` is declared, not `clientOmits`'d.* The omission was
  never a decision; an exemption would have recorded it as one. Follows
  `Task.blocks` — declared, with the task that renders it named (LAI-158).
- *`SetupSystemStatus`, not `SystemStatus`* — the component that draws it already
  owns that name.
- *`ProjectView` is not paired, and the task's table is wrong to ask for it.*

**Discovered**

**Coverage can be inherited, and the census cannot see it.** `PAIRS` has
`ProjectSummary` → `Project`, `ProjectSummary extends ProjectView`, and
`fieldsOf` **resolves `extends`** — so `ProjectView` was already fully compared.
Adding the entry made the drift check red, correctly: it asserts the base type
sends the five fields the summary derives. **LAI-444's total is not a to-do
list**, and the reason is now written beside the `ProjectSummary` pair, where
somebody would go to re-add it.

**A data structure with no slot for the answer.** Trying to record ProjectView as
"covered transitively" turned CORE's `names a client type that exists` red — the
map's second column *is* the client type. Left their row exactly as it was and
said why in their file. **That the schema cannot express it is the finding**, not
an obstacle to route around.

**Twelve of fourteen agreed.** Small, and it confirms LAI-444 from the other
side: the convention is followed; the guard was not reading it.

**Verified the pairs are not vacuous before trusting the green** — a pair whose
parser finds nothing compares two empty sets and passes. Probed all 20 for field
counts (`TokenView` 9 vs 9, `InviteView` 9 vs 9, …), then two mutations, both
red, one in each direction.

**Ports:** none.

## 20:30 +05:30 — LAI-439 → review. M5's exit criterion renders.

**Changed**
- `src/api/presence.ts` (new), `src/routes/screens/capacity/` (new: screen,
  derivations, css), `src/components/markers.css` (new).
- `UnlistedList.tsx` extracted from `UnlistedScreen`; `api/tasks.ts` gains
  `getTask`; route table, `AppShell`, and three nav expectations updated.

**Decisions**
- *`repo === undefined` is the withheld-location test.* The other two candidates
  cannot distinguish the cases at all.
- *Polling, not SSE.* `GET /events` carries activity, not heartbeats — a screen
  driven by it would sit still while going stale. Said on screen: `updated
  every 20s`.
- *REVIEW, not WORK*, per AC1 — and offered, by `nav-truth.test.ts`'s own rule
  that a route ships once there is a screen behind it.
- *Extract rather than copy*, three times: the unlisted list, the agent marker
  CSS, and a name map instead of a `Member` prop I could only have filled by
  inventing fields.

**Discovered**

**The task file was wrong about two of four shapes, and CHIEF corrected it after
I measured.** `matched_task_id` and `project_ids` are **not** absent when a
reader may not be told where somebody is working — they arrive `null` and `[]`.
`repo`/`branch` do go absent. Read from `services/presence.ts` and confirmed
against a live heartbeat on my own instance. **The version in the task would have
been a silent bug, not a round trip**: `matched_task_id === undefined` never
fires, so every withheld row would have rendered as located with no location.

**Two mutations found gaps in my tests rather than in the code.** Inferring
`disabled` from an empty list passed everything, because the disabled fixture had
`enabled: false` *and* `people: []` — one fixture, both conditions, no way to
tell which was doing the work. And `unlisted ?? []` passed because nothing
asserted the count line. **Both are the exact distinctions the criteria are
about**, and a green suite plus a reviewer would have missed both.

**And I wrote my own duplicate.** `locationHidden` in `api/presence.ts` was a
second copy of `hasLocation`, unused. The structure check refusing my test's
*name* is what led me to the file where I found it — a check firing for one
reason and surfacing another.

**Ports:** my own on **3371** (uptime_ms 7846 on first read), killed after.
Never touched `localhost:3000`.

## 21:35 +05:30 — LAI-440 → review. The board's presence surfaces are real.

**Changed**
- `components/PresencePerson.tsx` + css (new) — LAI-439's row, lifted out.
- `board/PresenceStrip.tsx` rewritten; `board/BoardRail.tsx` agent card rewritten;
  `BoardScreen.tsx` polls `GET /presence`.
- `api/presence.ts` gains `hasLocation`; `capacity-derive.ts` loses its copy.
- **Deleted** `src/demo/presence.ts` and `src/demo/agent-sessions.ts`.
- `test/browser/board-presence.test.ts` (new, 7).

**Decisions**
- *Three states, three renderings.* A heading with nothing under it is a state
  and must say which one. `enabled: false` hides the strip **and** the rail card
  — on the Board there is nothing to explain and no room to explain it.
- *The invented progress bar is deleted, not reconnected.* Nothing on the wire
  measures it, and a bar that means nothing is worse than no bar.
- *One renderer.* A chip and a row look different and decide the same three
  things; two copies drift into "unknown" here and a dash there.

**Discovered**

**A block-level element cannot show you that its contents overflow.** The chip
ran off the right edge of the rail's 250px card — obvious in a screenshot — and
my first assertion measured the `li`, which is the card's width either way. So
removing the fix left the test **green**. Only the mutation said so. It measures
the chip now, plus `scrollWidth - clientWidth`.

That is the third time today a browser assertion was written against the wrong
element: LAI-157's marker needed containment rather than height, LAI-436's needed
a computed colour rather than a class, and this one needed the child rather than
the parent. **The harness sees pixels; whether it is pointed at the right ones is
still a judgement, and mutation is the only thing that has caught it.**

**Gate:** red on one assertion in CORE's census — `UNPAIRED.has('CapacityView')`
— which LAI-439's approved pairing invalidated and CORE is replacing. Quoted on
the task rather than ticked. Everything else green: 639 web, 49 cli, 1817/1818.

**Ports:** none. The browser harness builds into a temp directory.
