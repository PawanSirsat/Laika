# CORE — 2026-09-09

## 2026-09-09T14:40:00Z — LAI-163

**What changed**
- `server/test/tooling/schema-spec-drift.test.ts` — a `statedNullability` parser,
  a `record` helper, four tests, and two docblocks. Nothing else; no production
  code was touched by this task.
- `.tasks/backlog/LAI-165-timeline-sprint-fixture-expired.md` (new)
- `.tasks/backlog/LAI-166-board-presence-times-out-under-the-gate.md` (new)

**Decisions**

- **"§4 states it" means the word appears attached to a name, and silence means
  nothing.** §4 uses `nullable` 27 times and `not null` once, against 191
  columns. A table row states it for the columns its first cell names; prose
  states it only in a parenthetical attached to a name, because a prose list
  puts the word on the same line as `token_hash`, which is required. Inferring
  *required* from silence would have failed on most of the schema.

- **So it would not have caught the thing it was filed for, and the docblock says
  so.** LAI-449 changed `comments.author_id` and §4.7 says neither word about it.
  Writing that down was the point: the whole task is about a guard that reads as
  though it covers more than it does, and shipping a second one would have been
  the joke rather than the fix.

- **The row half shares `fieldsIn` with `parseSpecTables` instead of matching a
  name itself.** See below — this was not a style choice.

- **Not extended to types**, per the task's Notes. The header says so and points
  at `schema-migration-drift.test.ts`'s *"columns agree on type, nullability and
  primary key"*, which I opened and confirmed compares `type` before citing it.

**Discovered**

*I wrote the bug the task exists to prevent, into the fix for it.* My first
row parser took one backticked name followed by `|`. §4 has rows whose first
cell names several columns — `| `last_used_at`, `expires_at`, `revoked_at` |
nullable |` and §4.6's `started_at`, `completed_at` — and those rows were
skipped whole. **Five columns were checked for existence and not for
nullability**, with the block green at 24 statements and nothing to suggest a
gap. LAI-163's own words are *"a guard whose reach is decided by something
nobody is checking"*, and the something was a regex I had written forty lines
below one that already knew the answer.

The fix was to delete my opinion, not to widen it: the row half now calls
`fieldsIn`, so the two halves of this file cannot disagree about what a row *is*.
24 → 29 statements. A test names those five columns so it cannot regress
quietly. **The general form: when a file already parses the artefact, a second
parser of the same artefact is a bug with a delay on it** — and the delay is
however long until the document uses a shape only one of them handles.

*The same pass found the section never ended.* `table` was only reset by the next
`### 4.x` heading, so after §4 the last table stayed current to the end of
SPEC.md, and any `` `name` (nullable) `` below would have been recorded against
`sprints`. It was green only because nothing below §4 happens to use that
spelling. Now ends on any heading, as `parseSpecTables` does, with a test.

*Two numbers in my own comments were wrong, and I had not counted either.* The
docblock said §4 says `nullable` "sixteen times" — it is 27. And I wrote that the
stated set covers a few dozen of "the schema's several hundred" columns — it is
191, across 22 tables. Both were plausible, both were in a task about a comment
that overclaims, and neither would have been caught by any test. I have written
the count as **29 of 191 when this was written** so it reads as a snapshot rather
than a live claim, because a bare number in a comment rots and the next reader
has no way to tell it has.

*The root gate is `EXIT 1`, and none of it is mine.* Four failures, all in
`server/web/`, proven by stashing my change and running them on a clean tree.
Two findings, filed rather than fixed:

- **LAI-165 (p1).** `timeline-blocked-and-tabs.test.ts` pins absolute sprint
  dates and asserts date-relative UI text. S3 was current until 2026-09-06; S4
  began on 09-07. Three tests fail every run, and will fail *differently* after
  09-20 when no sprint contains today. Its fixture comment says *"`now` is pinned
  by the sprint that contains today"* — nothing pins `now`, and that sentence is
  why nobody asked what happens when today moves. Same class as the two comments
  I fixed in my own file this hour, which is not a coincidence I like.
- **LAI-166 (p2).** `board-presence.test.ts` waits 20s for a chip that arrives in
  1883ms alone. With LAI-452 (cli, 1-in-2) and LAI-456 (server, 466ms → 5464ms)
  that is **three tests in three workspaces with one signature**: fine alone,
  over a fixed deadline under parallel load. Three instances is a property of the
  gate, not three flaky tests, and I said so in the task rather than leaving the
  next person to notice it a fourth time.

**State**
In review. **AC5 is not ticked and must not be** — the root gate is `EXIT 1` on
four `server/web/` failures that predate my change and are not mine to fix
(D-031). Server workspace alone, lint, format and typecheck are green; the
figures are in the review note. LAI-165 is what turns the gate green.

## 2026-09-09T17:20:00Z — LAI-451

**What changed**
- `server/src/services/meeting-reviews.ts` — `applyMeetingReview`,
  `applyProposal`, `appendDecision`, and the validation helpers.
- `server/src/http/routes/meeting-reviews.ts` (new) — `POST /:id/apply`.
- `server/src/app.ts` — mounted at `${API_BASE}/meeting-reviews`.
- `server/test/services/meeting-reviews.test.ts` — 25 tests, up from 8.
- `server/test/http/routes/meeting-reviews.test.ts` (new) — 6 transport tests.
- `server/test/tooling/activity-payload-names.test.ts` — `meeting.applied` added
  to the emitter sweep.

**Decisions**

- **`can()` per proposal is not a table in this file.** Each proposal calls the
  service a person would have called, and each of those runs its own
  `assertCan`. §3.2's rows therefore decide each change, and there is no second
  opinion here to fall out of step with them. The case that makes this concrete:
  `meeting_proposal.apply` is member-and-up, but a `decision` writes
  `context_md`, which is lead-only — **so a member may apply one proposal in a
  review and not another.** The test proves both halves *and* that the owner can
  do the one the member could not, so the refusal is provably about the role
  rather than about the proposal being unapplicable.

  Same reasoning `finishTask` gives for not re-checking its halves, and the same
  §3.4 gives for the webhook performing `task.write` rather than an action of its
  own: the principal is what differs, not the operation.

- **A `decision` is not a no-op; §11.4.2 says so.** The task file's Notes
  wondered whether accepting one might change nothing. §11.4.2: *"Accepted
  `decision` proposals from a meeting diff append to it with the date. That is
  the mechanism that keeps it current instead of stale."* So it appends. One
  `## Decisions` heading created once, one list across meetings, an ISO day
  rather than a locale because the document is read by whoever opens it.

- **One `meeting.applied` row for the set, not one per proposal.** AC5 reads
  *"**Every applied proposal** writes `meeting.applied` … with **which
  proposals** in the payload"*, and those two clauses point opposite ways. §4.8
  decides it rather than my preference: **`sprint.tasks_changed` is one verb for
  both directions "and the payload carries which"** — the identical shape, a set
  operation with one row. The property §4.8 actually demands is its own stated
  test, *"could a reader answer 'when did this happen?' without inspecting a
  payload?"*, and one row passes it.

- **Idempotence is per proposal, stamped into `proposals_json`.** The review's
  `status` column cannot carry it: it is one column for a set, so a client that
  posted, lost the connection and retried would either double every change or be
  refused wholesale. A retry after a dropped response is the ordinary case this
  criterion exists for.

- **Expiry is read from `expires_at`, not only from `status`.** §11.6's sweep
  runs on a schedule, so between lapse and sweep the row still says `pending`.
  The timestamp is the truth, and there is a test for exactly that window.

- **`change` proposals are validated at apply time.** `changes` arrives as
  `Record<string, unknown>` straight off the model — `parseProposals` stores it
  without inspecting it, because §10.2 does not say what may be in it — so an
  unrecognised key is **refused, not dropped**. Dropping it is the whole bug in
  miniature: a human accepted a proposal that read *"set priority and reassign"*,
  and an apply performing one of those has applied something nobody accepted.

**Discovered**

*LAI-045's guard caught its own author, mid-task.* My `meeting.applied` payload
named `taskId`. The sweep went red with `meeting.applied: "taskId"` — a Drizzle
property leaking into an audit row, in the one payload a person reads when
asking why a task moved. It is `task_id` now. The guard I built two tasks ago to
stop exactly this found it in code I had just written and believed was clean,
which is the only real evidence that a guard works.

*The structure and vocabulary guards wrote half this task's plan.* Before any
test of mine ran, `structure.test.ts` demanded a mirrored route test and
`activity-payload-names.test.ts` demanded the new emitter be exercised. Neither
is a check I would have thought to run; both are things I would have missed.

*Seven break-probes, all red*, each with the anchor asserted present and the
file checksummed back afterwards: apply-everything-regardless-of-ids, skip the
stranger-id check, write `context_md` directly instead of through the service,
drop the `applied_at` stamp, reduce the audit payload to a count, remove the
transaction, and trust `status` over the clock. The transaction probe reddened
five tests including the partial-failure one — **and I only know that because I
re-ran it printing every failure**; the harness had truncated at three and the
one criterion I most wanted to see was the one below the cut.

**State**
In review. **Root gate `EXIT 0`** — server 103 files / 1889 tests, `server/web`
644, `cli` 72, lint / format / typecheck all clean.

## 2026-09-09T19:05:00Z — LAI-454

**What changed**
- `server/src/services/meeting-reviews.ts` — `listMeetingReviews`,
  `getMeetingReview`, `discardMeetingReview`, `requireReadableReview`, and the
  `MeetingReviewView` / `MeetingReviewDetailView` / `ProposalView` shapes.
- `server/src/http/routes/meeting-reviews.ts` — `GET /:id`,
  `POST /:id/discard`, and `projectMeetingReviewRoutes` for the project-scoped
  list.
- `server/src/app.ts` — the second router mounted under `/projects`.
- `server/src/db/enums.ts` — `meeting_review.discarded`, and `discarded` on
  `MEETING_REVIEW_STATUSES`.
- `server/src/db/migrations/0021_discarded_meeting_reviews.sql` (new) — rebuilds
  `activity` and `meeting_reviews` for the two CHECK constraints.
- `server/test/services/meeting-reviews.test.ts` — 37 tests, up from 25.
- `server/test/http/routes/meeting-reviews.test.ts` — 14, up from 6.
- `server/test/tooling/schema-spec-drift.test.ts` — one exemption in, then out.
- `server/test/tooling/response-type-coverage.test.ts` — three `UNPAIRED`
  entries.
- `server/test/tooling/activity-payload-names.test.ts` — discard in the sweep.
- `.tasks/backlog/LAI-167`, `LAI-168` (new).

**Decisions**

- **I refused to build AC3 and filed LAI-167 instead.** It asked the detail
  response to carry the transcript; §4.12 has no column for one and LAI-450
  deliberately kept only a hash. Three artefacts disagreed — §11.4.2's screen
  line, §4.12's column list, and the task file's Notes — and **choosing between
  them would have added a column nobody decided on.** CHIEF's D-056 settled it
  the other way: the screen line was wrong, the schema stands, and D-005's reach
  is now written down as *"quotes are kept, the transcript is not"*.

- **The list carries `proposal_count`, not the proposals — and the real reason
  is not size.** Every proposal carries a `quote`, and **a quote is verbatim
  transcript content.** A paginated list returning proposals would hand a broad
  read the quoted parts of every meeting the project ever recorded. So the
  quotes sit behind `GET /meeting-reviews/:id`, which is a `can()` on one
  review. The task's Notes had the right instinct on a false premise; the
  instinct survives the premise being corrected.

- **One disclosure helper for all three endpoints, and `apply` now uses it too.**
  `requireReadableReview` answers `not_found` identically for "no such row" and
  "not yours". This changed `apply` from `403` to `404` after LAI-451 was
  accepted — flagged to CHIEF. One endpoint disclosing what its neighbours
  conceal is the leak, and a shared helper is what stops three of them drifting.

- **`discard` is `meeting_proposal.apply`, not `project.read`.** It ends a
  review nobody can then act on. Grading it as a read would let a viewer throw
  away a proposal set they are only allowed to look at — and there is a test
  that reads as a viewer and is refused the discard.

- **`meeting_review.discarded`, not `meeting.discarded`.** CHIEF's name, and
  better than mine: it matches `meeting_review.expired`, and both are events
  about the *review*, where `meeting.applied` is an event about the *board*.

**Discovered**

*My response types were invisible to the census because of what I called them.*
`response-type-coverage.test.ts` finds served types by the `...View` suffix or a
`c.json<Type>` annotation. `MeetingReviewSummary` and `MeetingReviewDetail` had
neither, so **two of my three new response types would never have been counted**
— while `ProposalView` was, purely because of its name. I renamed to the house
convention rather than exempting them, and all three then appeared. This is the
same shape as LAI-163's five skipped columns, one file over: **a guard whose
reach is decided by a naming convention nobody re-checks**, and it silently
under-reports rather than failing.

*The exemption mechanism completed a full cycle, and the guard is what closed
it.* I took one `ACTIVITY_TYPE_EXEMPTIONS` entry naming LAI-168, per §4.4 step 2.
After merging CHIEF's `master`, the staleness guard went red and **that** removed
it — not me remembering. First time I have watched that work end to end, and the
difference between it and a comment saying *"remove this later"* is the whole
argument for the mechanism.

*I went looking for a bug that was already handled.* Migration 0021 rebuilds
`activity`, and 0017 did the same and never recreated §4.8's append-only
triggers. `migrate.ts` re-establishes them **unconditionally after every boot**,
and its post-condition reads each trigger's stored SQL for the `RAISE` clause
rather than trusting the name — because *"a paste that lost its `RAISE` would
create a trigger that is present, correctly named, and enforces nothing"*.
Worth recording that the check I would have written is the weaker one.

*Fourteen probes across two tasks, all red.* The seven here: list returns
proposals, unreadable answers `forbidden`, discard graded as a read, a discarded
review applies again, discard skips its status check, expiry hides the review,
and discard writes the wrong verb.

**State**
In review, pending the gate. AC3 is ticked **against D-056's corrected
§11.4.2.1**, not against the task file's wording, at CHIEF's instruction.

## 2026-09-09T20:15:00Z — LAI-456

**What changed**
- `server/test/services/events.test.ts` — `fill()` appends directly instead of
  going through `write()`.

**Decisions**

- **No timeout was raised, and none needed to be.** AC1 asked where the 466ms
  goes before anything else, and the answer decided the fix: **it was not the
  500 inserts.** `fill` called `write()`, which reads the whole log twice around
  every insert so it can hand back the row it just wrote — and `fill` throws
  that row away. At `MAX_REPLAY` that is roughly **250,000 row reads to produce
  500 values nobody looks at**: quadratic setup for a linear fixture.

  | `fill(MAX_REPLAY)` | |
  | --- | --- |
  | via `write()` | **217ms** |
  | appending directly | **46ms** |
  | the discarded reads | **172ms — 79% of it** |

  That 79% is also *why* it was contention-sensitive rather than merely slow:
  the time was mostly queries competing for the same cores as a browser suite
  and a cli suite.

- **The neighbour was measured, not assumed** (AC3). *"refuses one past the
  limit"* does the same `fill` and was 446ms; it is 59ms now. One change fixed
  both, which is what AC3 was guarding against — fixing only the test that
  happened to fail leaves the other armed.

- **`write()` is untouched.** The tests that need the row back still get it. The
  fixture rows are identical either way — same table, same columns, same order —
  so nothing about what the tests prove changed.

**Discovered**

*The measurement was worth more than the fix.* *"466ms alone, 5464ms together"*
reads as *"this test is slow, give it room"*, and the timeout raise would have
been three characters and would have passed review. **The test was not slow; its
setup was doing something quadratic and pointless**, and the 11× contention
factor was the symptom that made a constant-factor problem visible. A raised
timeout would have left `fill` in the file for the next person to inherit at
`MAX_REPLAY = 1000`.

*Verified under contention rather than alone* (AC4): with the root gate running
and a second vitest instance on top — harsher than the condition that produced
the original failure — the two tests were **189ms and 488ms** against the 5000ms
default. Roughly a 10× margin remaining, from none.

**State**
In review. **Root gate `EXIT 1`, and the two failures are LAI-169's**, inherited
from LAI-454 which is already in review for exactly them:
`STREAM_TYPES equals ACTIVITY_TYPES, in order` and `covers every verb the server
can write`, both on `meeting_review.discarded`, both in `server/web/`. **Zero
failures outside `server/web/`** — `server` and `cli` clean, lint and format
`EXIT 0`. Nothing in this task touches either.

## 2026-09-09T21:00:00Z — LAI-462

**What changed**
- `server/test/tooling/schema-spec-drift.test.ts` — three docblock passages and
  one test comment. No behaviour.

**Decisions**

- **The correction is not "it went stale". It was never true.** CHIEF's diagnosis
  was that *"it would not have caught what it was filed for"* was true against the
  §4.7 I started from and became false when `master` merged LAI-449's row.
  `git show 9e96cb7^:docs/SPEC.md` says otherwise: **§4.7 already read
  `` `author_id` (**nullable**) `` at the commit before LAI-163 landed.** My own
  first measurement dump listed `comments.author_id` among the covered columns,
  on the day I wrote the sentence saying it was not.

  So the failure was not staleness. **I wrote a claim about a section I had read
  once at the start of the task and never reopened**, in the same task where two
  other unmeasured claims had already been caught. That is a worse diagnosis and
  the useful one.

- **The uncovered example is `tasks.title`, and it was verified before it was
  written down.** Making it nullable in `schema.ts` leaves the file green.
  §4.5's row is `` | `title`, `description_md` | | `` — one row, two columns, an
  empty cell — and it is the sharp case rather than a stray: **the two columns
  genuinely differ**, `title` required and `description_md` nullable, so §4 could
  not state them in that shape without splitting the row.

- **Dated snapshots rather than a self-checking assertion.** I considered a test
  that reads the docblock's own numbers and asserts them against reality. It
  would have made every §4 nullability edit — CHIEF's area — redden a `server/`
  test, which is the LAI-169 coupling I had just spent a task inside, for a
  comment's illustrative count. *"28 times (measured 2026-09-09)"* cannot become
  false instead.

**Discovered**

*AC3 said look for a fourth. There were four more.* Two were numbers — `nullable`
is **28**, not 27, and the reach is **30 of 191**, not 29. One was a distance:
*"the same reader `parseSpecTables` uses one screen up"* — `fieldsIn` is at line
65 and `statedNullability` at line 350, **285 lines apart**, and that was wrong
when written, not later.

**The last two are the worst, because they were section numbers.** A test comment
credited the `` | `last_used_at`, `expires_at`, `revoked_at` | `` row to **§4.5**
and `started_at`, `completed_at` to **§4.6**. `tokens` is **§4.9**; `tasks` is
**§4.5**; §4.6 is `task_dependencies` and has neither. **Swapped, and both
wrong**, in a comment explaining the fix for a bug caused by not checking. Each
was one `grep -n '^### 4\.'` away.

*The reach moved without a code change, which settles CHIEF's reframing.* 29 → 30
when §4.2 gained a row for `orgs.transcript_webhook_secret_enc` — the column that
left `COLUMNS_NOT_IN_SPEC` under D-034. So *"29 of 191"* was never a fact about
this guard; it is a measurement of how much §4 says out loud, and the docblock
now says so and points at `docs/` for widening it.

*I audited every remaining §4.N reference in the file, including the ones I did
not write.* The table/prose classifications from LAI-051 (§4.1, §4.2, §4.5
tables; §4.4, §4.7, §4.10, §4.14 prose) all hold, checked against the document.
Only my two were wrong.

**State**
In review.
