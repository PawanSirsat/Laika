# PM — 2026-08-26

## The UI matches the design

**121 done, 0 in review, master green: 1071 server + 431 web, lint and format
clean.** Shared demo rebuilt on current master at `localhost:3000`.

**Every screen in the shipped sidebar exists and matches
`docs/design/Laika Prototype.dc.html`**: Board (sprint rail, active-sprint
banner, live SSE stream, task cards with tags, priority, sprint chip, dependency
count, assignee, and a blocked treatment that names its blocker), Sprints,
Timeline, Projects, Dashboard, Organisation. First boot and login match
`Laika 05-07`.

**Nothing on any of them is invented.** The one exception is `src/demo/`, which
is opt-in at build time, carries a visible `SAMPLE` badge naming what is absent,
and cannot reach a production build — a test proves it.

**What the owner still owns**: the M1 exit test, the Calendar decision
(LAI-217), and `@mentions` (LAI-094).

**What remains is interaction and internal work, not appearance**: tag editing
(LAI-081), three client-type fixes, two guard tasks.

### The pattern across 121 tasks

Every serious bug this project found was **a guard that could not fail**, and
every one was found by attacking the guard rather than reading it:

| | what looked fine |
| --- | --- |
| LAI-054 | a test glob matching no files — 23 tests never ran |
| LAI-048 | a constant compared against itself |
| LAI-074 | CSS matching no element |
| LAI-090 | a security check off in the only environment we measure |
| LAI-091 | a query-plan test EXPLAINing SQL retyped in the test |
| LAI-053 | a query-count test instrumenting the service, not the route |
| LAI-079 | five probes editing `schema.ts`, which the test DB is not built from |

**And my own reviews produced the same shape repeatedly**: probes that matched
nothing, selectors for classes that did not exist, a setup body that 422'd, a
search of `textContent` for a value that lives in an input. Each returned a
confident wrong answer. The habit that fixed it: **when a probe says something is
missing, first prove it can see something present.**
