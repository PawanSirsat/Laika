# SHELL — 2026-09-19

## 2026-09-19T12:40:00Z — LAI-266

**What changed**

Server (the owner-granted crossing — named file by file in the task):

- `server/src/db/schema.ts` — `board_columns`, `board_column_statuses`, and
  `projects.board_hide_done_days`
- `server/src/db/migrations/0023_awesome_guardian.sql` + `meta/` — generated
- `server/src/db/board-columns.ts` — **new**: the two seed sets and the function
  that writes them
- `server/src/db/backfill.ts` — `backfillBoardColumns`, beside the timestamps one
- `server/src/db/migrate.ts` — one call added inside the protected window
- `server/src/services/board-columns.ts` — **new**: list / create / rename /
  set-statuses / delete-with-reassignment / atomic reorder
- `server/src/services/task-lifecycle.ts` — the §5 table takes a `TransitionRule`
- `server/src/auth/resolve-actor.ts` — `isAgentPrincipal`
- `server/src/services/tasks.ts` — `changeStatus` derives the rule; `finishTask`
  pins `'agent'`
- `server/src/services/projects.ts`, `setup.ts` — seed columns on create
- `server/src/http/routes/board-columns.ts` — **new**; mounted in `app.ts`
- `server/src/services/projects.ts` — `board_hide_done_days` on the view and
  `PATCH`
- `server/test/**` — 28 service tests, 14 route tests, 12 seed tests, plus the
  lifecycle split; two `TABLES_NOT_IN_SPEC` entries and one
  `COLUMNS_NOT_IN_SPEC` entry naming LAI-264

Web:

- `server/web/src/api/columns.ts`, `use-columns.ts` — **new**
- `server/web/src/api/board-derive.ts` — `BOARD_COLUMNS` → `MOVABLE_STATUSES`
  (re-export, not a copy), `COLUMN_LABELS` → `STATUS_LABELS` with `cancelled`,
  `statusLabel`, `primaryStatus`, `groupByColumn` now returns `Lane[]`
- `server/web/src/routes/screens/board/` — `ViewSettings.tsx`,
  `ColumnDialog.tsx`, `card-fields.ts`, `view-preferences.ts`,
  `use-view-preferences.ts`, `group-lanes.ts`, `view-settings.css`,
  `column-editor.css` — all **new**
- `KanbanView.tsx` — lanes from columns, column drag, `.lane-order` keyboard path
- `TaskCard.tsx` — one `fields` prop gating nine optional elements
- `board.css` — `repeat(5, …)` → `grid-auto-flow: column`
- `SpaceTopBar.tsx` / `SpaceSlot.tsx` — a `SpaceBarSlot` portal
- `TaskMeta.tsx` — the drawer's status select gains `cancelled`
- five files lost the identical `status === 'cancelled' ? …` ternary

**Decisions**

- **The §5 transition table splits by principal, and the seam is `actor.token`.**
  The obvious predicate, `isSystemPrincipal`, is *wrong*: it means
  `kind === 'system'` — cron and webhooks — so a token-bearing agent is an
  ordinary `ResolvedActor` and branching on it would have handed every MCP
  caller the widened rule. Checked rather than assumed.
- **The composite FK is `CASCADE`, not `RESTRICT`.** RESTRICT is the obvious
  choice and it is unsafe here: SQLite resolves cascades in **table-creation
  order**, and drizzle-kit emits tables alphabetically, so
  `board_column_statuses` is created first and a project delete then fails.
  Measured both orders both ways. RESTRICT's correctness would have rested on
  how two tables happen to be *named*.
- **The backfill gives existing projects five lanes, not the new four.**
  `DEFAULT_COLUMNS` folds `backlog` into "To do", which is the better default and
  would be a silent edit to a live board on upgrade.
- **`todo` leads "To do", not `backlog`.** The first status is the primary — what
  a drop sets and where the lane's colour comes from — and there is no
  `.lane-dot-backlog` rule, so the enum's order would have drawn a grey lane
  labelled "To do" that dropped cards into `backlog`.
- **A 404 from `/board-columns` falls back to one lane per status.** Rejected an
  error screen: a 404 is a server without the feature, and those five labels were
  already ours. It also keeps all 14 existing board fixtures green.
- **The View settings trigger lives in the space bar, not the slot below it.**
  First built in the slot; `sprint-strip.test.ts` caught that this adds a band
  the reference does not have. Moved rather than weakening the guard.
- **Group-by ships read-only.** A drop there is three endpoints with three
  permission checks and no keyboard equivalent. → LAI-288.
- **Seeds live in `db/`, not `services/`.** eslint caught `db/backfill.ts`
  importing `services/` — CONVENTIONS §2 makes `db/` the bottom layer.

**Discovered**

- `claimTask` writes `from: 'todo'` into an **append-only** activity row
  regardless of the task's real prior status, and stamps `started_at`
  unconditionally where `changeStatus` stamps only when null. Found while
  verifying the principal seam; unrelated to this diff.
  → **LAI-265** filed (`area: server`).
- **A project cannot be deleted at all today** — `activity.project_id` is
  `ON DELETE restrict` and `activity` is append-only by trigger. Not caused by
  this work; it is LAI-154's subject. My first test asserted the opposite and was
  rewritten to use a bare project row so it tests *these two tables* rather than
  quietly asserting somebody else's bug.
- `TaskMeta`'s status select offered five statuses, so **cancelling a task was
  unreachable from the entire UI**. Fixed here since the drag deliberately never
  resolves to `cancelled`.
- `BOARD_COLUMNS` and `STATUSES` were byte-identical declarations of one
  vocabulary. Collapsed.
- The browser harness's stubs are shape-sensitive in ways that fail as a blank
  page: `/members` is `{ members }` not a page, `/tags` is `{ tags }`. Three
  probes to find it. **The token guard also reads CSS comments** — `var(--n)`
  inside a comment failed it.
- → **LAI-264** filed (`area: docs`, p1): §11.4.1 still says custom columns are
  not in v1 and that column order is derived.

**State**

Server and web both built and green; migration verified against a **copy of the
seeded demo database** — all three existing projects reproduce their five lanes
exactly, every status mapped once. Remaining before review: the live-instance
walkthrough (create / rename / drag / delete) and the both-themes width sweep.
One guard is knowingly red until LAI-264 lands, quoted in the task file.

## 2026-09-19T13:20:00Z — LAI-266 (verification)

**What changed**

- `server/web/src/api/board-derive.ts` — `hideOldDone`
- `server/web/src/routes/screens/BoardScreen.tsx` — applies the space's
  hide-done setting to the board *and* the list, and discloses what it hid
- `server/web/test/api/board-derive.test.ts` — four cases for it

**Decisions**

- **`hideOldDone` only touches `done` with a real `completed_at`.** `cancelled`
  is not "finished", and a `done` task whose completion was never stamped has no
  age to judge — hiding it would be guessing at a date nobody recorded.
- It returns the count as well as the list, because the board has to *say* what
  it hid.

**Discovered**

- **The hide-done setting was stored, editable, and did nothing.** The column,
  the endpoint and the panel all landed; nothing ever filtered by it and no line
  said so. Found by re-reading my own acceptance criteria against the code after
  the live walkthrough rather than by any test — which is the gap worth noting:
  every *other* criterion had a test that would have failed, and this one had a
  control that looked like it worked.
- **A scroller measurement of mine was wrong, not the code.** The width sweep
  reported `boardScrolls=false` at 900px and 420px with eight lanes, which is
  impossible. `.board-main` is the scroller at wide widths and **`.kanban`
  itself** becomes it at narrow ones; the probe only looked at the first. The
  first version of that probe also sliced to six results and `.visually-hidden`
  filled every slot.

**Live verification** (own instance, port 3381, own database, `uptime_ms` checked)

- First boot's project got its four columns — `setup.ts` is the creation site a
  migration backfill cannot reach.
- create → set-statuses → rename → reorder → delete-with-reassignment: after
  **every** step, all six statuses mapped exactly once and all five seeded tasks
  still visible. No task ever disappeared.
- `backlog → done` as a person: **done**. The same move with a real PAT:
  **422, "Cannot move a task from backlog to done"**. The agent still walks
  todo → in_progress → review → done.
- Migration against a **copy of the seeded demo database**: all three existing
  projects reproduce their five lanes exactly.
- Width sweep, both themes, 1600/1440/1280/900/420 — page overflow `0` at every
  size, no page errors, and again at **eight** columns with every lane holding
  the 206px floor.

**State**

Gate green: `TEST 0` · `LINT 0` · `FMT 0`. Not yet moved to review — see the
next entry.

## 2026-09-19T12:44:30Z — LAI-290

**What changed**
- server/web/src/routes/screens/BoardScreen.tsx — `onWheel` on `.board-main`
  forwards `deltaX` to whichever element is the horizontal scroller at the
  current width
- server/web/src/routes/screens/board/board-rail.css — `.board-main` gains an
  explicit `overflow-y: hidden`
- server/web/src/routes/screens/board/board.css — `.lane-body` gains
  `overflow-x: hidden`; the `+` tile is a 2rem square, `align-self: start`
- server/web/test/browser/board-lane-scroll.test.ts — `CROWDED_STUB` and three
  tests: sideways-over-a-card, vertical-over-a-card, no vertical scrollbar

**Decisions**
- **The wheel handler asks which element scrolls rather than assuming
  `event.currentTarget`.** Above 1200px `.board-main` is the scroller; below it
  goes `overflow-x: visible` and `.kanban` scrolls itself instead
  (`board-rail.css`'s `max-width: 1200px` block). Binding to `currentTarget` —
  which is what I wrote first — is a no-op at exactly the widths the owner was
  most likely to hit. The alternative, moving the handler onto `.kanban`, loses
  the wide case for the same reason.
- **`overflow-y: hidden` is declared rather than left to default.** `overflow-x:
  auto` coerces a `visible` `overflow-y` to `auto`, so the pane became a
  vertical scroller nobody asked for. It then scrolled over a 91px phantom — the
  clipped `.lane-move` / `.lane-order` absolutes, positioned against it — while
  every real child ended 18px inside. That is why the bar had no visible content
  under it.
- **CSS cannot fix the sideways case, so the fix is JS.** Measured both
  candidates: `overflow-x: hidden` leaves `.lane-body` a scroll container and
  Chrome still latches to it; `clip` is coerced back to `hidden` because the
  other axis is `auto`.

**Discovered**
- **Chrome latches a wheel gesture to the first scroll container under the
  pointer and drops the axis that container cannot scroll** — it does not chain
  to the ancestor that can. Measured: the same gesture over lane padding gave
  `scrollLeft=90`, over a card `scrollLeft=0`.
- **React's `onWheel` is passive, so `preventDefault()` in it is inert.** Found
  while mutation-checking: mutating the handler to `preventDefault()` left the
  vertical test green. The test does go red on the version that can do the
  damage — a native `addEventListener('wheel', …, { passive: false })`. Both
  facts are now in the test's comment, because the first one alone would have
  read as the test being weak.
- **A guard clause aimed at `.lane-body` measured the wrong lane.**
  `CROWDED_STUB` puts every card in `todo`, which is column *one*, not zero, so
  `querySelector('.lane-body')` returned an empty lane with no overflow and the
  test failed on its own guard. Same family as the fixtures that could not
  violate the property they checked — three times this week now.
- nothing filed; all three are properties of code I own.

**State**
LAI-290 still in progress — the toolbar duplicates the inline Priority/Tag/
Assignee selects in the space bar, which is the last item before review.

## 2026-09-19T12:52:00Z — LAI-290 (environment)

**What changed**
- no source files — the local demo setup only
- `.laika-local/board-demo.db` — the seeded database moved out of this session's
  scratchpad to a stable, gitignored, shared path
- `server/public/` — rebuilt with plain `vite build` (the repo default output)
- the instance on `:3381` restarted to serve `server/public` instead of the
  `/tmp/laika-demo-spa` snapshot

**Decisions**
- **Serve the build's *default* output rather than a snapshot.** A second Claude
  session is working in this same checkout, on this same branch, and we had each
  been building into our own directory and pointing the demo at it — so every
  build silently deleted the other's. `vite.config.ts:10` already sets
  `OUT_DIR = '../public'`; the divergence existed only because I overrode it
  with `--outDir`. Serving the default means neither of us has to remember a
  flag, which is the only version of this that survives being forgotten.
- **Kept my seeded database, not theirs.** `:3371` serves the earlier thin seed
  (`.laika-local/laika.db`); `board-demo.db` has the 21 tasks, 5 users, 4
  sprints, 8 tags and 5 columns the owner asked to see. Copied rather than
  moved, and verified row counts on both sides before trusting it — the `-wal`
  was 0 bytes, so nothing was stranded.
- **Did not kill `:3371`.** It is the other session's process; §4.3 says kill by
  port and only your own. Told them instead.

**Discovered**
- **A `200` from this server does not mean the endpoint exists.** Probing for
  the login route, `/auth/sign-in/email` and `/api/v1/auth/sign-in/email` both
  answered `200` — the first was the SPA's catch-all `index.html`. A status-code
  sweep cannot tell a route from the fallback; only the *body* can. Same family
  as the §4.3 `uptime_ms` trap and the mutation harness that never ran: the
  instrument returning success for the wrong reason. The real base is
  `AUTH_BASE_PATH = '/api/v1/auth'` (`server/src/auth/auth.ts:13`).
- **The other session is committing to `shell`, in this worktree** (46807bd,
  cc0d785, 043bf0d — LAI-291, the add-column dialog). Its work is already in my
  branch and is good; the risk is the one §4.2 names, that a shared checkout
  stops the task-file move being a lock. Sent them the setup, asked for explicit
  path staging and for the id before a claim.
- **LAI-291 is taken** — my plan had earmarked it for `work_type`/`flagged_at`.
  Renumbering mine; lowest free in SHELL's range is now LAI-292.

**State**
LAI-290 still in progress. One local setup on `:3381`, shared.

## 2026-09-19T12:55:00Z — LAI-290 (shared gate)

**What changed**
- `/tmp/laika-gate/gate-lock.sh` — a lock both sessions take around the gate.
  Outside the repo deliberately; nothing in the tree changed.

**Decisions**
- **A file lock, not "message me before you commit."** The other session asked
  for the message version. `pnpm test` is ~4 minutes and we are both async, so
  the window in which a message can cross a gate run is the whole run — the
  protocol fails exactly when it is needed. A lock does not depend on timing.
- **`mkdir` as the primitive, not `[ -f ]` then `touch`.** `mkdir` is atomic and
  fails when the directory exists; check-then-create has a race as wide as the
  check, which is the bug the lock exists to prevent.
- **It breaks a lock older than 15 minutes.** A gate that can be wedged for ever
  by a crashed run would get bypassed by hand within a day, and a protocol
  people route around is worse than none.
- **Kept out of the repo.** It was in `.laika-local/` first and `git status`
  went `?? .laika-local/` — `.gitignore:9` is `*.db`, which covers the database
  and not the directory. An untracked file in a checkout two sessions share is
  what a stray `git add -A` sweeps. Moving it out beat editing `.gitignore`,
  which is repo-root config and CHIEF's; no task needed once it lives in `/tmp`.

**Discovered**
- **A stale *snapshot* and a stale *browser cache* look identical until you use
  `curl`** — the other session's find, worth keeping: `curl` returned a bundle
  hash with **no matching file** in `public/assets`. A browser cache cannot
  produce that, so the one line separates "hard-reload" from "you are looking at
  a different build root". It is the same family as the `200`-from-the-SPA-
  fallback trap logged above: the measurement succeeding for the wrong reason.
- nothing filed.

**State**
LAI-290 in progress, lock free, not held by me. One open item: the space bar
carries the inline Priority/Tag/Assignee selects as well as the Filter button.

## 2026-09-19T13:02:00Z — LAI-290 (verifying a peer's sweep)

**What changed**
- no files — a check of four "already done" backlog claims and one "blocked"
  claim from the other session in this checkout

**Decisions**
- **Verified rather than relayed.** A peer reported four backlog tasks as
  already built and both of its p1s as blocked on acceptance. Two of the five
  findings were wrong, and both would have cost something real, so the check
  paid for itself. CLAUDE.md's own rule applies to a teammate's summary as much
  as to a task file: *a description is not the artefact.*
- **Believed the guard over the file listing.** For LAI-233 the acceptance
  criterion is the removal of an exemption, so `endpoint-coverage.test.ts` is
  the evidence and "`DependenciesSection.tsx` exists" is not. The same file
  confirms LAI-234 and LAI-235 are still open, by still exempting
  `api/v1/comments/*` and `api/v1/projects/*/tags/*`.
- **Flagged, did not fix.** All the leftovers are in `server/web/` and therefore
  mine to write, but they belong to unclaimed backlog tasks and I hold LAI-290.
  §2 is one task at a time, and `.tasks/` is CHIEF's.

**Discovered**
- **The peer was not blocked at all.** `LAI-254 depends-on [LAI-251]` and
  `LAI-257 depends-on [LAI-252]`; both dependencies are in `.tasks/done/` on
  `master` **and** on `shell`, accepted in 1387563 / 5c29853 and merged to
  `shell` at 9e3ad55. It had read a pre-merge state and concluded a ten-deep
  review queue existed; `.tasks/review/` on `shell` holds exactly `.gitkeep`,
  LAI-266 and LAI-291. Precisely the §2 failure — *history says who touched it,
  only the current path says where it is* — reached from the review side rather
  than the claim side.
- **"Already done" was two done, two partial.** LAI-233 done (exemption gone).
  LAI-242 missing its last AC — four dead `.boot-rail .theme-toggle*` rules at
  `src/routes/screens/auth.css:342,347,352,353`. LAI-223 missing AC 3 — no
  `comment_count` and no stated reason in `src/routes/screens/list/ListView.tsx`.
  LAI-245's CSS fix is in `index.css` but AC 2's test does not exist; the only
  `visually-hidden` under `test/` is a **comment** at
  `board-lane-scroll.test.ts:182`. Closing those three as already-built would
  silently drop a cleanup, a field and the regression guard for a bug both
  sessions tripped over today.
- nothing filed — `.tasks/` is CHIEF's and these are existing tasks, not new
  work.

**State**
LAI-290 in progress, lock free. One open item unchanged: the duplicate
Priority/Tag/Assignee selects beside the Filter button.

## 2026-09-19T13:10:00Z — LAI-290

**What changed**
- `src/components/space/SpaceSlot.tsx` — `SpaceFilterClaim`,
  `useClaimSpaceFilters`, `useSpaceFiltersClaimed`
- `src/components/space/SpaceLayout.tsx` — wraps bar and screen in the provider
- `src/components/space/SpaceTopBar.tsx` — Priority, Tag, Assignee and
  Ready-only render only when no view has claimed filtering
- `src/routes/screens/board/BoardToolbar.tsx` — claims it
- `test/browser/space-bar.test.ts` — two tests, one per direction
- `test/browser/sprint-strip.test.ts` — the filters-exist guard follows them
- `.tasks/backlog/LAI-293-board-toolbar-moves-above-the-lanes.md` — filed

**Decisions**
- **Hidden, not deleted, and that was the whole difficulty.** The obvious fix —
  delete the four controls from `SpaceTopBar` — breaks every view that is not
  the board. `SpaceLayout` renders the bar for all of them and only the board
  has a toolbar, so Timeline, Calendar and Capacity would have lost filtering
  with nothing to replace it. **The second test exists for exactly that
  regression** and I mutation-checked it by forcing the controls off.
- **A claim, not a path check.** `SpaceLayout` already branches on
  `path === '/board'` for the presence strip and copying it would have been the
  shorter diff. It would also have encoded a contingent fact — *the route is the
  board* — where the real property is *a view supplied its own filtering*. The
  next view to grow a toolbar now gets the behaviour without anyone editing the
  bar. It paid off within the hour: the other session needs the same mechanism
  for Search and the avatars.
- **The `Agents` chip stays.** It looks like a fifth duplicate and is not: it
  carries a live presence count the Filter panel has no equivalent for.
- **Updated the two guards rather than deleting them.** Both asserted where the
  filters live, which LAI-290 deliberately changes; both still assert what they
  were really protecting — no second row, and the filters remain reachable.

**Discovered**
- **`innerText` returns the *rendered* casing.** `sprint-strip` failed on
  `/Priority/` because the panel's labels are uppercased by CSS and the DOM text
  comes back `PRIORITY`. The assertion was right about the content and wrong
  about the case, and the failure looked like a broken fix.
- **My overflow probe never ran, twice.** First `setTheme(h, …)` when it takes a
  `Page`; then the import edit collided with a `setTheme` the file already had,
  so the suite died with `SyntaxError` and the run reported only *"test
  failed"*. Same family as the mutation harness that never applied: **an
  instrument that cannot tell "did not fail" from "did not run"**. Measured the
  real instance with Playwright instead, which is better evidence — real data,
  real server: page overflow `0` at 1600/1440/1280/900/420 in both themes.
- nothing filed beyond LAI-293.

**State**
LAI-290 in `.tasks/review/` — ten criteria ticked, all three gates `EXIT 0`.
LAI-293 filed, `depends-on: [LAI-292]`, unclaimed; LAI-292 belongs to the other
session. Nothing in progress.

## 2026-09-19T14:50:35Z — LAI-295

**What changed**
- server/web/src/components/Spinner.tsx — the circular primitive; `currentColor`,
  decorative unless given a `label`
- server/web/src/components/use-delayed.ts — 150ms delay, 300ms hold, measured
  from when it appeared rather than from when work stopped
- server/web/src/components/LoadingState.tsx — `board`, `table`, `drawer` shapes;
  the board shape builds LaneRow's exact grid template including the add tile
- server/web/src/components/states.css — `.spinner`, `.skeleton-body`,
  `.skeleton-lane-add`, `.board-main > .skeleton-list`
- server/web/src/components/forms/Button.tsx — spins and keeps its label;
  `busyLabel` retired
- server/web/src/routes/screens/BoardScreen.tsx — board/list/drawer skeletons
  inside `.board-main`; `mayAddColumn`
- server/web/src/api/use-session.ts — `signIn` marks loading **before** the post
- server/web/src/routes/screens/sprints/use-sprints.ts — mutations keyed, so a
  card spins the button that was pressed
- 21 further screens — the spinner on every confirming action; presence counts
  guarded in SpaceTopBar, ActivityScreen, PresenceStrip, ActivityPanels,
  InviteScreen
- server/web/test/loading-sweep.test.ts (new), test/components/use-delayed.test.ts
  (new), test/components/loading-primitives.test.ts

**Decisions**
- **Keyed `use-sprints`' in-flight mutation rather than spinning on `busy`.**
  `busy` is screen-wide and right for *disabling*; a spinner claims a specific
  control is working, and on `busy` all four buttons on every card would spin
  for one click. Feedback pointing at the wrong control is worse than none.
- **Retired `busyLabel` instead of leaving it for callers.** It had none left
  after the sweep, and a prop with no callers is how a retired decision returns.
- **Took the add-tile gate from the columns, not the project.** `mayConfigure`
  needs `project` — the thing the board is fetching — so it is false for the
  entire time the skeleton is on screen. `mayAddColumn` prefers `project?.id`
  and falls back to the columns', which load separately and carry the same id.
- **Filed the sprint strip rather than fixing it** — another session's file.

**Discovered**
- **`signIn` had its loading flag below the await**, so the most-pressed button
  in the app gave no feedback for the whole round trip and span for the fast
  `/me` re-read afterwards. Found by driving the instance; invisible in review.
- **The board skeleton missed the board four different ways** — beside
  `.board-main` rather than in it, `.skeleton-list` nested in itself, an 11px
  gap against 12px, and the add tile's 32px shared out among the lanes.
  71px jump and 12px per lane, now 0 and 0.
- **The assertion that should have caught the gap never read `.kanban`** — it
  asserted a literal `0.6875rem` under a comment claiming it matched the board.
  A comment claiming more than the assertion proves, in the exact shape §5 warns
  about. It now compares both files.
- **`presence?.present` gives `0` for "not asked yet"**, so "Agents 0" and
  "0 agent sessions running" were shown as facts and then changed.
  `ActivityPanels` already guarded its own count — that is what made the rest
  visible. → LAI-297 filed (discovered-from: LAI-295).
- **A mutation harness with `set -e` and a trailing restore leaves the mutation
  behind on every *successful* catch** — the shell exits on the non-zero test
  run before reaching the `cp`. Five files at once, found only because the next
  anchor refused. The restore is a `trap` now. And `git checkout --` is the
  wrong restore for uncommitted work: it goes to HEAD, not to the pre-mutation
  tree, which silently discarded an edit earlier in the same session.
- **18 mutations, 18 caught** — two only after adding the assertion that was
  missing (the doubled wrapper, and the skeleton's add-tile gate).

**State**
Moved to .tasks/review/. Gate green under the lock: TEST 0, LINT 0, FMT 0
(1999 server, 1020 web). LAI-297 filed for the other session. Instance on
:3381, own database, `uptime_ms` checked on every restart.

## 2026-09-19T14:58:00Z — LAI-296, LAI-297

**What changed**
- `board/group-lanes.ts` — rows sort busiest-first; the `''` bucket trails
- `board/board-rail.css` — `.board-main.board-grouped { overflow-y: auto }`
- `BoardScreen.tsx` — marks the pane grouped; tracks `sprintsLoading`
- `board/SprintStrip.tsx` — `loading` prop, ghost chips, early return moved
  below the hooks
- `board/sprint-strip.css` — `.strip-chip-ghost`
- tests: `group-lanes.test.ts` (+6, 3 updated), `board-lane-scroll.test.ts`
  (+2), `sprint-strip.test.ts` (+2)

**Decisions**
- **Unassigned trails whatever its count.** The owner asked for busiest-first;
  sorting the bucket among people by size would say nobody is busier than Ada.
  It is a bucket, not a workload. Three existing guards asserted the old order
  and were updated, not deleted — the requirement changed, so they follow.
- **The grouped scroll is opt-in (`.board-grouped`), not `overflow-y: auto`.**
  The plain board must keep the LAI-290 clip or the phantom scrollbar returns;
  both halves are asserted in one file because the bug was fixing one and not
  noticing the other.
- **`loading` is a real prop, not `sprints.length === 0`.** Those are different
  facts: a project with no sprints must reserve nothing, or one jump becomes a
  permanent empty band. `setSprintsLoading(false)` sits in `finally`, so a
  failed request also stops reserving.
- **Height comes from the real `All sprints` button**, not a reserved pixel
  count — same CSS as the loaded state, so it cannot drift.

**Discovered**
- **`SprintStrip` had a conditional hook call**, found while fixing the jump:
  `if (sprints.length === 0) return null` sat **above** `useRef`/`useState`/
  `useEffect`, so the empty render ran zero hooks and the next ran three. It
  survived only because the strip was effectively always mounted with its
  sprints in hand — and giving it a loading state makes the empty render the
  normal one, so the fix would have walked into it.
- **My grouped-scroll test passed with the fix removed.** It scrolled the pane
  with `scrollTop = 99_999`, which works fine on `overflow: hidden` — script is
  not bound by the property that stops a person scrolling. Now a wheel gesture
  plus the computed style.
- **A test can pass its assertion and fail the run.** The delaying route was
  still sleeping at `close()`; node reported `unhandledRejection` with
  `# fail 0` printed above a non-zero exit. `unrouteAll({ behavior:
  'ignoreErrors' })` before closing.
- **I collided with the other session on LAI-295.** §3's sweep answered
  `LAI-296` immediately before the `git mv`, as required — and I had already
  hardcoded `295` into the file, so the right answer went into a shell where
  nothing consumed it. Renumbered to LAI-296; two commits keep the wrong id and
  the task file records it. **A check whose result is not wired to the thing it
  guards is decoration.**

**State**
LAI-293, LAI-296 and LAI-297 all in `.tasks/review/`. Nothing in progress.
Gate green: TEST/LINT/FMT all `EXIT 0`.

## 2026-09-19T15:00:13Z — LAI-295 (combined verification with LAI-297)

**What changed**
- no source files — measurement only, after the other SHELL session landed
  LAI-297 on this branch

**Decisions**
- **Measured the two fixes together rather than trusting either report.**
  LAI-295 took the board skeleton's geometry to zero and LAI-297 took the
  sprint strip's 57px; neither of us had measured the *composition*, and the
  strip sits above the board, so only the pair can be checked.

Three request orderings, 1600x1000, own instance on :3381:

| ordering | `.kanban` top during → after | jump |
| --- | --- | --- |
| everything slow | 189 → 189 | **0px** |
| sprints slow only | 189 → 189 | **0px** |
| tasks slow only | 189 → 189 | **0px** |

The first is the case that measured **57px** when LAI-297 was filed.

**Discovered**
- **One residual, in the "everything slow" ordering only**: skeleton lanes draw
  329px against the board's 318px. `board-columns` has not arrived either, so
  the skeleton falls back to four lanes and reserves no add tile — it does not
  know the lane count, and 11px is the smaller of the two errors it is already
  making. Vertical position is exact in all three. Not worth a task: the
  fallback is inherently approximate and says so.
- **None of my tests delay a route**, so the other session's
  `unhandledRejection` finding does not reach them — checked rather than
  assumed. The only route-delaying test on this branch is their
  `browser/sprint-strip.test.ts`, and it carries the
  `unrouteAll({ behavior: 'ignoreErrors' })` fix at line 468.
- **Their conditional-hook find is real and was mine to walk into.**
  `SprintStrip` returned `null` above `useRef`/`useState`/`useEffect`, so an
  empty render ran zero hooks and a loaded one ran three. It survived only
  because the strip was always mounted with sprints in hand — and giving it a
  loading state is exactly what makes the empty render normal. Verified on the
  branch: hooks at 74, 75, 77, 98; the early return now at 114.

**State**
LAI-295 in review, unchanged — this adds no code. Gate re-run on the shared
branch with their work in: TEST 0, LINT 0, FMT 0 (1999 server, 1022 web).

## 2026-09-19T15:03:52Z — LAI-295 (correction: the 11px residual)

**What changed**
- .tasks/backlog/LAI-298-skeleton-reserves-the-add-tile-before-columns-land.md
  — filed, after being wrong about it in the entry above

**Decisions**
- **Withdrew the reasoning for not filing it.** The previous entry called the
  329px-vs-318px residual "inherently approximate", on the grounds that the
  skeleton is already guessing the column count. The other SHELL session
  pointed out those are **two independent terms**: the count is genuinely
  unknown, but the trailing tile costs `(tileWidth + gap) / n` for *any* n and
  does not depend on it. One is an approximation; the other is an omission.
- **Measured it rather than accepting the argument.** Forced `addTile={true}`
  and rebuilt: skeleton lanes went `329 → 318`, exactly matching
  `[318,318,318,318,32]` against the board's `[318,318,318,318,32,1]`.
  **The whole 11px was the tile**; the count fallback of four cost nothing,
  because four is right for this project. Restored by checksum.
- **Filed p3, not p2.** It is 11px, in one request ordering, with `top` exact
  in all three. The common ordering already reserves the tile.

**Discovered**
- **`canConfigureProject` does not need the project id for three of five
  roles** — `owner`/`admin` return true and `viewer` false from `org_role`
  alone (`api/tasks.ts:245`). `me` lands well before `board-columns`, so the
  gate is in hand at skeleton time for most readers, and only `member`/`lead`
  genuinely need the id. That is what makes this deterministic rather than a
  coin flip, and it is the part I had not checked before arguing it was
  unknowable.
- **The empirical run cannot separate the two terms on this project**, because
  the fallback of four is correct here — it can only show the tile accounts for
  all of the observed 11. The separation is analytic, from `LaneRow`'s template.
  Recorded so the next reader does not credit the measurement with more than it
  shows. A five-column project would show both terms.

**State**
LAI-295 still in review, unchanged — this adds no code to it. LAI-298 in
backlog, unclaimed.

## 2026-09-20T00:30:00Z — LAI-606 (phases 0a–9)

**What changed**
- banked the 71-file uncommitted baseline (phase 0a), then ten more commits:
  red-clearance + new tokens, a computed-style-proven `board.css`
  consolidation (27 selectors × 16 props × 2 themes, zero diffs), prototype
  fonts (Inter out of the bundle), the 17-role type system regenerated, the
  Obsidian re-values with the WCAG suite live, the lane chrome, the card
  anatomy, board geometry + dense mode, TAG_COLORS, AA-solved avatars, and
  the 218px meta-row guard

**Decisions**
- **The task file arrived after the work it describes** — recorded in the
  file itself; LAI-605 deliberately skipped (comments cited 606 fifty times).
- **Unknown tags are neutral, not hashed** — a colour that arrives by hash
  looks deliberate, and a deliberate-looking colour that means nothing is
  noise with authority.
- **Per-hue avatar lightness, solved not eyeballed** — one uniform value
  failed AA on three of eight hues; green needs L34 where blue passes at 47.
  The check is `avatar-contrast.test.ts` now, not a hand ritual.
- **In-progress is `--chip-blue`, never `--accent`** — the file coloured the
  status with its blue accent; ours is purple, and a status must not ride
  accent edits.

**Discovered**
- **The bundle orders `type.css` after component CSS**, so a role's
  `color: inherit` beats any single-class colour in `board.css` — measured
  four times (blocked line, footer, tag hues, tab weight). The scoped
  two-class fix is now the house pattern.
- **A mutation can fail to land and read as a surviving guard.** Mutating
  the first `flex-wrap: nowrap` changed a rule a later block overrides —
  computed value unchanged, test "survived". Verify the mutation in the
  computed system before believing either verdict.
- **A guard needs a vacuity check.** At 218px the fixture fits, so the wrap
  assertion could never fail; the 120px squeeze now asserts something
  *overflows*, or the test says it proves nothing.
- **The role sync test was blind to deletions** — both sides derive from one
  object, so a swallowed `body-sm` passed 16 === 16. An independent roster
  assertion closes it.

**State**
Phases 0a–9 committed. Full gate blocked on the peer's untracked
`zz-tmp2.test.ts` (fails tsc) and their uncommitted column-reorder work;
count-badge mapping suite waits on the same file. Screenshots at 1440/1024
both themes taken; delta report written into the task file.
