## 2026-09-18T08:52:11+05:30 — LAI-459 — org settings and org-role management

Submitted to review **red**, under §4.4 step 1. Two assertions fail, both in
CORE's `test/tooling/response-type-coverage.test.ts`, both quoted verbatim in the
task file; `LAI-239` (filed, p1) turns them green. Nothing else fails:
`Tests 2 failed | 1907 passed`. Lint `0`, format `0`.

**Changed**

- `server/web/src/api/org.ts` — new. `Org`, `OrgAi`, `OrgPatch`, `getOrg`,
  `updateOrg`.
- `server/web/src/api/users.ts` — `updateUser(id, {org_role?, is_active?})`.
- `server/web/src/routes/screens/organisation/OrganisationScreen.tsx` — the org
  card, the role select, deactivate/reactivate, the server's refusal.
- `server/web/src/routes/screens/organisation/organisation.css`
- `server/web/test/browser/organisation-roles.test.ts` — new, 14 tests.
- `server/web/test/api/org.test.ts` — new, the CONVENTIONS §4 mirror.
- `server/web/test/browser/harness.ts` — records every `/api/` call; `reply()`
  and `refuse()` for non-200 answers.
- `server/web/test/api/view-type-drift.test.ts` — `Org`↔`OrgView`,
  `OrgAi`↔`OrgAiView`.
- `server/web/test/api/endpoint-coverage.test.ts` — both LAI-459 exemptions gone.
- `server/web/test/routes/screens/organisation/organisation-screen.test.ts` —
  rewritten.

**Decisions**

*Pairing `Org` with `OrgView` is what made the census red, and I did it anyway.*
Creating the client type is what made `'no client type exists'` false. Not
pairing would have left a false claim in CORE's list that no check could see, and
left the org's drift unguarded on the one endpoint carrying §12's settings. The
census's staleness guard caught it in the same run and printed the fix. That list
is CORE's — only the `WEB_*` maps in `structure.test.ts` are mine (D-026) — so
this is §4.4's named case where there is no entry to take that is not a crossing.

*Deleting the old absence-assertions rather than keeping them.* That test read
the screen's source for *no `<select>`, no `<button>`* because `PATCH /users/:id`
answered `404` in LAI-086. **It went red for the change that satisfied its own
task** — LAI-158 again. Replaced by a browser test, because what a person is
*offered* is a property of the rendered page. One new source assertion stayed, as
the thing source is genuinely better at: **no client-side last-owner rule.**

*An Admin is not offered `Owner`* — §3.1's *"(not to Owner)"*, which `can()`
evaluates. Mirroring a stated rule, not inventing one. But **their current role
is always an option regardless**, or a `<select>` whose value matches no option
renders blank and an Owner's row shows no role at all.

**Discovered**

*Two visual defects every assertion passed.* The key tail fell onto its own line
(`dd` is a flex column, so the tail span was an item); and
`.org-person-inactive { opacity: 0.55 }` dimmed the whole row, so **`Reactivate`
— the one control that is somebody's way back — read as greyed-out.** The exact
present-and-disabled confusion this screen argues against, arriving through CSS
rather than a `disabled` attribute.

*A mutation came back GREEN and I kept it visible.* Deleting `.org-ai`'s rule
leaves the provider and tail correctly on one line — **the wrapper element is
what fixes it, not the rule.** My comment had claimed the rule's credit; §5 says
a comment may not claim more than the code under it proves, so the comment was
rewritten rather than the mutation re-aimed.

*My own mirror test was aimed at the wrong file and reported it as a finding
about the server.* It grepped `services/orgs.ts` for `AI_PROVIDERS` and failed
with *"the server no longer declares AI_PROVIDERS as a literal array"* — the
service **re-exports** it from `db/enums.ts`. Same shape as the wrong-location
criteria in CLAUDE.md: a sentence that reads as authoritative and is about where
the test was looking. It now reads `db/enums.ts` and separately asserts the
service re-exports that list, so the client is not compared against a constant
the endpoint never sees.

*And it reported a doc comment explaining a rule as a breach of it* — scanning
`OrgAiView` for `plaintext` hit *"nothing decrypts it… plaintext tail"* in the
comment above the field. `code()` first; that is what the helper exists for.

**Verification:** 14 browser tests. **8/8 screen mutations caught**, harness
checksum-verified on restore. Screenshots both themes at 1280/760/420px,
horizontal overflow `0px` at every width.

**Gate:** `EXIT 1`, and only as quoted above. **Ports:** none.

## 2026-09-18T09:41:30+05:30 — LAI-238 — an admin can revoke another user's tokens

In review. The gate is `EXIT 1` on **exactly the two LAI-239 assertions LAI-459
already submitted red with** and nothing else — this branch carries LAI-459, so
it inherits the colour. Verified by running the gate before and after this
task's commits: same two, same file. Lint `0`, format `0`.

**Changed**

- `server/web/src/components/TokenRow.tsx` + `token-row.css` — new, the one
  token renderer, lifted out of `TokensScreen`.
- `server/web/src/routes/screens/tokens/TokensScreen.tsx`, `tokens.css` — render
  `TokenRow`; the row's styles left with it.
- `server/web/src/api/tokens.ts` — `listUserTokens`, `revokeUserToken`.
- `server/web/src/routes/screens/organisation/use-user-tokens.ts` — new.
- `.../organisation/OrganisationScreen.tsx`, `organisation.css` — the toggle,
  the panel, and the narrow-width row.
- `server/web/test/browser/organisation-tokens.test.ts` — new, 11 tests.
- `server/web/test/browser/organisation-roles.test.ts` — locators narrowed.
- `server/web/test/api/endpoint-coverage.test.ts` — both exemptions removed.
- `server/test/tooling/structure.test.ts` — one `WEB_NO_MIRROR_REQUIRED` entry
  (the `WEB_*` map only, D-026).

**Decisions**

*Claimed with LAI-459 in `review/`, not `done/`.* §2 wants every `depends-on` in
`done/` on `master` and it was not. CHIEF accepted LAI-459 and directed this one
next; the hold is CORE's unrelated two lines. Recorded in the task file as a
deviation on CHIEF's call rather than a reading in which the rule was satisfied.

*The panel sits behind the same gate as the role controls.* `token.list_any` and
`token.revoke_any` are Owner-and-Admin in §3.1 — the same grade — so reusing that
gate beats a second one that would have to be kept in step with it.

*Extracted the row rather than copying it.* Two copies of a row that says what a
credential can do is how one of them quietly stops matching the other, and this
is the row where "quietly wrong" means somebody believes a token is revoked.

*One criterion read wider than written, and flagged.* *"prefix, name, last used,
and nothing else"* — the shared row also shows scope, projects and expiry.
The criterion's subject is the token **value**, which is absolute and absent.
Trimming the rest here would have recreated two different rows.

**Discovered**

*The extraction was verified by screenshot, not by reasoning.* `TokensScreen` is
byte-for-byte identical to its pre-extraction baseline — checked after the move
and again after the narrow-width CSS. *"It should be fine"* is not an instrument.

*`.tok-revoke`'s geometry came from a rule grouped with `.tok-copy` and
`.tok-dismiss`* — buttons that stay behind. Splitting the group was the price of
the move; the grouping was convenience, not a relationship.

*A fixture pinned to an absolute epoch, again.* `TokenRow` computes "Used 1h ago"
against the screen's `Date.now()`, so a fixed-epoch fixture read as 240 days.
**LAI-420's shelf-life bug exactly**, second time. Offsets from `Date.now()` now.

*The panel overflowed 89px at 420px and every assertion passed*, because the
theme test measured overflow at one width. Then the narrow fix broke wide —
111px at 1280px — because **`.org-people li` has no `flex-wrap` at the base
width**, being shared with the invites list, so a full-width item had no second
line to wrap onto. Found by enumerating elements whose right edge passed the
viewport rather than guessing at the cascade a third time. The check now runs at
three widths **and** asserts the panel is *below* the name: zero overflow is
necessary and not sufficient, because content can be clipped instead of pushing.

*This task broke LAI-459's tests, and that was the right signal.* A second button
on the person row made `row.locator('button')` a strict-mode violation in five
places. Narrowed to `.org-deactivate` / `.org-reactivate` — **except two that
were deliberately left broad**: "a member is offered no button" and "nothing on
my own row" are assertions about *any* ungated control arriving, which is exactly
this task's shape. Both now say so in a comment.

**Verification:** 11 browser tests; **9/9 mutations caught** across five files,
all checksum-verified on restore. A fixture carrying a `secret` the server never
sends, asserting no part of it renders. Screenshots both themes at 1280 and 420.

**Gate:** `EXIT 1`, inherited only. **Ports:** 3371, the owner's local instance,
still running.

## 2026-09-18T10:34:07+05:30 — LAI-240 — the client never asked for inactive users

In review. `pnpm test` from the root is **`EXIT 0`, 1932 passed** — LAI-239
landed so LAI-459's inherited red is gone. `pnpm lint` is **`EXIT 1` on
`master`**, in CORE's area; reported, not touched.

**Changed**

- `server/web/src/api/users.ts` — `ListUsersQuery.includeInactive`;
  `listAllUsers(signal, {includeInactive?, maxPages?})`, flag carried on every
  page.
- `.../organisation/use-organisation.ts` — passes it. A **directory**.
- `.../screens/AddMemberForm.tsx` — does not. A **picker**, now stated.
- `server/web/test/api/users.test.ts`, `.../browser/organisation-roles.test.ts` —
  four new tests, all on the URL or the cross-screen count.

**Decisions**

*Asserted on the request URL, not the rendered list.* The old test asserted the
list and `harness.ts` matches on path alone, so the fixture answered regardless
of the query — which is how this defect survived a test claiming to cover it.
`page.on('request')` needs no harness change and is what LAI-240's own AC3 says
to use while LAI-241 is outstanding.

*AC1 asserted as the symptom.* Organisation's row count must equal Capacity's.
Neither number is wrong-looking alone; **the disagreement is the bug**, and it is
what a user actually reports.

*The two callers ask different questions and each now says which*, rather than
being made uniform. A directory wants people who left, because they still hold
history; a picker does not, because you cannot hand work to somebody locked out.

*`listAllUsers` takes an options object now*, so `maxPages` stopped being
positional. One existing test moved to `{ maxPages: 3 }`; no other caller passed
it.

**Discovered**

*`lsof -ti tcp:<port>` matches the browser, and §4.3 tells you to kill it.*
Restarting the local instance, that command returned **two** pids — the node
listener and **Google Chrome**, which held an open connection to the page. The
kill got a two-line string and did nothing, the new server hit `EADDRINUSE` and
exited, and **the health check answered `200` from the old process**:
`uptime_ms: 567918` — 9.5 minutes on a server started five seconds earlier.
§4.3's own `uptime_ms` rule caught it; §4.3's own command caused it. One flag
fixes it — `lsof -ti tcp:<port> -sTCP:LISTEN`. Reported to CHIEF; `CLAUDE.md` is
theirs.

*The gate has two exit codes and CLAUDE.md §5 names one.* `pnpm test` passes
while `pnpm lint` has been red on `master` since LAI-466. Somebody running the
documented command sees green. Same shape as the rest of this task — an
instrument that cannot see the thing.

**Verification:** 4/4 mutations caught across both test files, restored by
checksum. **Including the mutation that reproduces the original defect** — the
directory dropping the flag — which the previous test could not see. On the live
instance: Organisation **4 → 5**, Capacity 5, the two now agree; chip and
`Reactivate` reachable; request carries `include_inactive=true`.

**Gate:** `pnpm test` `EXIT 0`. `pnpm lint` `EXIT 1`, inherited from `master`.
**Ports:** 3371, listener pid 52587, still running for the owner.

## 2026-09-18T11:02:55+05:30 — LAI-241 — the harness can be told to care about a query

In review. `pnpm test` **`EXIT 0`, 1932 passed**. `pnpm lint` **`EXIT 1` on
`master`** in CORE's area — unchanged, same as LAI-240, reported not touched.

**Changed**

- `server/web/test/browser/harness.ts` — a stub key may carry a query and is then
  required. Subset matching, most-specific-first, opt in, no fall-through, and
  `harness.unmatched` carrying the URL actually requested.
- `server/web/test/browser/harness-query.test.ts` — new, four tests.

**Decisions**

*Opt in, not opt out.* A key with no `?` behaves exactly as before. Making every
fixture query-exact would have broken dozens of tests to fix a handful — CHIEF's
constraint and the right one.

*No fall-through to a path-only stub.* Once any key for a path names a query, an
unmatched request is refused. Answering it from the broad stub would be this
defect with an extra step.

*Subset, not exact.* The key names the parameters it cares about; `listAllUsers`
adds `cursor` on page two and a fixture should not have to predict that.

**Discovered**

*One mutation came back GREEN and the test was at fault, not the code.*
`subset becomes exact` was uncaught: the test named *"matching is by subset, so
unrelated parameters do not break it"* used a single-page fixture, so the only
parameter sent was the one the key named — **exact and subset are
indistinguishable there**. A test named for a property it does not exercise, which
is §5's comment rule arriving in a test title. Fixed with a two-page fixture so
the second request really carries `cursor`; red now. **Reported rather than
re-aimed** — the green was the finding.

*The AC4 audit was instrumented, not reasoned.* A temporary probe logged every
`/api/` request carrying a query, per file, across all twelve. Ten carry one; two
(`capacity`, `first-boot-status`) carry none. **No assertion depends on a query
besides LAI-240's** — the rest are `limit=` caps and `?project=` on the SSE
stream, and **a fixture returns its list regardless of `limit`**, so dropping it
changes no assertion.

*One nearest miss, named.* `card-click.test.ts` exercises a genuine filter —
`activity?task_id=t1&limit=50` — and escapes only because its fixture is empty.
Fill it in and the assertion becomes unfalsifiable unless the stub is keyed. Not
changed: AC4 says report, §3 says file rather than do.

**Verification:** written failing first — all four red against the old harness
before it was touched. **5/5 mutations caught** after the test fix, harness
restored by checksum each time.

**Gate:** `pnpm test` `EXIT 0`. `pnpm lint` `EXIT 1`, inherited from `master`.
**Ports:** 3371, still up for the owner.

## 2026-09-18T11:38:52+05:30 — LAI-175 — board lanes keep their width and scroll

In review. Owner-reported, p1. `pnpm test` **`EXIT 0`, 1932 passed**; `pnpm lint`
`EXIT 1` on `master` in CORE's area, inherited and unchanged.

**Changed**

- `server/web/src/routes/screens/board/board.css` — `.kanban` gets a
  `12.875rem` track floor, `overflow-x: auto` and scrollbar clearance; **both**
  `@media (max-width: 1100px)` blocks deleted.
- `server/web/test/browser/board-lane-scroll.test.ts` — new, five tests.

**Decisions**

*The floor is measured from the design, not chosen.* Rendered
`docs/design/Laika Prototype.dc.html` in a browser: the grid computes to
`206px ×5`, gap 11px. Not calculated from canvas arithmetic — the board-only
frame has no sidebar and is marked superseded, so a mis-identified part would
have produced a plausible wrong number.

*The old comment blamed the floor; the floor was never the bug.* It said
`minmax(13rem, …)` forced the grid past 1100px and the last column ran under the
rail. **13rem is 208px — the design's own number.** The bug was a floor with
nowhere to overflow to. `overflow-x: auto` on `.kanban`, which is `flex: 1`
beside the rail, is what was missing.

**Discovered**

*There were two `max-width: 1100px` blocks, not one.* The task quotes the first;
the second sits 370 lines lower setting `minmax(12rem, …)` = 192px. **My own
test found it** — the floor held at every width except 1100. Both gone.

*A green mutation, and it was the most important one.* Removing `min-width: 0`
from `.board-main > .kanban` — the exact shape of the original collision —
turned nothing red. Measured rather than re-aimed: `min-width` computes to
**`0px` either way** and the geometry is identical, because a flex item that is a
scroll container resolves `min-width: auto` to zero. **The mutation is a no-op,
so the green is right — but my docblock had credited `min-width: 0` with the
property.** §5's comment rule, third time today. Corrected to say `overflow-x`
carries it alone.

*My own removal guard matched its own docblock.* The check asserting no
`1100px` rule survives fired on the prose explaining why the rule was removed —
`test/helpers/code.ts`'s whole reason for existing, reproduced in a check I had
just written. Fixed by stripping comments before scanning.

*ListView does not share the behaviour.* A `<table>`, not a five-track grid;
cells reflow rather than squeeze. Measured at 1280: right edge 984 vs rail 996,
no scroll needed, no page overflow. No change — the task asked to say either way.

**Verification:** 4/4 real mutations caught (floor removed · scroller removed ·
clearance removed · smaller-than-design floor). Live instance at 1680/1440/1280:
lanes 222/206/206, scrolls no/yes/yes, strip right edge 12px short of the rail at
every width, page overflow 0. Both themes.

**For the owner:** where the lanes do not fit, the rightmost visible lane is
clipped by the scrollport — the standard "there is more" affordance and what was
asked for. It resembles the original screenshot and is a different mechanism:
before the lane was too narrow, now it is scrolled. Worth an eye.

**Gate:** `pnpm test` `EXIT 0`. `pnpm lint` `EXIT 1`, `master`'s.
**Ports:** 3371, listener 30653.

## 2026-09-18T12:24:40+05:30 — LAI-244 — the Live stream rail scrolls with the lanes

In review. Owner asked directly, with a screenshot, right after LAI-175 landed:
*"also add that live stream in the scroll row so that will not fixed there on
screen"*. `pnpm test` **`EXIT 0`, 1938 passed**; `pnpm lint` `EXIT 1` on
`master`, CORE's area, inherited.

**Changed**

- `server/web/src/routes/screens/board/board-rail.css` — `.board-main` is the
  scroller now (`overflow-x: auto`, `position: relative`, scrollbar clearance);
  `.board-main > .kanban` is `flex: 1 1 0; min-width: min-content`.
- `server/web/src/routes/screens/board/board.css` — the scroller moved off
  `.kanban`; the track floor stays.
- `server/web/test/browser/board-lane-scroll.test.ts` — assertions rewritten
  around the property; fixture given three sprints and three activity rows.

**Decisions**

*The scroller moved up one level* so the rail is inside it. That is the whole
ask: before, the lanes slid under a rail that never moved.

*`flex: 1 1 0` with `min-width: min-content`.* Two other shapes were tried and
measured wrong: `flex: 1 0 auto` sizes the grid to max-content, which `1fr`
leaves unbounded (311px lanes, 1602px strip); `min-width: 0` lets the tracks
overflow a box nothing scrolls, which is the lanes-under-the-rail collision back
again, silently, at 1440px.

**Discovered**

*The 146px page overflow was never the board.* Every box measured correctly
while the page gained a horizontal scrollbar. Bisecting found
**`span.visually-hidden`** inside the rail's feed: `position: absolute` with no
coordinates, so it sits at its static position — in the scrolled-out part of the
rail — and an **unpositioned** scroller is not its containing block, so
`overflow-x` never clips it. `position: relative` on `.board-main` fixes it;
**LAI-245** files the general case, because every future scroller inherits it and
the symptom is a page scrollbar with no visibly oversized element.

*Two mutations came back green and both were the test's fault.*
`position: relative` removed was **green** because the fixture's rail had no feed
rows, so no `.visually-hidden` existed in the scrolled region; `flex: 1 0 auto`
restored was **green** because the lane assertion was `>= 206` and 311 satisfies
it. Fixed in the test — a representative fixture, and an upper bound that applies
whenever the row scrolls. **A floor alone is half an assertion.** 5/5 after.

*Two id problems, both mine.* CHIEF filed LAI-243 concurrently, so this task
renumbered 243 → 244 — **and its two code commits are still tagged `[LAI-243]`**,
now somebody else's id. They stand; §4 forbids rewriting them. Then I filed a
second task as LAI-244 from a "lowest free" check taken before the renumber
landed; it moved to LAI-245. **D-017 from a direction it does not describe:
"lowest unused number" is not a lock against yourself when two filings straddle a
rename.**

*And a check that was wrong rather than a file that was.* I re-added an assertion
already present, having grepped for a phrase the existing version did not use.
`tsc` caught it.

**Verification:** live instance at 1920/1600/1440/1280/1220 — lanes
270/206/206/206/206, row scrolls where it must, strip's right edge 12px short of
the rail at every width, page overflow **0** everywhere. Rail travels 321px on a
full scroll. Both themes.

**Gate:** `pnpm test` `EXIT 0`. `pnpm lint` `EXIT 1`, `master`'s.
**Ports:** 3371, still up for the owner.

## 2026-09-18T13:41:07+05:30 — LAI-246 — the design's contrast-lifted tokens

In review. **All three gates `EXIT 0`** — test 1938 passed, lint, format. CORE's
`restore-drill` lint break is fixed, so nothing is inherited any more.

The owner redirected the whole queue: the app should follow the Claude Design
prototype's flow and philosophy, keeping every feature. This is the first and
smallest piece of that.

**Changed**

- `server/web/src/theme/tokens.css` — 9 values, light and dark.

**Decisions**

*Read the design through the MCP before building.* The plan's Phase 0 was
*"confirm the source"*, and it earned itself immediately — see below.

*Applied the design's tint asymmetry verbatim.* `--purs/--grns/--ambs/--reds`
and their borders still carry the **old** rgb in the design while their solids
moved; only `--accs/--accb` moved together. Matching the design was the
instruction, so it is matched — flagged in the task as probable oversight rather
than silently "corrected".

**Discovered**

*The repo's design was not stale — one file was.* I told CHIEF "docs/design is
stale" and they wrote that into `docs/design/README.md` on my word. Hashing all
but one file against the MCP: **five of six identical**; only
`Laika Prototype.dc.html` differs (94 hunks, +16.5 KB). `Laika 05-07` returned
inline rather than persisting and was never hashed — stated as unverified rather
than assumed. **The correction is in the task file and went to CHIEF.**

*13 → 9, caught by this task's own AC.* Filed as 13 changed tokens; it is 9.
`--mk/--ta/--sv/--jd/--rb` are design-only avatar colours we derive in
`avatar-color.ts` and have never stored. I had counted the design's changed list
rather than its intersection with ours. **AC3 — *by name, not by count* —
caught it in the task that wrote it.** A further 24 declarations differ only in
Prettier's formatting; normalised before comparing.

*Another session was writing into this worktree.* The previous SHELL was
mid-LAI-242 in `Laika-shell/` — 17 modified files and 3 new ones I did not
write. **Noticed only because a Playwright script failed**: it looked for
`getByRole('radio', {name:'Light'})` and the radios had become a button
mid-run. **A shared worktree does not announce itself; it makes the tests lie.**
Staged only `tokens.css`, by explicit path, after checking the diff was exactly
my nine lines; touched none of their work; did not gate until CHIEF had them
stand down and `git status` was empty.

**Verification:** every shared token compared to the design by name after the
edit — none differs. Contrast computed both themes: `--tx3` on `--page`
**2.67 → 4.99**, on `--card` 3.04 → 5.68, dark 4.06 → 7.01; accent/red/green/
amber/purple all 3.18–4.52 → **4.51–6.29**, every pair now AA. Values read back
out of the running browser, not the file. Screenshots both themes at 1440.

**Also:** six design files exported to `<scratchpad>/design-live/` for CHIEF,
who cannot reach the MCP. **LAI-041 is closed by this** — the owner answered its
contrast question in the design itself (CHIEF recorded D-059).

**Gate:** all three `EXIT 0`. **Ports:** 3371, listener 63038.
