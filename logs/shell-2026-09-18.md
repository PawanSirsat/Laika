## 2026-09-18T08:52:11+05:30 — LAI-459 — org settings and org-role management

Submitted to review **red**, under §4.4 step 1. Two assertions fail, both in
CORE's `test/tooling/response-type-coverage.test.ts`, both quoted verbatim in the
task file; `LAI-239` (filed, p1) turns them green. Nothing else fails:
`Tests 2 failed | 1907 passed`. Lint `0`, format `0`.

**Changed**

- `server/web/src/api/org.ts` — new. `Org`, `OrgAi`, `OrgPatch`, `getOrg`,
  `updateOrg`.
- `server/web/src/api/users.ts` — `updateUser(id, {org_role?, is_active?})`.
- `server/web/src/routes/screens/organisation/OrganisationScreen.tsx` — the org
  card, the role select, deactivate/reactivate, the server's refusal.
- `server/web/src/routes/screens/organisation/organisation.css`
- `server/web/test/browser/organisation-roles.test.ts` — new, 14 tests.
- `server/web/test/api/org.test.ts` — new, the CONVENTIONS §4 mirror.
- `server/web/test/browser/harness.ts` — records every `/api/` call; `reply()`
  and `refuse()` for non-200 answers.
- `server/web/test/api/view-type-drift.test.ts` — `Org`↔`OrgView`,
  `OrgAi`↔`OrgAiView`.
- `server/web/test/api/endpoint-coverage.test.ts` — both LAI-459 exemptions gone.
- `server/web/test/routes/screens/organisation/organisation-screen.test.ts` —
  rewritten.

**Decisions**

*Pairing `Org` with `OrgView` is what made the census red, and I did it anyway.*
Creating the client type is what made `'no client type exists'` false. Not
pairing would have left a false claim in CORE's list that no check could see, and
left the org's drift unguarded on the one endpoint carrying §12's settings. The
census's staleness guard caught it in the same run and printed the fix. That list
is CORE's — only the `WEB_*` maps in `structure.test.ts` are mine (D-026) — so
this is §4.4's named case where there is no entry to take that is not a crossing.

*Deleting the old absence-assertions rather than keeping them.* That test read
the screen's source for *no `<select>`, no `<button>`* because `PATCH /users/:id`
answered `404` in LAI-086. **It went red for the change that satisfied its own
task** — LAI-158 again. Replaced by a browser test, because what a person is
*offered* is a property of the rendered page. One new source assertion stayed, as
the thing source is genuinely better at: **no client-side last-owner rule.**

*An Admin is not offered `Owner`* — §3.1's *"(not to Owner)"*, which `can()`
evaluates. Mirroring a stated rule, not inventing one. But **their current role
is always an option regardless**, or a `<select>` whose value matches no option
renders blank and an Owner's row shows no role at all.

**Discovered**

*Two visual defects every assertion passed.* The key tail fell onto its own line
(`dd` is a flex column, so the tail span was an item); and
`.org-person-inactive { opacity: 0.55 }` dimmed the whole row, so **`Reactivate`
— the one control that is somebody's way back — read as greyed-out.** The exact
present-and-disabled confusion this screen argues against, arriving through CSS
rather than a `disabled` attribute.

*A mutation came back GREEN and I kept it visible.* Deleting `.org-ai`'s rule
leaves the provider and tail correctly on one line — **the wrapper element is
what fixes it, not the rule.** My comment had claimed the rule's credit; §5 says
a comment may not claim more than the code under it proves, so the comment was
rewritten rather than the mutation re-aimed.

*My own mirror test was aimed at the wrong file and reported it as a finding
about the server.* It grepped `services/orgs.ts` for `AI_PROVIDERS` and failed
with *"the server no longer declares AI_PROVIDERS as a literal array"* — the
service **re-exports** it from `db/enums.ts`. Same shape as the wrong-location
criteria in CLAUDE.md: a sentence that reads as authoritative and is about where
the test was looking. It now reads `db/enums.ts` and separately asserts the
service re-exports that list, so the client is not compared against a constant
the endpoint never sees.

*And it reported a doc comment explaining a rule as a breach of it* — scanning
`OrgAiView` for `plaintext` hit *"nothing decrypts it… plaintext tail"* in the
comment above the field. `code()` first; that is what the helper exists for.

**Verification:** 14 browser tests. **8/8 screen mutations caught**, harness
checksum-verified on restore. Screenshots both themes at 1280/760/420px,
horizontal overflow `0px` at every width.

**Gate:** `EXIT 1`, and only as quoted above. **Ports:** none.
