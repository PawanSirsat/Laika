
## LAI-455 → review. The Meeting review screen — M6's exit criterion.

**Changed**
- `src/api/meeting-reviews.ts`, `routes/screens/meeting-review/` (screen, derive,
  css), route table, `AppShell`.
- Tests: `review-derive.test.ts` (17), `browser/meeting-review.test.ts` (10),
  `api/meeting-reviews.test.ts` (9). Drift/census/nav expectations updated.

**Decisions**
- *Nothing accepted by default; an empty set cannot be applied.* The control is
  unavailable rather than sending nothing and reporting success.
- *A refusal is amber, not red.* A member accepting a lead-only `decision` is the
  ordinary case (LAI-451), not a fault, and red would tell them otherwise.
- *No "accept all".* If one is added it should be a decision with a reason, not a
  convenience that arrives during implementation.

**Discovered**

**AC2 is half stale and AC4 was not met by my first pass.** D-056 removed the
transcript pane, so *"locatable in the transcript pane"* has nothing to locate
it in — the quote is the whole of the evidence. And **AC4 I got wrong**: the
proposal carries only the new value, so I rendered `status done` until I looked
at the screen. The before now comes from the board, and is **omitted rather than
invented** where the board does not hold the task.

**Two mutations came back green; one was a real gap.**

*The selection not being reset between reviews* was genuinely uncovered — my
tests only ever opened one review, and carrying a tick across means applying
something accepted while reading a different meeting. Test added.

*Discard next to apply* stayed green through three attempts because **the
separation is structural, not CSS** — the accepted-count sits between the two
controls. 520px with two CSS separators removed. **Moving the button inside the
apply group turns it red at 12px**, which is the regression that matters.

**And I hit the same census wall as LAI-439** — `ApplyReviewResult` is not a
`*View`, so it cannot be paired. Second time; `meeting-reviews.test.ts` compares
it directly, including optionality, which the drift check discards.

**LAI-460's check confirmed this task's own wiring** by going green when its four
`LAI-455` exemptions were removed — the endpoint-coverage axis working hours
after it was built.

**Gate:** `EXIT 0` — 1909 server, 687 web, 75 cli. Eleven mutations red.

**Ports:** none.
