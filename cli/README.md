# cli/ — owned by **Builder-B**

The `laika` npm CLI (`npx laika init`, token setup, plugin install helpers).
Deliberately empty until M4 — do not start work here without a task file.

If you need a change here, write a task file in `.tasks/backlog/` with
`area: cli`.
